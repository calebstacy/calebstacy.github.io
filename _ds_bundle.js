/* @ds-bundle: {"format":4,"namespace":"CalebStacyPortfolioDesignSystem_4a3883","components":[{"name":"ScrollCue","sourcePath":"components/actions/ScrollCue.jsx"},{"name":"StageAction","sourcePath":"components/actions/StageAction.jsx"},{"name":"CaseMeta","sourcePath":"components/case/CaseMeta.jsx"},{"name":"CaseNav","sourcePath":"components/case/CaseNav.jsx"},{"name":"CaseQuote","sourcePath":"components/case/CaseQuote.jsx"},{"name":"CaseSection","sourcePath":"components/case/CaseSection.jsx"},{"name":"MetricStrip","sourcePath":"components/case/MetricStrip.jsx"},{"name":"StoryBeat","sourcePath":"components/case/StoryBeat.jsx"},{"name":"VersoHero","sourcePath":"components/case/VersoHero.jsx"},{"name":"DocFigure","sourcePath":"components/document/DocFigure.jsx"},{"name":"Endnotes","sourcePath":"components/document/Endnotes.jsx"},{"name":"NoteRef","sourcePath":"components/document/Endnotes.jsx"},{"name":"References","sourcePath":"components/document/Endnotes.jsx"},{"name":"Finding","sourcePath":"components/document/Finding.jsx"},{"name":"PaperTable","sourcePath":"components/document/PaperTable.jsx"},{"name":"Report","sourcePath":"components/document/Report.jsx"},{"name":"ReportSection","sourcePath":"components/document/ReportSection.jsx"},{"name":"ArtifactFigure","sourcePath":"components/evidence/ArtifactFigure.jsx"},{"name":"BeforeAfter","sourcePath":"components/evidence/BeforeAfter.jsx"},{"name":"CaptureFigure","sourcePath":"components/evidence/CaptureFigure.jsx"},{"name":"CardGrid","sourcePath":"components/evidence/CardGrid.jsx"},{"name":"DataTable","sourcePath":"components/evidence/DataTable.jsx"},{"name":"DisciplineVenn","sourcePath":"components/evidence/DisciplineVenn.jsx"},{"name":"EvidenceTrace","sourcePath":"components/evidence/EvidenceTrace.jsx"},{"name":"FactsPanel","sourcePath":"components/evidence/FactsPanel.jsx"},{"name":"FingerprintBars","sourcePath":"components/evidence/FingerprintBars.jsx"},{"name":"GateRun","sourcePath":"components/evidence/GateRun.jsx"},{"name":"GridCard","sourcePath":"components/evidence/GridCard.jsx"},{"name":"IndexRows","sourcePath":"components/evidence/IndexRows.jsx"},{"name":"Ledger","sourcePath":"components/evidence/Ledger.jsx"},{"name":"Outcome","sourcePath":"components/evidence/Outcome.jsx"},{"name":"Pipeline","sourcePath":"components/evidence/Pipeline.jsx"},{"name":"PullQuote","sourcePath":"components/evidence/PullQuote.jsx"},{"name":"ResearchGrid","sourcePath":"components/evidence/ResearchGrid.jsx"},{"name":"ResumeEntry","sourcePath":"components/evidence/ResumeEntry.jsx"},{"name":"RuleSplit","sourcePath":"components/evidence/RuleSplit.jsx"},{"name":"SourceMap","sourcePath":"components/evidence/SourceMap.jsx"},{"name":"TenGrid","sourcePath":"components/evidence/TenGrid.jsx"},{"name":"Toolchain","sourcePath":"components/evidence/Toolchain.jsx"},{"name":"VoiceProfile","sourcePath":"components/evidence/VoiceProfile.jsx"},{"name":"VoiceScoreRing","sourcePath":"components/evidence/VoiceScoreRing.jsx"},{"name":"Eyebrow","sourcePath":"components/foundation/Eyebrow.jsx"},{"name":"LineArrow","sourcePath":"components/foundation/LineArrow.jsx"},{"name":"ProvenanceNote","sourcePath":"components/foundation/ProvenanceNote.jsx"},{"name":"RegistrationMark","sourcePath":"components/foundation/RegistrationMark.jsx"},{"name":"Tag","sourcePath":"components/foundation/Tag.jsx"},{"name":"Panel","sourcePath":"components/layout/Panel.jsx"},{"name":"PanelRow","sourcePath":"components/layout/Panel.jsx"},{"name":"PaperFrame","sourcePath":"components/layout/PaperFrame.jsx"},{"name":"Stage","sourcePath":"components/layout/Stage.jsx"},{"name":"StageCopy","sourcePath":"components/layout/StageCopy.jsx"},{"name":"StageCurtain","sourcePath":"components/layout/StageCurtain.jsx"},{"name":"StorySection","sourcePath":"components/layout/StorySection.jsx"},{"name":"ChapterNav","sourcePath":"components/navigation/ChapterNav.jsx"},{"name":"IndexRail","sourcePath":"components/navigation/IndexRail.jsx"},{"name":"MobileIdentityBar","sourcePath":"components/navigation/MobileIdentityBar.jsx"},{"name":"MobileMenu","sourcePath":"components/navigation/MobileMenu.jsx"},{"name":"ProjectSpineItem","sourcePath":"components/navigation/ProjectSpineItem.jsx"},{"name":"UtilityNav","sourcePath":"components/navigation/UtilityNav.jsx"}],"sourceHashes":{"components/actions/ScrollCue.jsx":"8bb658147fda","components/actions/StageAction.jsx":"5a98543af3b5","components/evidence/ArtifactFigure.jsx":"127fb9077aa2","components/evidence/CardGrid.jsx":"803ac13ab7bd","components/evidence/DataTable.jsx":"e463933290ef","components/evidence/FactsPanel.jsx":"d1281305f605","components/evidence/GridCard.jsx":"fc995a72a9ed","components/evidence/Ledger.jsx":"30ffa1dd140d","components/evidence/Outcome.jsx":"87e9bb425f06","components/evidence/Pipeline.jsx":"0a78361dc86f","components/evidence/PullQuote.jsx":"b00277528fb2","components/evidence/ResumeEntry.jsx":"022a5e5c0dec","components/evidence/SourceMap.jsx":"223f6d8e812b","components/evidence/TenGrid.jsx":"189c24069a2b","components/foundation/Eyebrow.jsx":"339d08d17838","components/foundation/LineArrow.jsx":"d1caad2e912f","components/foundation/ProvenanceNote.jsx":"b8816411ea6e","components/foundation/RegistrationMark.jsx":"75a36243789a","components/foundation/Tag.jsx":"5778ff0b56c7","components/layout/PaperFrame.jsx":"0d2965baa9ba","components/layout/Stage.jsx":"1e5269281805","components/layout/StageCopy.jsx":"007e4ad0a85a","components/layout/StageCurtain.jsx":"2fe263912177","components/layout/StorySection.jsx":"e97b2c28af96","components/navigation/ChapterNav.jsx":"c1cb8c701f50","components/navigation/IndexRail.jsx":"82250570440b","components/navigation/ProjectSpineItem.jsx":"072cc8885ec3","components/navigation/UtilityNav.jsx":"d680f52dcd23","components/evidence/RuleSplit.jsx":"df64f7a3e979","components/evidence/ResearchGrid.jsx":"7ac09d17bc27","components/evidence/FingerprintBars.jsx":"662c407f2939","components/evidence/EvidenceTrace.jsx":"1c52a31cba97","components/evidence/IndexRows.jsx":"d1cf16db6e0d","components/evidence/DisciplineVenn.jsx":"e1565f7822a0","components/evidence/VoiceScoreRing.jsx":"f1d9d92fe57c","components/evidence/BeforeAfter.jsx":"acae0740ee64","components/evidence/VoiceProfile.jsx":"3760a08b0d90","components/layout/Panel.jsx":"5013c544891a","components/evidence/GateRun.jsx":"fc67aaf9e806","components/evidence/CaptureFigure.jsx":"d015bc84a966","components/document/Report.jsx":"d526bb6cda51","components/document/ReportSection.jsx":"1131f245e776","components/document/DocFigure.jsx":"ad268702d487","components/document/Endnotes.jsx":"70804d8c8006","components/evidence/Toolchain.jsx":"5e1d68775f89","components/document/Finding.jsx":"72c749fc75c2","components/document/PaperTable.jsx":"f6bb51014d4e","components/case/CaseSection.jsx":"09e3d510dfaa","components/case/StoryBeat.jsx":"3b7ad214cf82","components/case/CaseQuote.jsx":"851b5a124fe4","components/case/MetricStrip.jsx":"5f86becb4df6","components/case/CaseNav.jsx":"e12f0f5468cf","components/navigation/MobileIdentityBar.jsx":"e8e721c45205","components/navigation/MobileMenu.jsx":"923357937138","components/case/CaseMeta.jsx":"af757cdf13ec","components/case/VersoHero.jsx":"344f09715365"},"inlinedExternals":[],"unexposedExports":[]} */

(() => {

const __ds_ns = (window.CalebStacyPortfolioDesignSystem_4a3883 = window.CalebStacyPortfolioDesignSystem_4a3883 || {});

const __ds_scope = {};

(__ds_ns.__errors = __ds_ns.__errors || []);

// components/actions/ScrollCue.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Circled arrow plus a short promise, telling the reader the document continues. */
function ScrollCue({
  label = "The case study continues",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    "aria-hidden": "true",
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "center",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", null, label), /*#__PURE__*/React.createElement("i", {
    style: {
      width: 26,
      height: 26,
      display: "grid",
      placeItems: "center",
      border: "1px solid var(--stage-line)",
      borderRadius: "var(--radius-round)",
      color: "var(--signal-stage)",
      animation: "scroll-nudge var(--dur-nudge) ease-in-out infinite",
      fontStyle: "normal"
    }
  }, "\u2193"));
}
Object.assign(__ds_scope, { ScrollCue });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ScrollCue.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ArtifactFigure.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Framed artifact with a gutter-aligned provenance caption. */
function ArtifactFigure({
  src,
  alt = "",
  label,
  caption,
  contain = false,
  zoom = false,
  minHeight = 300,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", _extends({
    className: zoom ? "ds-figure-zoom" : undefined,
    style: {
      margin: 0,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight,
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--surface-mat)",
      boxShadow: "var(--story-shadow)",
      padding: contain ? "clamp(16px,2.2vw,32px)" : 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: "100%",
      height: "100%",
      minHeight: minHeight - 40,
      display: "block",
      objectFit: contain ? "contain" : "cover"
    }
  })), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { ArtifactFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ArtifactFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CardGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Hairline grid block. The ground and the offset shadow make it read as one object. */
function CardGrid({
  columns = 2,
  raised = true,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c)))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = highlight ? value.value : value;
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Contribution facts: mono label in the gutter, the claim at fact size beside it. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    }
  }, rest), facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "clamp(20px,2.2vw,30px) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content column. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "16px 12px 16px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "16px 18px"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 6,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 11,
      padding: "9px 11px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "7px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Counted facts. Label in the gutter, number on the content baseline — one row per fact, no dead height. */
function Ledger({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    }
  }, rest), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) auto",
      gap: 14,
      alignItems: "baseline",
      padding: "13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap"
    }
  }, item.value))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Outcome.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Amber label over a short results list or paragraph. Sits at the end of a stage copy column. */
function Outcome({
  label = "Results",
  items,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      maxWidth: 430,
      marginTop: 18,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--signal-stage)",
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      fontWeight: 700
    }
  }, label), items && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: "5px 0 0",
      padding: 0,
      listStyle: "none",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, item))), children && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, children));
}
Object.assign(__ds_scope, { Outcome });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Outcome.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Pipeline.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Ordered process rows. Step number in the gutter, the rule on one line — compressed, not spaced out. */
function Pipeline({
  steps = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ol", _extends({
    style: {
      margin: 0,
      padding: 0,
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      listStyle: "none",
      ...style
    }
  }, rest), steps.map((step, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === steps.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "14px 12px 14px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, step.step || String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "13px 18px",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(15px,1.15vw,17px)",
      fontWeight: 560,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, step.label))));
}
Object.assign(__ds_scope, { Pipeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Pipeline.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Reversed-out quote plate. Presence comes from figure/ground — never a coloured left edge. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  return /*#__PURE__*/React.createElement("figure", _extends({
    style: {
      maxWidth: 780,
      margin: "clamp(40px,5vw,68px) 0 0",
      border: "1px solid " + (dark ? "var(--ink)" : "var(--stage)"),
      background: dark ? "var(--ink)" : "var(--stage)",
      color: dark ? "var(--paper-bright)" : "var(--stage-ink)",
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "clamp(26px,3vw,40px) clamp(26px,3vw,40px) clamp(20px,2.4vw,30px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "pretty"
    }
  }, children), attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "11px clamp(26px,3vw,40px)",
      borderTop: "1px solid " + (dark ? "var(--dark-line)" : "var(--stage-line)"),
      background: dark ? "rgba(248,243,232,0.06)" : "rgba(242,236,224,0.07)",
      color: dark ? "var(--paper-dim)" : "var(--stage-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResumeEntry.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** One record in a résumé timeline: dates in the label gutter, org and role, then what I did there. */
function ResumeEntry({
  when,
  org,
  role,
  bullets = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: "clamp(16px,2vw,28px)",
      padding: "var(--space-7) 0",
      borderTop: "1px solid var(--story-line)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      paddingTop: 3,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.45
    }
  }, when), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--display-s-tracking)"
    }
  }, org), role && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, role), bullets.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 11,
      margin: "var(--space-5) 0 0",
      padding: 0,
      listStyle: "none"
    }
  }, bullets.map((b, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "14px minmax(0,1fr)",
      gap: 10,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em",
      textWrap: "pretty"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      marginTop: "0.62em",
      width: 7,
      height: 1,
      background: "var(--story-accent)"
    }
  }), b)))));
}
Object.assign(__ds_scope, { ResumeEntry });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResumeEntry.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Ruled inventory of inputs. Numbered in the gutter, named in the content column. */
function SourceMap({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    }
  }, rest), items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "34px minmax(0,1fr)",
      gap: 12,
      alignItems: "baseline",
      padding: "13px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/TenGrid.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Ten hairline cells; filled cells read as passes. A dimension meter, not a progress bar. */
function TenGrid({
  total = 10,
  pass = 0,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + total + ",1fr)",
      gap: 4,
      marginTop: "var(--space-5)",
      ...style
    }
  }, rest), Array.from({
    length: total
  }).map((_, i) => /*#__PURE__*/React.createElement("i", {
    key: i,
    style: {
      height: 10,
      border: "1px solid " + (i < pass ? "var(--story-accent)" : "var(--story-muted)"),
      background: i < pass ? "var(--story-accent)" : "transparent"
    }
  })));
}
Object.assign(__ds_scope, { TenGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/TenGrid.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Eyebrow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const TONES = {
  muted: "var(--story-muted, var(--muted-ink))",
  signal: "var(--signal)",
  action: "var(--signal-stage)",
  stage: "var(--stage-muted)"
};

/** Small mono label that opens a scene, a section, or a card. */
function Eyebrow({
  tone = "muted",
  size = "eyebrow",
  as = "p",
  children,
  style,
  ...rest
}) {
  const sizes = {
    eyebrow: {
      fontSize: "var(--eyebrow-size)",
      fontWeight: "var(--eyebrow-weight)",
      letterSpacing: "var(--eyebrow-tracking)",
      fontFamily: "var(--font-sans)"
    },
    meta: {
      fontSize: "var(--meta-size)",
      fontWeight: "var(--meta-weight)",
      letterSpacing: "-0.01em",
      fontFamily: "var(--font-mono)"
    },
    micro: {
      fontSize: "var(--micro-size)",
      fontWeight: "var(--micro-weight)",
      letterSpacing: "0",
      fontFamily: "var(--font-mono)"
    }
  };
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, _extends({
    style: {
      margin: 0,
      lineHeight: "var(--eyebrow-leading)",
      color: TONES[tone],
      ...sizes[size],
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/LineArrow.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** The drawn arrow glyph: 1px rule plus a rotated corner. Inherits currentColor. */
function LineArrow({
  reverse = false,
  width = 34,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    "aria-hidden": "true",
    style: {
      position: "relative",
      display: "inline-block",
      flex: "0 0 " + width + "px",
      width,
      height: 12,
      transform: reverse ? "rotate(180deg)" : undefined,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 5,
      right: 0,
      left: 0,
      height: 1,
      background: "currentColor"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 2,
      right: 0,
      width: 7,
      height: 7,
      borderTop: "1px solid currentColor",
      borderRight: "1px solid currentColor",
      transform: "rotate(45deg)"
    }
  }));
}
Object.assign(__ds_scope, { LineArrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/LineArrow.jsx", error: String((e && e.message) || e) }); }

// components/actions/StageAction.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const ACCENTS = {
  action: "var(--signal-stage)",
  signal: "var(--signal)",
  ink: "var(--ink)",
  stage: "var(--stage-ink)"
};

/** The system's only button: a text label on a 1px underline with a drawn arrow. */
function StageAction({
  accent = "action",
  href,
  onClick,
  reverse = false,
  children,
  style,
  ...rest
}) {
  const color = ACCENTS[accent];
  const shared = {
    width: "fit-content",
    display: "inline-flex",
    alignItems: "center",
    gap: 17,
    padding: "0 0 7px",
    border: 0,
    borderBottom: "1px solid " + color,
    background: "transparent",
    color,
    fontFamily: "var(--font-sans)",
    fontSize: 13,
    fontWeight: 700,
    letterSpacing: "-0.02em",
    whiteSpace: "nowrap",
    cursor: "pointer",
    ...style
  };
  const content = reverse ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, {
    reverse: true
  }), children) : /*#__PURE__*/React.createElement(React.Fragment, null, children, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, null));
  if (href) return /*#__PURE__*/React.createElement("a", _extends({
    href: href,
    style: shared
  }, rest), content);
  return /*#__PURE__*/React.createElement("button", _extends({
    type: "button",
    onClick: onClick,
    style: shared
  }, rest), content);
}
Object.assign(__ds_scope, { StageAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/StageAction.jsx", error: String((e && e.message) || e) }); }

// components/foundation/ProvenanceNote.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Hairline-topped note that states what an artifact is and is not. */
function ProvenanceNote({
  label = "Provenance",
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      marginTop: "var(--space-6)",
      paddingTop: "var(--space-4)",
      borderTop: "1px solid var(--story-line)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 700,
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.5,
      letterSpacing: "-0.015em"
    }
  }, children));
}
Object.assign(__ds_scope, { ProvenanceNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/ProvenanceNote.jsx", error: String((e && e.message) || e) }); }

// components/foundation/RegistrationMark.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Corner crosshair with a gold centre — the print-registration device used on every stage. */
function RegistrationMark({
  size = 22,
  color = "var(--stage-ink)",
  dot = "var(--signal-stage)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", _extends({
    "aria-hidden": "true",
    style: {
      position: "absolute",
      top: 22,
      right: 22,
      width: size,
      height: size,
      opacity: 0.65,
      pointerEvents: "none",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: size / 2 - 1,
      right: 0,
      left: 0,
      height: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: size / 2 - 1,
      width: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 4,
      border: "1px solid " + color,
      borderRadius: "var(--radius-round)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 8,
      borderRadius: "var(--radius-round)",
      background: dot
    }
  }));
}
Object.assign(__ds_scope, { RegistrationMark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/RegistrationMark.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Tag.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Hairline capability tag. Square corners, no fill. */
function Tag({
  on = "stage",
  children,
  style,
  ...rest
}) {
  const border = on === "stage" ? "var(--stage-line)" : "var(--soft-line)";
  return /*#__PURE__*/React.createElement("span", _extends({
    style: {
      padding: "9px 11px",
      border: "1px solid " + border,
      fontFamily: "var(--font-sans)",
      fontSize: 10,
      letterSpacing: "-0.01em",
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Tag.jsx", error: String((e && e.message) || e) }); }

// components/layout/PaperFrame.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** The bounded document: a paper card on a darker desk, hairline border, hard offset shadow, grain overlay. */
function PaperFrame({
  children,
  grain = true,
  columns = "var(--rail-width) minmax(0,1fr)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100%",
      background: "var(--surface-desk)",
      fontFamily: "var(--font-body)",
      color: "var(--ink)"
    }
  }, grain && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      zIndex: 5000,
      inset: 0,
      pointerEvents: "none",
      opacity: "var(--texture-grain-opacity)",
      mixBlendMode: "multiply",
      backgroundImage: "var(--texture-grain)"
    }
  }), /*#__PURE__*/React.createElement("div", _extends({
    style: {
      width: "min(calc(100% - 24px)," + "var(--frame-max))",
      height: "calc(100svh - 24px)",
      minHeight: 700,
      display: "grid",
      gridTemplateColumns: columns,
      margin: "var(--frame-inset) auto",
      overflow: "hidden",
      border: "1px solid var(--ink)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-4)",
      ...style
    }
  }, rest), children));
}
Object.assign(__ds_scope, { PaperFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/PaperFrame.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stage.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** The pine authored stage: gradient field, clipped, with an optional registration mark. */
function Stage({
  children,
  variant = "field",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", _extends({
    style: {
      position: "relative",
      zIndex: 1,
      minWidth: 0,
      minHeight: 0,
      overflow: "hidden",
      isolation: "isolate",
      background: variant === "section" ? "var(--stage-field-section)" : "var(--stage-field)",
      color: "var(--stage-ink)",
      ...style
    }
  }, rest), children);
}
Object.assign(__ds_scope, { Stage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stage.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(49%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", _extends({
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) 18px clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    }
  }, rest), eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title && /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", _extends({
    "aria-hidden": "true",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      transform: direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)",
      willChange: "transform",
      animation: plate,
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }

// components/layout/StorySection.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** A tone-scoped chapter of a case document: index, kicker, title, intro, then content. */
function StorySection({
  tone = "paper",
  index,
  chapterLabel,
  kicker,
  title,
  intro,
  children,
  id,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", _extends({
    id: id,
    "data-tone": tone,
    style: {
      display: "grid",
      gridTemplateColumns: "70px minmax(0,1fr)",
      gap: "clamp(28px,5vw,82px)",
      padding: "var(--section-pad) var(--gutter)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700
    }
  }, chapterLabel && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, chapterLabel), index && /*#__PURE__*/React.createElement("b", {
    style: {
      display: "block",
      color: "var(--story-accent)"
    }
  }, index)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 980
    }
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 16px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-m-size)",
      fontWeight: "var(--display-m-weight)",
      lineHeight: "var(--display-m-leading)",
      letterSpacing: "var(--display-m-tracking)",
      textWrap: "balance"
    }
  }, title), intro && /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 760,
      marginTop: 20,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      minWidth: 0,
      gridColumn: "1 / -1",
      color: "var(--story-ink)"
    }
  }, children));
}
Object.assign(__ds_scope, { StorySection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StorySection.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ChapterNav.jsx
try { (() => {
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }
/** Sticky chapter map. The chapter in view inverts to an ink chip. */
function ChapterNav({
  label = "Case map",
  chapters = [],
  active,
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", _extends({
    style: {
      position: "sticky",
      zIndex: 45,
      top: 0,
      minHeight: "var(--chapter-nav-height)",
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(22px,3vw,48px)",
      alignItems: "center",
      padding: "var(--space-2) var(--gutter-tight)",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      backdropFilter: "blur(14px)",
      ...style
    }
  }, rest), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 760,
      whiteSpace: "nowrap"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(chapters.length, 1) + ",minmax(74px,1fr))",
      gap: 4
    }
  }, chapters.map(c => {
    const on = c.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      type: "button",
      className: "ds-tab ds-tab-chapter",
      onClick: () => onSelect && onSelect(c.id),
      "aria-current": on ? "true" : undefined,
      style: {
        position: "relative",
        minHeight: 34,
        padding: "0 8px",
        border: 0,
        background: on ? "var(--ink)" : "transparent",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        cursor: "pointer",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, c.label);
  })));
}
Object.assign(__ds_scope, { ChapterNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ChapterNav.jsx", error: String((e && e.message) || e) }); }




// components/evidence/RuleSplit.jsx
try { (() => {
/** Two-column sort of a guidance set: what a model has to judge, and what code can check.
 *  The whole argument for a measurement layer fits in one of these — a reader can see the
 *  proportion before reading a single row. */
function RuleSplit({
  modelLabel = "Model judgment",
  codeLabel = "Checkable in code",
  rules = [],
  note,
  style,
  ...rest
}) {
  const model = rules.filter(r => r.kind === "model");
  const code = rules.filter(r => r.kind === "code");
  const column = (label, items, accent) => /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      borderRight: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "10px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.rule))));
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      border: "1px solid var(--story-line)",
      borderRight: 0,
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, column(modelLabel, model, "var(--story-muted)"), column(codeLabel, code, "var(--story-accent)")), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, note));
}
Object.assign(__ds_scope, { RuleSplit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/RuleSplit.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResearchGrid.jsx
try { (() => {
/** Published sources behind a measurement, one row each: who, when, and the thing it makes computable.
 *  A citation row is the difference between an instrument and an opinion, so the source is never
 *  paraphrased away — it stays attributable. */
function ResearchGrid({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    style: {
      padding: "15px 18px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, item.source), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, item.year)), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "9px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.12,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, item.gives), item.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.note))));
}
Object.assign(__ds_scope, { ResearchGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResearchGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FingerprintBars.jsx
try { (() => {
/** A measured voice fingerprint: one row per dimension, the reading drawn against the band a
 *  surface is allowed to sit in. The band is the point — a fingerprint is a tolerance, not a
 *  score, and a reading outside it is the only thing that counts as a miss. */
function FingerprintBars({
  dimensions = [],
  scale = 100,
  showBand = true,
  style,
  ...rest
}) {
  const pct = n => Math.max(0, Math.min(100, n / scale * 100));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, dimensions.map((d, i) => {
    const out = showBand && d.band && (d.value < d.band[0] || d.value > d.band[1]);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        display: "grid",
        gridTemplateColumns: "minmax(96px,0.9fr) minmax(0,2.4fr) 42px",
        gap: 14,
        alignItems: "center",
        padding: "9px 16px",
        borderBottom: i === dimensions.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700,
        lineHeight: 1.3
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        position: "relative",
        height: 9,
        display: "block",
        borderBottom: "1px solid var(--story-line)"
      }
    }, showBand && d.band && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 0,
        bottom: 0,
        left: pct(d.band[0]) + "%",
        width: pct(d.band[1]) - pct(d.band[0]) + "%",
        background: "var(--story-band)",
        borderLeft: "1px solid var(--story-line)",
        borderRight: "1px solid var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 2,
        bottom: 2,
        left: 0,
        width: pct(d.value) + "%",
        background: out ? "var(--story-accent)" : "var(--story-ink)"
      }
    })), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 14,
        fontWeight: 700,
        letterSpacing: "-0.04em",
        textAlign: "right",
        fontVariantNumeric: "tabular-nums"
      }
    }, d.value));
  }));
}
Object.assign(__ds_scope, { FingerprintBars });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FingerprintBars.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === steps.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "13px 12px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "12px 18px"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail))))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      overflowX: "auto",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), rows.map((r, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      padding: "13px 16px",
      borderBottom: i === rows.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DisciplineVenn.jsx
try { (() => {
/** Three disciplines and the work that only exists where they overlap. Drawn as a registration
 *  diagram — hairline circles, no fills, the intersection marked the way the system marks a
 *  registration point — so it reads as a technical figure rather than a slide graphic. */
function DisciplineVenn({
  fields = [],
  intersection,
  caption,
  tone = "story",
  style,
  ...rest
}) {
  const ink = tone === "stage" ? "var(--stage-ink)" : "var(--story-ink)";
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const muted = tone === "stage" ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = tone === "stage" ? "var(--signal-stage)" : "var(--story-accent)";

  /* Three circles on an equilateral arrangement, centred on 150,142. */
  const circles = [{
    cx: 150,
    cy: 96,
    label: fields[0]
  }, {
    cx: 111,
    cy: 164,
    label: fields[1]
  }, {
    cx: 189,
    cy: 164,
    label: fields[2]
  }];
  const anchors = [{
    x: 150,
    y: 26,
    align: "middle"
  }, {
    x: 44,
    y: 224,
    align: "start"
  }, {
    x: 256,
    y: 224,
    align: "end"
  }];
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      padding: "var(--space-7) 0",
      border: "1px solid " + line,
      background: tone === "stage" ? "transparent" : "var(--story-raised)",
      boxShadow: tone === "stage" ? "none" : "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "0 0 300 244",
    width: "100%",
    style: {
      maxWidth: 400,
      height: "auto",
      overflow: "visible"
    },
    role: "img",
    "aria-label": fields.join(", ") + " — " + intersection
  }, circles.map(c => /*#__PURE__*/React.createElement("circle", {
    key: c.label,
    cx: c.cx,
    cy: c.cy,
    r: "62",
    fill: "none",
    stroke: line,
    strokeWidth: "1"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "15",
    fill: "none",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "150",
    y1: "120",
    x2: "150",
    y2: "164",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "128",
    y1: "142",
    x2: "172",
    y2: "142",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "3.5",
    fill: accent
  }), circles.map((c, i) => /*#__PURE__*/React.createElement("text", {
    key: c.label,
    x: anchors[i].x,
    y: anchors[i].y,
    textAnchor: anchors[i].align,
    fill: muted,
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c.label)))), (intersection || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "13px 0 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.4
    }
  }, "Overlap"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, intersection && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, intersection), caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: intersection ? "8px 0 0" : 0,
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, caption))));
}
Object.assign(__ds_scope, { DisciplineVenn });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DisciplineVenn.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceScoreRing.jsx
try { (() => {
/** How a reading reaches the person who has to act on it: one alignment figure, the dimensions
 *  underneath it, and a verdict in words. The arc draws itself on mount rather than fading in —
 *  a measurement should look like it was taken, not like it was already there. */
function VoiceScoreRing({
  score = 0,
  outOf = 100,
  label = "Voice alignment",
  verdict,
  scope = [],
  dimensions = [],
  size = 148,
  style,
  ...rest
}) {
  const [drawn, setDrawn] = React.useState(false);
  React.useEffect(() => {
    const id = requestAnimationFrame(() => requestAnimationFrame(() => setDrawn(true)));
    return () => cancelAnimationFrame(id);
  }, []);
  const r = (size - 18) / 2;
  const c = 2 * Math.PI * r;
  const filled = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope.length > 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, scope.join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(18px,2.4vw,32px)",
      alignItems: "center",
      padding: "18px 16px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: "7",
    strokeLinecap: "butt",
    strokeDasharray: c,
    strokeDashoffset: drawn ? c * (1 - filled) : c,
    style: {
      transition: "stroke-dashoffset var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "of ", outOf))), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, verdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 13px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)"
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(80px,1fr) minmax(0,1.6fr) 34px",
      gap: 12,
      alignItems: "center",
      padding: "7px 0",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 5,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: (drawn ? d.value : 0) + "%",
      background: "var(--story-ink)",
      transition: "width var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value)))))));
}
Object.assign(__ds_scope, { VoiceScoreRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceScoreRing.jsx", error: String((e && e.message) || e) }); }

// components/evidence/BeforeAfter.jsx
try { (() => {
/** A rewrite delivered as a diff with its measurements attached, so the person deciding whether to
 *  take it can see what moved and by how much. The deltas are the argument — without them this is
 *  just someone else's preferred wording. */
function BeforeAfter({
  before,
  after,
  beforeLabel = "Before",
  afterLabel = "After",
  deltas = [],
  actions,
  style,
  ...rest
}) {
  const pane = (label, text, tone) => /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "14px 16px",
      borderRight: tone === "before" ? "1px solid var(--story-line)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: tone === "after" ? "var(--story-accent)" : "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: tone === "before" ? "var(--story-muted)" : "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: tone === "after" ? 560 : 480,
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, text));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      borderBottom: "1px solid var(--story-line)"
    }
  }, pane(beforeLabel, before, "before"), pane(afterLabel, after, "after")), deltas.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(deltas.length, 1) + ",minmax(0,1fr))",
      borderBottom: actions ? "1px solid var(--story-line)" : 0
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        padding: "11px 14px",
        borderRight: i === deltas.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "block",
        marginBottom: 6,
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "baseline",
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 13,
        fontWeight: 560,
        letterSpacing: "-0.03em",
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-accent)",
        fontSize: 17,
        fontWeight: 700,
        letterSpacing: "-0.04em"
      }
    }, d.to)));
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-8)",
      alignItems: "center",
      padding: "12px 16px"
    }
  }, actions));
}
Object.assign(__ds_scope, { BeforeAfter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/BeforeAfter.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceProfile.jsx
try { (() => {
/** The target a surface is written to, published so a team can argue with it: each dimension's
 *  setting and the one sentence saying why it sits there. A profile without its reasoning is a
 *  number nobody will honour. */
function VoiceProfile({
  product,
  surface,
  dimensions = [],
  notes = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Voice profile"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, [product, surface].filter(Boolean).join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit,minmax(128px,1fr))",
      borderBottom: notes.length ? "1px solid var(--story-line)" : 0
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      padding: "13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 8,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      height: 3,
      display: "block",
      marginTop: 9,
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: d.value + "%",
      background: "var(--story-accent)"
    }
  }))))), notes.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === notes.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, n.name), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, n.why)))));
}
Object.assign(__ds_scope, { VoiceProfile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceProfile.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DisciplineVenn.jsx
try { (() => {
/** Three disciplines and the work that only exists where they overlap. Drawn as a registration
 *  diagram — hairline circles, no fills, the intersection marked the way the system marks a
 *  registration point — so it reads as a technical figure rather than a slide graphic. */
function DisciplineVenn({
  fields = [],
  intersection,
  caption,
  tone = "story",
  style,
  ...rest
}) {
  const ink = tone === "stage" ? "var(--stage-ink)" : "var(--story-ink)";
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const muted = tone === "stage" ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = tone === "stage" ? "var(--signal-stage)" : "var(--story-accent)";

  /* Three circles on an equilateral arrangement, centred on 150,142. */
  const circles = [{
    cx: 150,
    cy: 96,
    label: fields[0]
  }, {
    cx: 111,
    cy: 164,
    label: fields[1]
  }, {
    cx: 189,
    cy: 164,
    label: fields[2]
  }];
  /* Labels sit outside the circle field so a long discipline name cannot crowd its neighbour. */
  const anchors = [{
    x: 150,
    y: 20,
    align: "middle"
  }, {
    x: -34,
    y: 240,
    align: "start"
  }, {
    x: 334,
    y: 240,
    align: "end"
  }];
  const leaders = [{
    x1: 150,
    y1: 27,
    x2: 150,
    y2: 36
  }, {
    x1: 62,
    y1: 233,
    x2: 84,
    y2: 213
  }, {
    x1: 238,
    y1: 233,
    x2: 216,
    y2: 213
  }];
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      padding: "var(--space-7) 0",
      border: "1px solid " + line,
      background: tone === "stage" ? "transparent" : "var(--story-raised)",
      boxShadow: tone === "stage" ? "none" : "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "-40 8 380 244",
    width: "100%",
    style: {
      maxWidth: 440,
      height: "auto",
      overflow: "visible"
    },
    role: "img",
    "aria-label": fields.join(", ") + " — " + intersection
  }, circles.map(c => /*#__PURE__*/React.createElement("circle", {
    key: c.label,
    cx: c.cx,
    cy: c.cy,
    r: "62",
    fill: "none",
    stroke: line,
    strokeWidth: "1"
  })), leaders.map((l, i) => /*#__PURE__*/React.createElement("line", {
    key: i,
    x1: l.x1,
    y1: l.y1,
    x2: l.x2,
    y2: l.y2,
    stroke: line,
    strokeWidth: "1"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "15",
    fill: "none",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "150",
    y1: "120",
    x2: "150",
    y2: "164",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "128",
    y1: "142",
    x2: "172",
    y2: "142",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "3.5",
    fill: accent
  }), circles.map((c, i) => /*#__PURE__*/React.createElement("text", {
    key: c.label,
    x: anchors[i].x,
    y: anchors[i].y,
    textAnchor: anchors[i].align,
    fill: muted,
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c.label)))), (intersection || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "13px 0 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.4
    }
  }, "Overlap"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, intersection && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, intersection), caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: intersection ? "8px 0 0" : 0,
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, caption))));
}
Object.assign(__ds_scope, { DisciplineVenn });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DisciplineVenn.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      maxWidth: 700,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  if (!gutter) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      /* Sized by the words, not by a column. A quote that wraps short leaves no dead ground. */
      width: "fit-content",
      maxWidth: 700,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      /* Sized by the words, not by a column. A quote that wraps short leaves no dead ground. */
      width: "fit-content",
      maxWidth: 560,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CardGrid.jsx
try { (() => {
/** Hairline grid block, on the instrument grammar: an optional silkscreen header naming what the set is
 *  and how many are in it, then cells sized by their contents. Without a label it stays a bare grid —
 *  but if you cannot name the set, ask whether it needed a frame at all. */
function CardGrid({
  columns = 2,
  raised = true,
  label,
  scope,
  children,
  style,
  ...rest
}) {
  const count = React.Children.count(children);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, scope || String(count).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, children));
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content
 *  column. No height floor — a short cell is short. The padding is an instrument's, not a page margin's. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "var(--card-pad) 12px var(--card-pad) 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--card-pad) var(--card-pad-wide)"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 5,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 9,
      padding: "8px 10px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "6px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
/** Ruled inventory of inputs: numbered in the gutter, named in the content column, under a silkscreen
 *  header declaring what the inventory is and how many entries it holds. */
function SourceMap({
  items = [],
  columns = 2,
  label = "Inputs",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "30px minmax(0,1fr)",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal",
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item)))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts. Label in the gutter, number on the content baseline — one row per fact, no dead
 *  height, under a silkscreen header saying what is being counted. */
function Ledger({
  items = [],
  columns = 2,
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) auto",
      gap: 14,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 var(--gutter-tight)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "var(--gutter-tight)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ArtifactFigure.jsx
try { (() => {
/** Framed artifact with a gutter-aligned provenance caption. No height floor by default — the frame is
 *  the artifact's own edge, so a short artifact gets a short frame. Pass `minHeight` only when a set of
 *  figures genuinely has to align to each other. */
function ArtifactFigure({
  src,
  alt = "",
  label,
  caption,
  contain = false,
  zoom = false,
  minHeight,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    className: zoom ? "ds-figure-zoom" : undefined,
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight,
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--surface-mat)",
      boxShadow: "var(--story-shadow)",
      padding: contain ? "clamp(14px,1.8vw,24px)" : 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: "100%",
      height: minHeight ? "100%" : "auto",
      minHeight: minHeight ? minHeight - 40 : undefined,
      display: "block",
      objectFit: contain ? "contain" : "cover"
    }
  })), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { ArtifactFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ArtifactFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content
 *  column. No height floor — a short cell is short. The padding is an instrument's, not a page margin's. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter-tight) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "var(--card-pad) 12px var(--card-pad) 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--card-pad) var(--card-pad-wide)"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 5,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 9,
      padding: "8px 10px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "6px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Cells are as wide as their own label-and-number pair and wrap when they run out of room — they do
 *  not stretch to fill a column and fling the label and the number to opposite corners. A gap you could
 *  park a sentence in is the same failure as a height floor, turned on its side. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      gap: 16,
      alignItems: "baseline",
      flex: "0 1 auto",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
/** Ruled inventory of inputs, sized by what it names.
 *
 *  Each entry is as wide as its own number-and-name pair and wraps when it runs out of room. An entry
 *  does not stretch to half the page and then sit alone at the left edge of it. */
function SourceMap({
  items = [],
  label = "Inputs",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 24,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "flex",
      gap: 12,
      alignItems: "baseline",
      flex: "0 1 auto",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal",
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item)))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Label above value, so a cell is only as wide as the wider of the two and a row of four fits without
 *  stretching. Side-by-side reads fine until a value is a word rather than a number, at which point the
 *  cells go ragged and the label and the number end up at opposite ends of a void. This is the same
 *  stacking `VoiceProfile` uses, which is the system's answer for a set of small readings. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: "0 1 auto",
      padding: "12px 18px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 132px",
      minWidth: 132,
      padding: "13px 14px",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 150px",
      minWidth: 150,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CaptureFigure.jsx
try { (() => {
/** A capture of shipped behaviour, in a frame that says what you are looking at.
 *
 *  Video is the only honest way to show an interaction that happens in time, so this plays inline —
 *  muted, looping, playsinline, with controls available. Autoplay is off by default: the accessibility
 *  floor forbids a loop that plays without the reader asking for it. */
function CaptureFigure({
  src,
  poster,
  label,
  caption,
  quote,
  autoPlay = false,
  controls = true,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--ink)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("video", {
    src: src,
    poster: poster,
    muted: true,
    loop: true,
    playsInline: true,
    autoPlay: autoPlay,
    controls: controls,
    preload: "metadata",
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), quote && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "The string"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, quote)), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { CaptureFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CaptureFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 14px",
      borderRight: "1px solid " + line,
      overflowWrap: "break-word"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  if (!gutter) {
    return /*#__PURE__*/React.createElement("div", {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum.
 *  `as` sets the element, so a row inside an ordered list can be a real `li` rather than a div. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  as = "div",
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const Tag = as;
  if (!gutter) {
    return /*#__PURE__*/React.createElement(Tag, {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      listStyle: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel, PanelRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceScoreRing.jsx
try { (() => {
/** How a reading reaches the person who has to act on it: one alignment figure, the dimensions
 *  underneath it, and a verdict in words. The arc draws itself on mount rather than fading in —
 *  a measurement should look like it was taken, not like it was already there. */
function VoiceScoreRing({
  score = 0,
  outOf = 100,
  label = "Voice alignment",
  verdict,
  scope = [],
  dimensions = [],
  size = 148,
  style,
  ...rest
}) {
  const [drawn, setDrawn] = React.useState(false);
  React.useEffect(() => {
    /* Two frames, so the browser paints the zero state before the arc animates to its value.
       Both ids are tracked — cancelling only the outer one leaves the inner frame live. */
    let inner;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setDrawn(true));
    });
    return () => {
      cancelAnimationFrame(outer);
      if (inner) cancelAnimationFrame(inner);
    };
  }, []);
  const r = (size - 18) / 2;
  const c = 2 * Math.PI * r;
  const filled = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope.length > 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, scope.join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(18px,2.4vw,32px)",
      alignItems: "center",
      padding: "18px 16px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: "7",
    strokeLinecap: "butt",
    strokeDasharray: c,
    strokeDashoffset: drawn ? c * (1 - filled) : c,
    style: {
      transition: "stroke-dashoffset var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "of ", outOf))), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, verdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 13px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)"
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(80px,1fr) minmax(0,1.6fr) 34px",
      gap: 12,
      alignItems: "center",
      padding: "7px 0",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 5,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: (drawn ? d.value : 0) + "%",
      background: "var(--story-ink)",
      transition: "width var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value)))))));
}
Object.assign(__ds_scope, { VoiceScoreRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceScoreRing.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(49%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) 18px clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title && /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum.
 *  `as` sets the element, so a row inside an ordered list can be a real `li` rather than a div. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  as = "div",
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const Tag = as;
  if (!gutter) {
    return /*#__PURE__*/React.createElement(Tag, {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      listStyle: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel, PanelRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title && /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 14px",
      borderRight: "1px solid " + line,
      overflowWrap: "break-word"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35,
      overflowWrap: "break-word"
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Outcome.jsx
try { (() => {
/** Amber label over a short results list or paragraph. Sits at the end of a stage copy column. */
function Outcome({
  label = "Results",
  items,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(430px,88%)",
      marginTop: 18,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--signal-stage)",
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      fontWeight: 700
    }
  }, label), items && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: "5px 0 0",
      padding: 0,
      listStyle: "none",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, item))), children && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, children));
}
Object.assign(__ds_scope, { Outcome });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Outcome.jsx", error: String((e && e.message) || e) }); }


// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title &&
  /*#__PURE__*/
  /* data-doc-heading + tabIndex={-1}: the one place per document the app moves focus to
     after a destination change (see app.jsx). Focusable without joining the tab order;
     the ring itself is drawn by :focus-visible in tokens/interaction.css, so a mouse
     click never shows one and a keyboard move always does. */
  React.createElement("h1", {
    "data-doc-heading": true,
    tabIndex: -1,
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Abstract"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/document/ReportSection.jsx
try { (() => {
/** A numbered, citable section. The number hangs in the gutter — the same anatomy the evidence
 *  components use for their labels, so document mode reads as the same system rather than a
 *  different one. Numbering is done by CSS counters, so inserting a section never renumbers by hand
 *  and a section can never claim a number it does not have. */
function ReportSection({
  level = 1,
  title,
  children,
  id,
  style,
  ...rest
}) {
  const sub = level > 1;
  const Heading = sub ? "h3" : "h2";
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: sub ? "ds-doc-subsection" : "ds-doc-section",
    style: {
      marginTop: sub ? "var(--space-9)" : "var(--space-11)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-num",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: sub ? 10 : 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement(Heading, {
    style: {
      margin: 0,
      maxWidth: "var(--doc-measure)",
      fontFamily: "var(--font-sans)",
      fontSize: sub ? "clamp(16px,1.35vw,19px)" : "clamp(20px,1.9vw,26px)",
      fontWeight: sub ? 650 : 700,
      lineHeight: 1.2,
      letterSpacing: "-0.035em",
      textWrap: "balance"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: "var(--space-5)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, children));
}
Object.assign(__ds_scope, { ReportSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/ReportSection.jsx", error: String((e && e.message) || e) }); }

// components/document/DocFigure.jsx
try { (() => {
/** A numbered figure or table. Two things a report needs that an exhibition does not: a number a
 *  reader can cite, and permission to break out past the reading measure — a wide table is
 *  unreadable squeezed into a prose column.
 *
 *  Any of the system's instruments can sit inside one. In exhibition mode an instrument IS the
 *  moment; here it is evidence with a number attached, which is the whole difference in register. */
function DocFigure({
  kind = "figure",
  caption,
  source,
  wide = false,
  children,
  id,
  style,
  ...rest
}) {
  const isTable = kind === "table";
  return /*#__PURE__*/React.createElement("figure", {
    id: id,
    className: isTable ? "ds-doc-table" : "ds-doc-figure",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: wide ? "none" : "var(--doc-measure)",
      marginLeft: wide ? "0" : "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, children), /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginTop: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: isTable ? "ds-doc-tablabel" : "ds-doc-figlabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, caption), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "break-word"
    }
  }, source))));
}
Object.assign(__ds_scope, { DocFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/DocFigure.jsx", error: String((e && e.message) || e) }); }

// components/document/Endnotes.jsx
try { (() => {
/** Endnotes and references — the two things that let a reader check you.
 *
 *  `NoteRef` marks a claim in the prose; `Endnotes` lists them at the end. The numbering is a CSS
 *  counter shared with the marks, so a note can never be numbered wrong. References are separate
 *  because they are a different kind of object: a note qualifies a claim, a reference names a source.
 *  A report that measures things needs both, and their absence is what makes an argument look like
 *  an assertion. */
function NoteRef({
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-noteref",
    "aria-hidden": "true",
    style: style,
    ...rest
  });
}
function Endnotes({
  notes = [],
  label = "Notes",
  style,
  ...rest
}) {
  if (!notes.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    style: {
      marginTop: "var(--space-11)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      padding: "9px 0",
      borderBottom: i === notes.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, i + 1), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5
    }
  }, n)))));
}
function References({
  items = [],
  label = "References",
  style,
  ...rest
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    className: "ds-doc-refs",
    style: {
      marginTop: "var(--space-10)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ul", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "9px 0",
      borderBottom: i === items.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5,
      textIndent: "-1.2em",
      paddingLeft: "1.2em"
    }
  }, r.href ? /*#__PURE__*/React.createElement("a", {
    href: r.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "inherit",
      textDecoration: "none",
      borderBottom: "1px solid var(--story-accent)"
    }
  }, r.text) : r.text))));
}
Object.assign(__ds_scope, { NoteRef, Endnotes, References });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Endnotes.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Abstract"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/document/ReportSection.jsx
try { (() => {
/** A numbered, citable section. The number hangs in the gutter — the same anatomy the evidence
 *  components use for their labels, so document mode reads as the same system rather than a
 *  different one. Numbering is done by CSS counters, so inserting a section never renumbers by hand
 *  and a section can never claim a number it does not have. */
function ReportSection({
  level = 1,
  title,
  children,
  id,
  style,
  ...rest
}) {
  const sub = level > 1;
  const Heading = sub ? "h3" : "h2";
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: sub ? "ds-doc-subsection" : "ds-doc-section",
    style: {
      marginTop: sub ? "var(--space-9)" : "var(--space-11)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-num",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: sub ? 10 : 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement(Heading, {
    style: {
      margin: 0,
      maxWidth: "var(--doc-measure)",
      fontFamily: "var(--font-sans)",
      fontSize: sub ? "clamp(16px,1.35vw,19px)" : "clamp(20px,1.9vw,26px)",
      fontWeight: sub ? 650 : 700,
      lineHeight: 1.2,
      letterSpacing: "-0.035em",
      textWrap: "balance"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: "var(--space-5)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, children));
}
Object.assign(__ds_scope, { ReportSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/ReportSection.jsx", error: String((e && e.message) || e) }); }

// components/document/DocFigure.jsx
try { (() => {
/** A numbered figure or table. Two things a report needs that an exhibition does not: a number a
 *  reader can cite, and permission to break out past the reading measure — a wide table is
 *  unreadable squeezed into a prose column.
 *
 *  Any of the system's instruments can sit inside one. In exhibition mode an instrument IS the
 *  moment; here it is evidence with a number attached, which is the whole difference in register. */
function DocFigure({
  kind = "figure",
  caption,
  source,
  wide = false,
  children,
  id,
  style,
  ...rest
}) {
  const isTable = kind === "table";
  return /*#__PURE__*/React.createElement("figure", {
    id: id,
    className: isTable ? "ds-doc-table" : "ds-doc-figure",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: wide ? "none" : "var(--doc-measure)",
      marginLeft: wide ? "0" : "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, children), /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginTop: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: isTable ? "ds-doc-tablabel" : "ds-doc-figlabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, caption), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "break-word"
    }
  }, source))));
}
Object.assign(__ds_scope, { DocFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/DocFigure.jsx", error: String((e && e.message) || e) }); }

// components/document/Endnotes.jsx
try { (() => {
/** Endnotes and references — the two things that let a reader check you.
 *
 *  `NoteRef` marks a claim in the prose; `Endnotes` lists them at the end. The numbering is a CSS
 *  counter shared with the marks, so a note can never be numbered wrong. References are separate
 *  because they are a different kind of object: a note qualifies a claim, a reference names a source.
 *  A report that measures things needs both, and their absence is what makes an argument look like
 *  an assertion. */
function NoteRef({
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-noteref",
    "aria-hidden": "true",
    style: style,
    ...rest
  });
}
function Endnotes({
  notes = [],
  label = "Notes",
  style,
  ...rest
}) {
  if (!notes.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    style: {
      marginTop: "var(--space-11)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      padding: "9px 0",
      borderBottom: i === notes.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, i + 1), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5
    }
  }, n)))));
}
function References({
  items = [],
  label = "References",
  style,
  ...rest
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    className: "ds-doc-refs",
    style: {
      marginTop: "var(--space-10)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ul", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "9px 0",
      borderBottom: i === items.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5,
      textIndent: "-1.2em",
      paddingLeft: "1.2em"
    }
  }, r.href ? /*#__PURE__*/React.createElement("a", {
    href: r.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "inherit",
      textDecoration: "none",
      borderBottom: "1px solid var(--story-accent)"
    }
  }, r.text) : r.text))));
}
Object.assign(__ds_scope, { NoteRef, Endnotes, References });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Endnotes.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Toolchain.jsx
try { (() => {
/** What a system was actually built out of, named and linked so a reader can go and check.
 *
 *  The engineering counterpart to `ResearchGrid`: same anatomy, same register. Deliberately no logos —
 *  a row of third-party brand marks would be the one place this system's ink-and-hairline language
 *  breaks, and it is the oldest tell in portfolio design. A name, a licence, and what it actually
 *  provides is more credible than a badge, because it can be verified. */
function Toolchain({
  items = [],
  label = "Built with",
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: item.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(140px,0.9fr) minmax(0,2fr) auto",
      gap: "clamp(12px,2vw,26px)",
      alignItems: "baseline",
      padding: "12px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, item.href ? /*#__PURE__*/React.createElement("a", {
    href: item.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em",
      textDecoration: "none",
      borderBottom: "1px solid currentColor",
      width: "fit-content",
      paddingBottom: 1
    }
  }, item.name) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em"
    }
  }, item.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.provides), item.meta && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      textAlign: "right"
    }
  }, item.meta)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 640,
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45
    }
  }, note));
}
Object.assign(__ds_scope, { Toolchain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Toolchain.jsx", error: String((e && e.message) || e) }); }

// components/document/Finding.jsx
try { (() => {
/** A finding stated twice: once in English, once in numbers.
 *
 *  A report that only speaks in measurements excludes everyone who has not read the method, and a
 *  report that only speaks in plain language cannot be checked. So both, adjacent, with the plain
 *  sentence leading — a reader travelling fast reads only those and still gets the argument; a
 *  reader who wants proof drops one line.
 *
 *  This is the receiver-side / producer-side distinction as a typographic device. "Help headings
 *  repeat themselves" is what a reader experiences. "Lexical diversity 1.45 against a 4.9–7.2 band"
 *  is what the writer did. Neither one alone is the finding. */
function Finding({
  children,
  measured,
  verdict,
  style,
  ...rest
}) {
  const tone = verdict === "noise" ? "var(--story-muted)" : verdict === "underpowered" ? "var(--story-muted)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      margin: "0 0 var(--space-6)",
      paddingLeft: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(17px,1.5vw,21px)",
      fontWeight: 620,
      lineHeight: 1.34,
      letterSpacing: "-0.032em",
      textWrap: "pretty"
    }
  }, children), (measured || verdict) && /*#__PURE__*/React.createElement("p", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "0 14px",
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      lineHeight: 1.5
    }
  }, measured && /*#__PURE__*/React.createElement("span", null, measured), verdict && /*#__PURE__*/React.createElement("span", {
    style: {
      color: tone
    }
  }, verdict)));
}
Object.assign(__ds_scope, { Finding });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Finding.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  plain,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), plain && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 10,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "In plain terms"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(18px,1.6vw,23px)",
      fontWeight: 620,
      lineHeight: 1.36,
      letterSpacing: "-0.033em",
      textWrap: "pretty"
    }
  }, plain)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Method and scope"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
/** A hyphen or dash already in the text is a standard line-break point (Unicode UAX #14) in every
 *  browser, with no special CSS involved — "help-centre" wraps to "help-" / "centre" the instant its
 *  column narrows. That reads fine in flowing prose but garbles a short label or an identifier
 *  ("age-of-acquisition", "p (BH-adj)", a dotted record id). A zero-width word joiner (U+2060) glued
 *  to the dash suppresses just that break opportunity — normal space-based wrapping is untouched, so
 *  long sentences still wrap for readability; only the dash itself stops being a place to split.
 *  - hyphen-minus, ‐‑ hyphen/non-breaking hyphen, ‒ figure dash, – en dash,
 *  — em dash, ― horizontal bar; ⁠ is the word joiner itself. */
const NO_BREAK_AFTER_DASH = /([-‐-―])(?=\S)/g;
const glueDashes = v => typeof v === "string" ? v.replace(NO_BREAK_AFTER_DASH, "$1⁠") : v;

/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, glueDashes(c))))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = glueDashes(highlight ? value.value : value);
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
/** A hyphen or dash already in the text is a standard line-break point (Unicode UAX #14) in every
 *  browser, with no special CSS involved, and MOST of the time that is correct: "help-centre" wrapping
 *  to "help-" / "centre" at a narrow column is ordinary, everyday typography — books do it every day —
 *  and gluing it shut would make the word un-searchable and un-copyable (the rendered string would
 *  carry an invisible character a reader can't see or type back). Two narrow cases are genuinely wrong
 *  rather than merely unlucky, because there the two sides of the dash are not independently meaningful
 *  words: a digit range ("2025-2026" splitting to "2025-" / "2026" reads as a typo, not a range) and a
 *  dash-joined token inside a parenthetical statistical label ("p (BH-adj)" splitting to "p (BH-" /
 *  "adj)" garbles the abbreviation). Only those two get a zero-width word joiner (U+2060) glued to the
 *  dash; every other hyphenated compound is left exactly as written. */
const DASH = "\\u002D\\u2010-\\u2015"; // hyphen-minus; hyphen through horizontal bar (covers en/em dash)
const WJ = "⁠"; // word joiner — zero-width, forbids a line break at this point, otherwise invisible
const DIGIT_RANGE_DASH = new RegExp("(\\d)([" + DASH + "])(?=\\d)", "g");
const PAREN_GROUP = /\(([^()]*)\)/g;
const DASH_IN_PAREN = new RegExp("([" + DASH + "])(?=\\S)", "g");
const glueDashes = v => {
  if (typeof v !== "string") return v;
  const rangedNumbers = v.replace(DIGIT_RANGE_DASH, "$1$2" + WJ);
  return rangedNumbers.replace(PAREN_GROUP, (m, inner) => "(" + inner.replace(DASH_IN_PAREN, "$1" + WJ) + ")");
};

/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, glueDashes(c))))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = glueDashes(highlight ? value.value : value);
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/actions/ScrollCue.jsx
try { (() => {
/** Circled arrow plus a short promise, telling the reader the document continues. */
function ScrollCue({
  label = "The case study continues",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "center",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", null, label), /*#__PURE__*/React.createElement("i", {
    style: {
      width: 26,
      height: 26,
      display: "grid",
      placeItems: "center",
      border: "1px solid var(--stage-line)",
      borderRadius: "var(--radius-round)",
      color: "var(--signal-stage)",
      animation: "scroll-nudge var(--dur-nudge) ease-in-out infinite",
      fontStyle: "normal"
    }
  }, "↓"));
}
Object.assign(__ds_scope, { ScrollCue });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ScrollCue.jsx", error: String((e && e.message) || e) }); }

// components/actions/StageAction.jsx
try { (() => {
const ACCENTS = {
  action: "var(--signal-stage)",
  signal: "var(--signal)",
  ink: "var(--ink)",
  stage: "var(--stage-ink)"
};

/** The system's only button: a text label on a 1px underline with a drawn arrow. */
function StageAction({
  accent = "action",
  href,
  onClick,
  reverse = false,
  children,
  style,
  ...rest
}) {
  const color = ACCENTS[accent];
  const shared = {
    width: "fit-content",
    display: "inline-flex",
    alignItems: "center",
    gap: 17,
    padding: "0 0 7px",
    border: 0,
    borderBottom: "1px solid " + color,
    background: "transparent",
    color,
    fontFamily: "var(--font-sans)",
    fontSize: 13,
    fontWeight: 700,
    letterSpacing: "-0.02em",
    whiteSpace: "nowrap",
    cursor: "pointer",
    ...style
  };
  const content = reverse ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, {
    reverse: true
  }), children) : /*#__PURE__*/React.createElement(React.Fragment, null, children, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, null));
  if (href) return /*#__PURE__*/React.createElement("a", {
    href: href,
    style: shared,
    ...rest
  }, content);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    style: shared,
    ...rest
  }, content);
}
Object.assign(__ds_scope, { StageAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/StageAction.jsx", error: String((e && e.message) || e) }); }

// components/document/DocFigure.jsx
try { (() => {
/** A numbered figure or table. Two things a report needs that an exhibition does not: a number a
 *  reader can cite, and permission to break out past the reading measure — a wide table is
 *  unreadable squeezed into a prose column.
 *
 *  Any of the system's instruments can sit inside one. In exhibition mode an instrument IS the
 *  moment; here it is evidence with a number attached, which is the whole difference in register. */
function DocFigure({
  kind = "figure",
  caption,
  source,
  wide = false,
  children,
  id,
  style,
  ...rest
}) {
  const isTable = kind === "table";
  return /*#__PURE__*/React.createElement("figure", {
    id: id,
    className: isTable ? "ds-doc-table" : "ds-doc-figure",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: wide ? "none" : "var(--doc-measure)",
      marginLeft: wide ? "0" : "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, children), /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginTop: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: isTable ? "ds-doc-tablabel" : "ds-doc-figlabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, caption), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "break-word"
    }
  }, source))));
}
Object.assign(__ds_scope, { DocFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/DocFigure.jsx", error: String((e && e.message) || e) }); }

// components/document/Endnotes.jsx
try { (() => {
/** Endnotes and references — the two things that let a reader check you.
 *
 *  `NoteRef` marks a claim in the prose; `Endnotes` lists them at the end. The numbering is a CSS
 *  counter shared with the marks, so a note can never be numbered wrong. References are separate
 *  because they are a different kind of object: a note qualifies a claim, a reference names a source.
 *  A report that measures things needs both, and their absence is what makes an argument look like
 *  an assertion. */
function NoteRef({
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-noteref",
    "aria-hidden": "true",
    style: style,
    ...rest
  });
}
function Endnotes({
  notes = [],
  label = "Notes",
  style,
  ...rest
}) {
  if (!notes.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    style: {
      marginTop: "var(--space-11)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      padding: "9px 0",
      borderBottom: i === notes.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, i + 1), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5
    }
  }, n)))));
}
function References({
  items = [],
  label = "References",
  style,
  ...rest
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    className: "ds-doc-refs",
    style: {
      marginTop: "var(--space-10)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ul", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "9px 0",
      borderBottom: i === items.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5,
      textIndent: "-1.2em",
      paddingLeft: "1.2em"
    }
  }, r.href ? /*#__PURE__*/React.createElement("a", {
    href: r.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "inherit",
      textDecoration: "none",
      borderBottom: "1px solid var(--story-accent)"
    }
  }, r.text) : r.text))));
}
Object.assign(__ds_scope, { NoteRef, Endnotes, References });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Endnotes.jsx", error: String((e && e.message) || e) }); }

// components/document/Finding.jsx
try { (() => {
/** A finding stated twice: once in English, once in numbers.
 *
 *  A report that only speaks in measurements excludes everyone who has not read the method, and a
 *  report that only speaks in plain language cannot be checked. So both, adjacent, with the plain
 *  sentence leading — a reader travelling fast reads only those and still gets the argument; a
 *  reader who wants proof drops one line.
 *
 *  This is the receiver-side / producer-side distinction as a typographic device. "Help headings
 *  repeat themselves" is what a reader experiences. "Lexical diversity 1.45 against a 4.9–7.2 band"
 *  is what the writer did. Neither one alone is the finding. */
function Finding({
  children,
  measured,
  verdict,
  style,
  ...rest
}) {
  const tone = verdict === "noise" ? "var(--story-muted)" : verdict === "underpowered" ? "var(--story-muted)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      margin: "0 0 var(--space-6)",
      paddingLeft: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(17px,1.5vw,21px)",
      fontWeight: 620,
      lineHeight: 1.34,
      letterSpacing: "-0.032em",
      textWrap: "pretty"
    }
  }, children), (measured || verdict) && /*#__PURE__*/React.createElement("p", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "0 14px",
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      lineHeight: 1.5
    }
  }, measured && /*#__PURE__*/React.createElement("span", null, measured), verdict && /*#__PURE__*/React.createElement("span", {
    style: {
      color: tone
    }
  }, verdict)));
}
Object.assign(__ds_scope, { Finding });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Finding.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  plain,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), plain && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 10,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "In plain terms"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(18px,1.6vw,23px)",
      fontWeight: 620,
      lineHeight: 1.36,
      letterSpacing: "-0.033em",
      textWrap: "pretty"
    }
  }, plain)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Method and scope"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/document/ReportSection.jsx
try { (() => {
/** A numbered, citable section. The number hangs in the gutter — the same anatomy the evidence
 *  components use for their labels, so document mode reads as the same system rather than a
 *  different one. Numbering is done by CSS counters, so inserting a section never renumbers by hand
 *  and a section can never claim a number it does not have. */
function ReportSection({
  level = 1,
  title,
  children,
  id,
  style,
  ...rest
}) {
  const sub = level > 1;
  const Heading = sub ? "h3" : "h2";
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: sub ? "ds-doc-subsection" : "ds-doc-section",
    style: {
      marginTop: sub ? "var(--space-9)" : "var(--space-11)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-num",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: sub ? 10 : 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement(Heading, {
    style: {
      margin: 0,
      maxWidth: "var(--doc-measure)",
      fontFamily: "var(--font-sans)",
      fontSize: sub ? "clamp(16px,1.35vw,19px)" : "clamp(20px,1.9vw,26px)",
      fontWeight: sub ? 650 : 700,
      lineHeight: 1.2,
      letterSpacing: "-0.035em",
      textWrap: "balance"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: "var(--space-5)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, children));
}
Object.assign(__ds_scope, { ReportSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/ReportSection.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ArtifactFigure.jsx
try { (() => {
/** Framed artifact with a gutter-aligned provenance caption. No height floor by default — the frame is
 *  the artifact's own edge, so a short artifact gets a short frame. Pass `minHeight` only when a set of
 *  figures genuinely has to align to each other. */
function ArtifactFigure({
  src,
  alt = "",
  label,
  caption,
  contain = false,
  zoom = false,
  minHeight,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    className: zoom ? "ds-figure-zoom" : undefined,
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight,
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--surface-mat)",
      boxShadow: "var(--story-shadow)",
      padding: contain ? "clamp(14px,1.8vw,24px)" : 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: "100%",
      height: minHeight ? "100%" : "auto",
      minHeight: minHeight ? minHeight - 40 : undefined,
      display: "block",
      objectFit: contain ? "contain" : "cover"
    }
  })), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { ArtifactFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ArtifactFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/BeforeAfter.jsx
try { (() => {
/** A rewrite delivered as a diff with its measurements attached, so the person deciding whether to
 *  take it can see what moved and by how much. The deltas are the argument — without them this is
 *  just someone else's preferred wording. */
function BeforeAfter({
  before,
  after,
  beforeLabel = "Before",
  afterLabel = "After",
  deltas = [],
  actions,
  style,
  ...rest
}) {
  const pane = (label, text, tone) => /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "14px 16px",
      borderRight: tone === "before" ? "1px solid var(--story-line)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: tone === "after" ? "var(--story-accent)" : "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: tone === "before" ? "var(--story-muted)" : "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: tone === "after" ? 560 : 480,
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, text));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      borderBottom: "1px solid var(--story-line)"
    }
  }, pane(beforeLabel, before, "before"), pane(afterLabel, after, "after")), deltas.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(deltas.length, 1) + ",minmax(0,1fr))",
      borderBottom: actions ? "1px solid var(--story-line)" : 0
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        padding: "11px 14px",
        borderRight: i === deltas.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "block",
        marginBottom: 6,
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "baseline",
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 13,
        fontWeight: 560,
        letterSpacing: "-0.03em",
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-accent)",
        fontSize: 17,
        fontWeight: 700,
        letterSpacing: "-0.04em"
      }
    }, d.to)));
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-8)",
      alignItems: "center",
      padding: "12px 16px"
    }
  }, actions));
}
Object.assign(__ds_scope, { BeforeAfter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/BeforeAfter.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CaptureFigure.jsx
try { (() => {
/** A capture of shipped behaviour, in a frame that says what you are looking at.
 *
 *  Video is the only honest way to show an interaction that happens in time, so this plays inline —
 *  muted, looping, playsinline, with controls available. Autoplay is off by default: the accessibility
 *  floor forbids a loop that plays without the reader asking for it. */
function CaptureFigure({
  src,
  poster,
  label,
  caption,
  quote,
  autoPlay = false,
  controls = true,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--ink)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("video", {
    src: src,
    poster: poster,
    muted: true,
    loop: true,
    playsInline: true,
    autoPlay: autoPlay,
    controls: controls,
    preload: "metadata",
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), quote && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "The string"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, quote)), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { CaptureFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CaptureFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CardGrid.jsx
try { (() => {
/** Hairline grid block, on the instrument grammar: an optional silkscreen header naming what the set is
 *  and how many are in it, then cells sized by their contents. Without a label it stays a bare grid —
 *  but if you cannot name the set, ask whether it needed a frame at all. */
function CardGrid({
  columns = 2,
  raised = true,
  label,
  scope,
  children,
  style,
  ...rest
}) {
  const count = React.Children.count(children);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, scope || String(count).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, children));
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
/** A hyphen or dash already in the text is a standard line-break point (Unicode UAX #14) in every
 *  browser, with no special CSS involved, and MOST of the time that is correct: "help-centre" wrapping
 *  to "help-" / "centre" at a narrow column is ordinary, everyday typography — books do it every day —
 *  and gluing it shut would make the word un-searchable and un-copyable (the rendered string would
 *  carry an invisible character a reader can't see or type back). Two narrow cases are genuinely wrong
 *  rather than merely unlucky, because there the two sides of the dash are not independently meaningful
 *  words: a digit range ("2025-2026" splitting to "2025-" / "2026" reads as a typo, not a range) and a
 *  dash-joined token inside a parenthetical statistical label ("p (BH-adj)" splitting to "p (BH-" /
 *  "adj)" garbles the abbreviation). Only those two get a zero-width word joiner (U+2060) glued to the
 *  dash; every other hyphenated compound is left exactly as written. */
const DASH = "\\u002D\\u2010-\\u2015"; // hyphen-minus; hyphen through horizontal bar (covers en/em dash)
const WJ = "⁠"; // word joiner — zero-width, forbids a line break at this point, otherwise invisible
const DIGIT_RANGE_DASH = new RegExp("(\\d)([" + DASH + "])(?=\\d)", "g");
const PAREN_GROUP = /\(([^()]*)\)/g;
const DASH_IN_PAREN = new RegExp("([" + DASH + "])(?=\\S)", "g");
const glueDashes = v => {
  if (typeof v !== "string") return v;
  const rangedNumbers = v.replace(DIGIT_RANGE_DASH, "$1$2" + WJ);
  return rangedNumbers.replace(PAREN_GROUP, (m, inner) => "(" + inner.replace(DASH_IN_PAREN, "$1" + WJ) + ")");
};

/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, glueDashes(c))))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = glueDashes(highlight ? value.value : value);
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DisciplineVenn.jsx
try { (() => {
/** Three disciplines and the work that only exists where they overlap. Drawn as a registration
 *  diagram — hairline circles, no fills, the intersection marked the way the system marks a
 *  registration point — so it reads as a technical figure rather than a slide graphic. */
function DisciplineVenn({
  fields = [],
  intersection,
  caption,
  tone = "story",
  style,
  ...rest
}) {
  const ink = tone === "stage" ? "var(--stage-ink)" : "var(--story-ink)";
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const muted = tone === "stage" ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = tone === "stage" ? "var(--signal-stage)" : "var(--story-accent)";

  /* Three circles on an equilateral arrangement, centred on 150,142. */
  const circles = [{
    cx: 150,
    cy: 96,
    label: fields[0]
  }, {
    cx: 111,
    cy: 164,
    label: fields[1]
  }, {
    cx: 189,
    cy: 164,
    label: fields[2]
  }];
  /* Labels sit outside the circle field so a long discipline name cannot crowd its neighbour. */
  const anchors = [{
    x: 150,
    y: 20,
    align: "middle"
  }, {
    x: -34,
    y: 240,
    align: "start"
  }, {
    x: 334,
    y: 240,
    align: "end"
  }];
  const leaders = [{
    x1: 150,
    y1: 27,
    x2: 150,
    y2: 36
  }, {
    x1: 62,
    y1: 233,
    x2: 84,
    y2: 213
  }, {
    x1: 238,
    y1: 233,
    x2: 216,
    y2: 213
  }];
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      padding: "var(--space-7) 0",
      border: "1px solid " + line,
      background: tone === "stage" ? "transparent" : "var(--story-raised)",
      boxShadow: tone === "stage" ? "none" : "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "-40 8 380 244",
    width: "100%",
    style: {
      maxWidth: 440,
      height: "auto",
      overflow: "visible"
    },
    role: "img",
    "aria-label": fields.join(", ") + " — " + intersection
  }, circles.map(c => /*#__PURE__*/React.createElement("circle", {
    key: c.label,
    cx: c.cx,
    cy: c.cy,
    r: "62",
    fill: "none",
    stroke: line,
    strokeWidth: "1"
  })), leaders.map((l, i) => /*#__PURE__*/React.createElement("line", {
    key: i,
    x1: l.x1,
    y1: l.y1,
    x2: l.x2,
    y2: l.y2,
    stroke: line,
    strokeWidth: "1"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "15",
    fill: "none",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "150",
    y1: "120",
    x2: "150",
    y2: "164",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "128",
    y1: "142",
    x2: "172",
    y2: "142",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "3.5",
    fill: accent
  }), circles.map((c, i) => /*#__PURE__*/React.createElement("text", {
    key: c.label,
    x: anchors[i].x,
    y: anchors[i].y,
    textAnchor: anchors[i].align,
    fill: muted,
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c.label)))), (intersection || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "13px 0 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.4
    }
  }, "Overlap"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, intersection && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, intersection), caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: intersection ? "8px 0 0" : 0,
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, caption))));
}
Object.assign(__ds_scope, { DisciplineVenn });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DisciplineVenn.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FingerprintBars.jsx
try { (() => {
/** A measured voice fingerprint: one row per dimension, the reading drawn against the band a
 *  surface is allowed to sit in. The band is the point — a fingerprint is a tolerance, not a
 *  score, and a reading outside it is the only thing that counts as a miss. */
function FingerprintBars({
  dimensions = [],
  scale = 100,
  showBand = true,
  style,
  ...rest
}) {
  const pct = n => Math.max(0, Math.min(100, n / scale * 100));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, dimensions.map((d, i) => {
    const out = showBand && d.band && (d.value < d.band[0] || d.value > d.band[1]);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        display: "grid",
        gridTemplateColumns: "minmax(96px,0.9fr) minmax(0,2.4fr) 42px",
        gap: 14,
        alignItems: "center",
        padding: "9px 16px",
        borderBottom: i === dimensions.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700,
        lineHeight: 1.3
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        position: "relative",
        height: 9,
        display: "block",
        borderBottom: "1px solid var(--story-line)"
      }
    }, showBand && d.band && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 0,
        bottom: 0,
        left: pct(d.band[0]) + "%",
        width: pct(d.band[1]) - pct(d.band[0]) + "%",
        background: "var(--story-band)",
        borderLeft: "1px solid var(--story-line)",
        borderRight: "1px solid var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 2,
        bottom: 2,
        left: 0,
        width: pct(d.value) + "%",
        background: out ? "var(--story-accent)" : "var(--story-ink)"
      }
    })), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 14,
        fontWeight: 700,
        letterSpacing: "-0.04em",
        textAlign: "right",
        fontVariantNumeric: "tabular-nums"
      }
    }, d.value));
  }));
}
Object.assign(__ds_scope, { FingerprintBars });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FingerprintBars.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 14px",
      borderRight: "1px solid " + line,
      overflowWrap: "break-word"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35,
      overflowWrap: "break-word"
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content
 *  column. No height floor — a short cell is short. The padding is an instrument's, not a page margin's. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter-tight) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "var(--card-pad) 12px var(--card-pad) 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--card-pad) var(--card-pad-wide)"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 5,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 9,
      padding: "8px 10px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "6px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Label above value, so a cell is only as wide as the wider of the two and a row of four fits without
 *  stretching. Side-by-side reads fine until a value is a word rather than a number, at which point the
 *  cells go ragged and the label and the number end up at opposite ends of a void. This is the same
 *  stacking `VoiceProfile` uses, which is the system's answer for a set of small readings. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: "0 1 auto",
      padding: "12px 18px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Outcome.jsx
try { (() => {
/** Amber label over a short results list or paragraph. Sits at the end of a stage copy column. */
function Outcome({
  label = "Results",
  items,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(430px,88%)",
      marginTop: 18,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--signal-stage)",
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      fontWeight: 700
    }
  }, label), items && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: "5px 0 0",
      padding: 0,
      listStyle: "none",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, item))), children && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, children));
}
Object.assign(__ds_scope, { Outcome });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Outcome.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Pipeline.jsx
try { (() => {
/** Ordered process rows. Step number in the gutter, the rule on one line — compressed, not spaced out. */
function Pipeline({
  steps = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      listStyle: "none",
      ...style
    },
    ...rest
  }, steps.map((step, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === steps.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "14px 12px 14px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, step.step || String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "13px 18px",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(15px,1.15vw,17px)",
      fontWeight: 560,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, step.label))));
}
Object.assign(__ds_scope, { Pipeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Pipeline.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      /* Sized by the words, not by a column. A quote that wraps short leaves no dead ground. */
      width: "fit-content",
      maxWidth: 560,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResearchGrid.jsx
try { (() => {
/** Published sources behind a measurement, one row each: who, when, and the thing it makes computable.
 *  A citation row is the difference between an instrument and an opinion, so the source is never
 *  paraphrased away — it stays attributable. */
function ResearchGrid({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    style: {
      padding: "15px 18px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, item.source), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, item.year)), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "9px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.12,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, item.gives), item.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.note))));
}
Object.assign(__ds_scope, { ResearchGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResearchGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResumeEntry.jsx
try { (() => {
/** One record in a résumé timeline: dates in the label gutter, org and role, then what I did there. */
function ResumeEntry({
  when,
  org,
  role,
  bullets = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: "clamp(16px,2vw,28px)",
      padding: "var(--space-7) 0",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      paddingTop: 3,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.45
    }
  }, when), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--display-s-tracking)"
    }
  }, org), role && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, role), bullets.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 11,
      margin: "var(--space-5) 0 0",
      padding: 0,
      listStyle: "none"
    }
  }, bullets.map((b, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "14px minmax(0,1fr)",
      gap: 10,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em",
      textWrap: "pretty"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      marginTop: "0.62em",
      width: 7,
      height: 1,
      background: "var(--story-accent)"
    }
  }), b)))));
}
Object.assign(__ds_scope, { ResumeEntry });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResumeEntry.jsx", error: String((e && e.message) || e) }); }

// components/evidence/RuleSplit.jsx
try { (() => {
/** Two-column sort of a guidance set: what a model has to judge, and what code can check.
 *  The whole argument for a measurement layer fits in one of these — a reader can see the
 *  proportion before reading a single row. */
function RuleSplit({
  modelLabel = "Model judgment",
  codeLabel = "Checkable in code",
  rules = [],
  note,
  style,
  ...rest
}) {
  const model = rules.filter(r => r.kind === "model");
  const code = rules.filter(r => r.kind === "code");
  const column = (label, items, accent) => /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      borderRight: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "10px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.rule))));
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      border: "1px solid var(--story-line)",
      borderRight: 0,
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, column(modelLabel, model, "var(--story-muted)"), column(codeLabel, code, "var(--story-accent)")), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, note));
}
Object.assign(__ds_scope, { RuleSplit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/RuleSplit.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
/** Ruled inventory of inputs, sized by what it names.
 *
 *  Each entry is as wide as its own number-and-name pair and wraps when it runs out of room. An entry
 *  does not stretch to half the page and then sit alone at the left edge of it. */
function SourceMap({
  items = [],
  label = "Inputs",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 24,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "flex",
      gap: 12,
      alignItems: "baseline",
      flex: "0 1 auto",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal",
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item)))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/TenGrid.jsx
try { (() => {
/** Ten hairline cells; filled cells read as passes. A dimension meter, not a progress bar. */
function TenGrid({
  total = 10,
  pass = 0,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + total + ",1fr)",
      gap: 4,
      marginTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, Array.from({
    length: total
  }).map((_, i) => /*#__PURE__*/React.createElement("i", {
    key: i,
    style: {
      height: 10,
      border: "1px solid " + (i < pass ? "var(--story-accent)" : "var(--story-muted)"),
      background: i < pass ? "var(--story-accent)" : "transparent"
    }
  })));
}
Object.assign(__ds_scope, { TenGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/TenGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Toolchain.jsx
try { (() => {
/** What a system was actually built out of, named and linked so a reader can go and check.
 *
 *  The engineering counterpart to `ResearchGrid`: same anatomy, same register. Deliberately no logos —
 *  a row of third-party brand marks would be the one place this system's ink-and-hairline language
 *  breaks, and it is the oldest tell in portfolio design. A name, a licence, and what it actually
 *  provides is more credible than a badge, because it can be verified. */
function Toolchain({
  items = [],
  label = "Built with",
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: item.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(140px,0.9fr) minmax(0,2fr) auto",
      gap: "clamp(12px,2vw,26px)",
      alignItems: "baseline",
      padding: "12px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, item.href ? /*#__PURE__*/React.createElement("a", {
    href: item.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em",
      textDecoration: "none",
      borderBottom: "1px solid currentColor",
      width: "fit-content",
      paddingBottom: 1
    }
  }, item.name) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em"
    }
  }, item.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.provides), item.meta && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      textAlign: "right"
    }
  }, item.meta)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 640,
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45
    }
  }, note));
}
Object.assign(__ds_scope, { Toolchain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Toolchain.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceProfile.jsx
try { (() => {
/** The target a surface is written to, published so a team can argue with it: each dimension's
 *  setting and the one sentence saying why it sits there. A profile without its reasoning is a
 *  number nobody will honour. */
function VoiceProfile({
  product,
  surface,
  dimensions = [],
  notes = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Voice profile"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, [product, surface].filter(Boolean).join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit,minmax(128px,1fr))",
      borderBottom: notes.length ? "1px solid var(--story-line)" : 0
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      padding: "13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 8,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      height: 3,
      display: "block",
      marginTop: 9,
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: d.value + "%",
      background: "var(--story-accent)"
    }
  }))))), notes.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === notes.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, n.name), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, n.why)))));
}
Object.assign(__ds_scope, { VoiceProfile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceProfile.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceScoreRing.jsx
try { (() => {
/** How a reading reaches the person who has to act on it: one alignment figure, the dimensions
 *  underneath it, and a verdict in words. The arc draws itself on mount rather than fading in —
 *  a measurement should look like it was taken, not like it was already there. */
function VoiceScoreRing({
  score = 0,
  outOf = 100,
  label = "Voice alignment",
  verdict,
  scope = [],
  dimensions = [],
  size = 148,
  style,
  ...rest
}) {
  const [drawn, setDrawn] = React.useState(false);
  React.useEffect(() => {
    /* Two frames, so the browser paints the zero state before the arc animates to its value.
       Both ids are tracked — cancelling only the outer one leaves the inner frame live. */
    let inner;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setDrawn(true));
    });
    return () => {
      cancelAnimationFrame(outer);
      if (inner) cancelAnimationFrame(inner);
    };
  }, []);
  const r = (size - 18) / 2;
  const c = 2 * Math.PI * r;
  const filled = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope.length > 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, scope.join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(18px,2.4vw,32px)",
      alignItems: "center",
      padding: "18px 16px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: "7",
    strokeLinecap: "butt",
    strokeDasharray: c,
    strokeDashoffset: drawn ? c * (1 - filled) : c,
    style: {
      transition: "stroke-dashoffset var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "of ", outOf))), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, verdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 13px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)"
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(80px,1fr) minmax(0,1.6fr) 34px",
      gap: 12,
      alignItems: "center",
      padding: "7px 0",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 5,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: (drawn ? d.value : 0) + "%",
      background: "var(--story-ink)",
      transition: "width var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value)))))));
}
Object.assign(__ds_scope, { VoiceScoreRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceScoreRing.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Eyebrow.jsx
try { (() => {
const TONES = {
  muted: "var(--story-muted, var(--muted-ink))",
  signal: "var(--signal)",
  action: "var(--signal-stage)",
  stage: "var(--stage-muted)"
};

/** Small mono label that opens a scene, a section, or a card. */
function Eyebrow({
  tone = "muted",
  size = "eyebrow",
  as = "p",
  children,
  style,
  ...rest
}) {
  const sizes = {
    eyebrow: {
      fontSize: "var(--eyebrow-size)",
      fontWeight: "var(--eyebrow-weight)",
      letterSpacing: "var(--eyebrow-tracking)",
      fontFamily: "var(--font-sans)"
    },
    meta: {
      fontSize: "var(--meta-size)",
      fontWeight: "var(--meta-weight)",
      letterSpacing: "-0.01em",
      fontFamily: "var(--font-mono)"
    },
    micro: {
      fontSize: "var(--micro-size)",
      fontWeight: "var(--micro-weight)",
      letterSpacing: "0",
      fontFamily: "var(--font-mono)"
    }
  };
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      margin: 0,
      lineHeight: "var(--eyebrow-leading)",
      color: TONES[tone],
      ...sizes[size],
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/LineArrow.jsx
try { (() => {
/** The drawn arrow glyph: 1px rule plus a rotated corner. Inherits currentColor. */
function LineArrow({
  reverse = false,
  width = 34,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      display: "inline-block",
      flex: "0 0 " + width + "px",
      width,
      height: 12,
      transform: reverse ? "rotate(180deg)" : undefined,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 5,
      right: 0,
      left: 0,
      height: 1,
      background: "currentColor"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 2,
      right: 0,
      width: 7,
      height: 7,
      borderTop: "1px solid currentColor",
      borderRight: "1px solid currentColor",
      transform: "rotate(45deg)"
    }
  }));
}
Object.assign(__ds_scope, { LineArrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/LineArrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/ProvenanceNote.jsx
try { (() => {
/** Hairline-topped note that states what an artifact is and is not. */
function ProvenanceNote({
  label = "Provenance",
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      marginTop: "var(--space-6)",
      paddingTop: "var(--space-4)",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 700,
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.5,
      letterSpacing: "-0.015em"
    }
  }, children));
}
Object.assign(__ds_scope, { ProvenanceNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/ProvenanceNote.jsx", error: String((e && e.message) || e) }); }

// components/foundation/RegistrationMark.jsx
try { (() => {
/** Corner crosshair with a gold centre — the print-registration device used on every stage. */
function RegistrationMark({
  size = 22,
  color = "var(--stage-ink)",
  dot = "var(--signal-stage)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      top: 22,
      right: 22,
      width: size,
      height: size,
      opacity: 0.65,
      pointerEvents: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: size / 2 - 1,
      right: 0,
      left: 0,
      height: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: size / 2 - 1,
      width: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 4,
      border: "1px solid " + color,
      borderRadius: "var(--radius-round)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 8,
      borderRadius: "var(--radius-round)",
      background: dot
    }
  }));
}
Object.assign(__ds_scope, { RegistrationMark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/RegistrationMark.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Tag.jsx
try { (() => {
/** Hairline capability tag. Square corners, no fill. */
function Tag({
  on = "stage",
  children,
  style,
  ...rest
}) {
  const border = on === "stage" ? "var(--stage-line)" : "var(--soft-line)";
  return /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "9px 11px",
      border: "1px solid " + border,
      fontFamily: "var(--font-sans)",
      fontSize: 10,
      letterSpacing: "-0.01em",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Tag.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum.
 *  `as` sets the element, so a row inside an ordered list can be a real `li` rather than a div. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  as = "div",
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const Tag = as;
  if (!gutter) {
    return /*#__PURE__*/React.createElement(Tag, {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      listStyle: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel, PanelRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/layout/PaperFrame.jsx
try { (() => {
/** The bounded document: a paper card on a darker desk, hairline border, hard offset shadow, grain overlay. */
function PaperFrame({
  children,
  grain = true,
  columns = "var(--rail-width) minmax(0,1fr)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100%",
      background: "var(--surface-desk)",
      fontFamily: "var(--font-body)",
      color: "var(--ink)"
    }
  }, grain && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      zIndex: 5000,
      inset: 0,
      pointerEvents: "none",
      opacity: "var(--texture-grain-opacity)",
      mixBlendMode: "multiply",
      backgroundImage: "var(--texture-grain)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "min(calc(100% - 24px)," + "var(--frame-max))",
      height: "calc(100svh - 24px)",
      minHeight: 700,
      display: "grid",
      gridTemplateColumns: columns,
      margin: "var(--frame-inset) auto",
      overflow: "hidden",
      border: "1px solid var(--ink)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-4)",
      ...style
    },
    ...rest
  }, children));
}
Object.assign(__ds_scope, { PaperFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/PaperFrame.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stage.jsx
try { (() => {
/** The pine authored stage: gradient field, clipped, with an optional registration mark. */
function Stage({
  children,
  variant = "field",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      position: "relative",
      zIndex: 1,
      minWidth: 0,
      minHeight: 0,
      overflow: "hidden",
      isolation: "isolate",
      background: variant === "section" ? "var(--stage-field-section)" : "var(--stage-field)",
      color: "var(--stage-ink)",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Stage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stage.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title &&
  /*#__PURE__*/
  /* data-doc-heading + tabIndex={-1}: the one place per document the app moves focus to
     after a destination change (see app.jsx). Focusable without joining the tab order;
     the ring itself is drawn by :focus-visible in tokens/interaction.css, so a mouse
     click never shows one and a keyboard move always does. */
  React.createElement("h1", {
    "data-doc-heading": true,
    tabIndex: -1,
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      transform: direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)",
      willChange: "transform",
      animation: plate,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }

// components/layout/StorySection.jsx
try { (() => {
/** A tone-scoped chapter of a case document: index, kicker, title, intro, then content. */
function StorySection({
  tone = "paper",
  index,
  chapterLabel,
  kicker,
  title,
  intro,
  children,
  id,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      display: "grid",
      gridTemplateColumns: "70px minmax(0,1fr)",
      gap: "clamp(28px,5vw,82px)",
      padding: "var(--section-pad) var(--gutter)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700
    }
  }, chapterLabel && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, chapterLabel), index && /*#__PURE__*/React.createElement("b", {
    style: {
      display: "block",
      color: "var(--story-accent)"
    }
  }, index)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 980
    }
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 16px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-m-size)",
      fontWeight: "var(--display-m-weight)",
      lineHeight: "var(--display-m-leading)",
      letterSpacing: "var(--display-m-tracking)",
      textWrap: "balance"
    }
  }, title), intro && /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 760,
      marginTop: 20,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      minWidth: 0,
      gridColumn: "1 / -1",
      color: "var(--story-ink)"
    }
  }, children));
}
Object.assign(__ds_scope, { StorySection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StorySection.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ChapterNav.jsx
try { (() => {
/** Sticky chapter map. The chapter in view inverts to an ink chip. */
function ChapterNav({
  label = "Case map",
  chapters = [],
  active,
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    style: {
      position: "sticky",
      zIndex: 45,
      top: 0,
      minHeight: "var(--chapter-nav-height)",
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(22px,3vw,48px)",
      alignItems: "center",
      padding: "var(--space-2) var(--gutter-tight)",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      backdropFilter: "blur(14px)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 760,
      whiteSpace: "nowrap"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(chapters.length, 1) + ",minmax(74px,1fr))",
      gap: 4
    }
  }, chapters.map(c => {
    const on = c.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      type: "button",
      className: "ds-tab ds-tab-chapter",
      onClick: () => onSelect && onSelect(c.id),
      "aria-current": on ? "true" : undefined,
      style: {
        position: "relative",
        minHeight: 34,
        padding: "0 8px",
        border: 0,
        background: on ? "var(--ink)" : "transparent",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        cursor: "pointer",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, c.label);
  })));
}
Object.assign(__ds_scope, { ChapterNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ChapterNav.jsx", error: String((e && e.message) || e) }); }




// components/actions/ScrollCue.jsx
try { (() => {
/** Circled arrow plus a short promise, telling the reader the document continues. */
function ScrollCue({
  label = "The case study continues",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "center",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", null, label), /*#__PURE__*/React.createElement("i", {
    style: {
      width: 26,
      height: 26,
      display: "grid",
      placeItems: "center",
      border: "1px solid var(--stage-line)",
      borderRadius: "var(--radius-round)",
      color: "var(--signal-stage)",
      animation: "scroll-nudge var(--dur-nudge) ease-in-out infinite",
      fontStyle: "normal"
    }
  }, "↓"));
}
Object.assign(__ds_scope, { ScrollCue });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ScrollCue.jsx", error: String((e && e.message) || e) }); }

// components/actions/StageAction.jsx
try { (() => {
const ACCENTS = {
  action: "var(--signal-stage)",
  signal: "var(--signal)",
  ink: "var(--ink)",
  stage: "var(--stage-ink)"
};

/** The system's only button: a text label on a 1px underline with a drawn arrow. */
function StageAction({
  accent = "action",
  href,
  onClick,
  reverse = false,
  children,
  style,
  ...rest
}) {
  const color = ACCENTS[accent];
  const shared = {
    width: "fit-content",
    display: "inline-flex",
    alignItems: "center",
    gap: 17,
    padding: "0 0 7px",
    border: 0,
    borderBottom: "1px solid " + color,
    background: "transparent",
    color,
    fontFamily: "var(--font-sans)",
    fontSize: 13,
    fontWeight: 700,
    letterSpacing: "-0.02em",
    whiteSpace: "nowrap",
    cursor: "pointer",
    ...style
  };
  const content = reverse ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, {
    reverse: true
  }), children) : /*#__PURE__*/React.createElement(React.Fragment, null, children, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, null));
  if (href) return /*#__PURE__*/React.createElement("a", {
    href: href,
    style: shared,
    ...rest
  }, content);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    style: shared,
    ...rest
  }, content);
}
Object.assign(__ds_scope, { StageAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/StageAction.jsx", error: String((e && e.message) || e) }); }

// components/document/DocFigure.jsx
try { (() => {
/** A numbered figure or table. Two things a report needs that an exhibition does not: a number a
 *  reader can cite, and permission to break out past the reading measure — a wide table is
 *  unreadable squeezed into a prose column.
 *
 *  Any of the system's instruments can sit inside one. In exhibition mode an instrument IS the
 *  moment; here it is evidence with a number attached, which is the whole difference in register. */
function DocFigure({
  kind = "figure",
  caption,
  source,
  wide = false,
  children,
  id,
  style,
  ...rest
}) {
  const isTable = kind === "table";
  return /*#__PURE__*/React.createElement("figure", {
    id: id,
    className: isTable ? "ds-doc-table" : "ds-doc-figure",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: wide ? "none" : "var(--doc-measure)",
      marginLeft: wide ? "0" : "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, children), /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginTop: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: isTable ? "ds-doc-tablabel" : "ds-doc-figlabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, caption), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "break-word"
    }
  }, source))));
}
Object.assign(__ds_scope, { DocFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/DocFigure.jsx", error: String((e && e.message) || e) }); }

// components/document/Endnotes.jsx
try { (() => {
/** Endnotes and references — the two things that let a reader check you.
 *
 *  `NoteRef` marks a claim in the prose; `Endnotes` lists them at the end. The numbering is a CSS
 *  counter shared with the marks, so a note can never be numbered wrong. References are separate
 *  because they are a different kind of object: a note qualifies a claim, a reference names a source.
 *  A report that measures things needs both, and their absence is what makes an argument look like
 *  an assertion. */
function NoteRef({
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-noteref",
    "aria-hidden": "true",
    style: style,
    ...rest
  });
}
function Endnotes({
  notes = [],
  label = "Notes",
  style,
  ...rest
}) {
  if (!notes.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    style: {
      marginTop: "var(--space-11)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      padding: "9px 0",
      borderBottom: i === notes.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, i + 1), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5
    }
  }, n)))));
}
function References({
  items = [],
  label = "References",
  style,
  ...rest
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    className: "ds-doc-refs",
    style: {
      marginTop: "var(--space-10)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ul", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "9px 0",
      borderBottom: i === items.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5,
      textIndent: "-1.2em",
      paddingLeft: "1.2em"
    }
  }, r.href ? /*#__PURE__*/React.createElement("a", {
    href: r.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "inherit",
      textDecoration: "none",
      borderBottom: "1px solid var(--story-accent)"
    }
  }, r.text) : r.text))));
}
Object.assign(__ds_scope, { NoteRef, Endnotes, References });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Endnotes.jsx", error: String((e && e.message) || e) }); }

// components/document/Finding.jsx
try { (() => {
/** A finding stated twice: once in English, once in numbers.
 *
 *  A report that only speaks in measurements excludes everyone who has not read the method, and a
 *  report that only speaks in plain language cannot be checked. So both, adjacent, with the plain
 *  sentence leading — a reader travelling fast reads only those and still gets the argument; a
 *  reader who wants proof drops one line.
 *
 *  This is the receiver-side / producer-side distinction as a typographic device. "Help headings
 *  repeat themselves" is what a reader experiences. "Lexical diversity 1.45 against a 4.9–7.2 band"
 *  is what the writer did. Neither one alone is the finding. */
function Finding({
  children,
  measured,
  verdict,
  style,
  ...rest
}) {
  const tone = verdict === "noise" ? "var(--story-muted)" : verdict === "underpowered" ? "var(--story-muted)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      margin: "0 0 var(--space-6)",
      paddingLeft: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(17px,1.5vw,21px)",
      fontWeight: 620,
      lineHeight: 1.34,
      letterSpacing: "-0.032em",
      textWrap: "pretty"
    }
  }, children), (measured || verdict) && /*#__PURE__*/React.createElement("p", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "0 14px",
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      lineHeight: 1.5
    }
  }, measured && /*#__PURE__*/React.createElement("span", null, measured), verdict && /*#__PURE__*/React.createElement("span", {
    style: {
      color: tone
    }
  }, verdict)));
}
Object.assign(__ds_scope, { Finding });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Finding.jsx", error: String((e && e.message) || e) }); }

// components/document/PaperTable.jsx
try { (() => {
/** A citation or URL in `source` is a long, unbroken run of characters, and the box it sits in narrows
 *  with the viewport. A blunt `word-break: break-word` last-resort breaker WILL split that run once it
 *  stops fitting its line -- including mid-letter, with nothing at the split to explain it ("th" / "is"
 *  torn across a line break). A real URL already carries its own natural break points (/, _, ., :, ?,
 *  &, =, ,); <wbr/> offers those to the browser as OPTIONAL line-break opportunities -- invisible in
 *  the rendered page, absent from a copy-paste, and only ever used when a break is actually needed --
 *  so a long source wraps at its own punctuation instead of fracturing an arbitrary character run.
 *  Ordinary short source text is untouched: it already wraps at its spaces like any other prose. */
const URL_BREAK_CHARS = /([/_.:?&=,])/;
function withBreakOpportunities(text) {
  if (typeof text !== "string") return text;
  return text.split(URL_BREAK_CHARS).map((part, i) => i % 2 === 1 ? /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, part, /*#__PURE__*/React.createElement("wbr", null)) : part);
}

/** A table set the way papers set tables: rules, not boxes.
 *
 *  A heavy rule above the header, a hairline under it, a heavy rule under the last row, and nothing
 *  else — no vertical rules, no cell borders, no fill. This is the booktabs convention and it is the
 *  single strongest visual signal that a page is a paper rather than an interface. Boxed grids read
 *  as UI; ruled tables read as evidence.
 *
 *  The caption sits ABOVE, which is also the convention and is genuinely better: a reader meets the
 *  claim before the numbers instead of after them. Figures caption below; tables caption above. */
function PaperTable({
  caption,
  source,
  columns = [],
  rows = [],
  align = [],
  note,
  style,
  ...rest
}) {
  const cellAlign = i => align[i] || (i === 0 ? "left" : "right");
  return /*#__PURE__*/React.createElement("figure", {
    className: "ds-doc-table",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: "none",
      ...style
    },
    ...rest
  }, caption && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginBottom: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-tablabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.45
    }
  }, caption)), /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))"
    }
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      borderCollapse: "collapse",
      fontFamily: "var(--font-sans)",
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      padding: "9px 14px 8px 0",
      textAlign: cellAlign(i),
      verticalAlign: "bottom",
      borderTop: "2px solid var(--story-ink)",
      borderBottom: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap"
    }
  }, c)))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r
  }, row.map((v, c) => /*#__PURE__*/React.createElement("td", {
    key: c,
    style: {
      padding: "8px 14px 8px 0",
      textAlign: cellAlign(c),
      verticalAlign: "baseline",
      borderBottom: r === rows.length - 1 ? "2px solid var(--story-ink)" : "none",
      color: c === 0 ? "var(--story-ink)" : "var(--story-muted)",
      fontSize: 13,
      lineHeight: 1.5,
      fontWeight: c === 0 ? 560 : 400
    }
  }, v))))))), (note || source) && /*#__PURE__*/React.createElement("div", {
    style: {
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: 9
    }
  }, note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, note), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: note ? "5px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "normal",
      overflowWrap: "normal"
    }
  }, withBreakOpportunities(source))));
}
Object.assign(__ds_scope, { PaperTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/PaperTable.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  plain,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), plain && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 10,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "In plain terms"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(18px,1.6vw,23px)",
      fontWeight: 620,
      lineHeight: 1.36,
      letterSpacing: "-0.033em",
      textWrap: "pretty"
    }
  }, plain)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Method and scope"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/document/ReportSection.jsx
try { (() => {
/** A numbered, citable section. The number hangs in the gutter — the same anatomy the evidence
 *  components use for their labels, so document mode reads as the same system rather than a
 *  different one. Numbering is done by CSS counters, so inserting a section never renumbers by hand
 *  and a section can never claim a number it does not have. */
function ReportSection({
  level = 1,
  title,
  children,
  id,
  style,
  ...rest
}) {
  const sub = level > 1;
  const Heading = sub ? "h3" : "h2";
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: sub ? "ds-doc-subsection" : "ds-doc-section",
    style: {
      marginTop: sub ? "var(--space-9)" : "var(--space-11)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-num",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: sub ? 10 : 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement(Heading, {
    style: {
      margin: 0,
      maxWidth: "var(--doc-measure)",
      fontFamily: "var(--font-sans)",
      fontSize: sub ? "clamp(16px,1.35vw,19px)" : "clamp(20px,1.9vw,26px)",
      fontWeight: sub ? 650 : 700,
      lineHeight: 1.2,
      letterSpacing: "-0.035em",
      textWrap: "balance"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: "var(--space-5)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, children));
}
Object.assign(__ds_scope, { ReportSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/ReportSection.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ArtifactFigure.jsx
try { (() => {
/** Framed artifact with a gutter-aligned provenance caption. No height floor by default — the frame is
 *  the artifact's own edge, so a short artifact gets a short frame. Pass `minHeight` only when a set of
 *  figures genuinely has to align to each other. */
function ArtifactFigure({
  src,
  alt = "",
  label,
  caption,
  contain = false,
  zoom = false,
  minHeight,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    className: zoom ? "ds-figure-zoom" : undefined,
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight,
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--surface-mat)",
      boxShadow: "var(--story-shadow)",
      padding: contain ? "clamp(14px,1.8vw,24px)" : 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: "100%",
      height: minHeight ? "100%" : "auto",
      minHeight: minHeight ? minHeight - 40 : undefined,
      display: "block",
      objectFit: contain ? "contain" : "cover"
    }
  })), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { ArtifactFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ArtifactFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/BeforeAfter.jsx
try { (() => {
/** A rewrite delivered as a diff with its measurements attached, so the person deciding whether to
 *  take it can see what moved and by how much. The deltas are the argument — without them this is
 *  just someone else's preferred wording. */
function BeforeAfter({
  before,
  after,
  beforeLabel = "Before",
  afterLabel = "After",
  deltas = [],
  actions,
  style,
  ...rest
}) {
  const pane = (label, text, tone) => /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "14px 16px",
      borderRight: tone === "before" ? "1px solid var(--story-line)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: tone === "after" ? "var(--story-accent)" : "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: tone === "before" ? "var(--story-muted)" : "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: tone === "after" ? 560 : 480,
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, text));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      borderBottom: "1px solid var(--story-line)"
    }
  }, pane(beforeLabel, before, "before"), pane(afterLabel, after, "after")), deltas.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(deltas.length, 1) + ",minmax(0,1fr))",
      borderBottom: actions ? "1px solid var(--story-line)" : 0
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        padding: "11px 14px",
        borderRight: i === deltas.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "block",
        marginBottom: 6,
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "baseline",
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 13,
        fontWeight: 560,
        letterSpacing: "-0.03em",
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-accent)",
        fontSize: 17,
        fontWeight: 700,
        letterSpacing: "-0.04em"
      }
    }, d.to)));
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-8)",
      alignItems: "center",
      padding: "12px 16px"
    }
  }, actions));
}
Object.assign(__ds_scope, { BeforeAfter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/BeforeAfter.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CaptureFigure.jsx
try { (() => {
/** A capture of shipped behaviour, in a frame that says what you are looking at.
 *
 *  Video is the only honest way to show an interaction that happens in time, so this plays inline —
 *  muted, looping, playsinline, with controls available. Autoplay is off by default: the accessibility
 *  floor forbids a loop that plays without the reader asking for it. */
function CaptureFigure({
  src,
  poster,
  label,
  caption,
  quote,
  autoPlay = false,
  controls = true,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--ink)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("video", {
    src: src,
    poster: poster,
    muted: true,
    loop: true,
    playsInline: true,
    autoPlay: autoPlay,
    controls: controls,
    preload: "metadata",
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), quote && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "The string"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, quote)), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { CaptureFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CaptureFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CardGrid.jsx
try { (() => {
/** Hairline grid block, on the instrument grammar: an optional silkscreen header naming what the set is
 *  and how many are in it, then cells sized by their contents. Without a label it stays a bare grid —
 *  but if you cannot name the set, ask whether it needed a frame at all. */
function CardGrid({
  columns = 2,
  raised = true,
  label,
  scope,
  children,
  style,
  ...rest
}) {
  const count = React.Children.count(children);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, scope || String(count).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, children));
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
/** A hyphen or dash already in the text is a standard line-break point (Unicode UAX #14) in every
 *  browser, with no special CSS involved, and MOST of the time that is correct: "help-centre" wrapping
 *  to "help-" / "centre" at a narrow column is ordinary, everyday typography — books do it every day —
 *  and gluing it shut would make the word un-searchable and un-copyable (the rendered string would
 *  carry an invisible character a reader can't see or type back). Two narrow cases are genuinely wrong
 *  rather than merely unlucky, because there the two sides of the dash are not independently meaningful
 *  words: a digit range ("2025-2026" splitting to "2025-" / "2026" reads as a typo, not a range) and a
 *  dash-joined token inside a parenthetical statistical label ("p (BH-adj)" splitting to "p (BH-" /
 *  "adj)" garbles the abbreviation). Only those two get a zero-width word joiner (U+2060) glued to the
 *  dash; every other hyphenated compound is left exactly as written. */
const DASH = "\\u002D\\u2010-\\u2015"; // hyphen-minus; hyphen through horizontal bar (covers en/em dash)
const WJ = "⁠"; // word joiner — zero-width, forbids a line break at this point, otherwise invisible
const DIGIT_RANGE_DASH = new RegExp("(\\d)([" + DASH + "])(?=\\d)", "g");
const PAREN_GROUP = /\(([^()]*)\)/g;
const DASH_IN_PAREN = new RegExp("([" + DASH + "])(?=\\S)", "g");
const glueDashes = v => {
  if (typeof v !== "string") return v;
  const rangedNumbers = v.replace(DIGIT_RANGE_DASH, "$1$2" + WJ);
  return rangedNumbers.replace(PAREN_GROUP, (m, inner) => "(" + inner.replace(DASH_IN_PAREN, "$1" + WJ) + ")");
};

/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, glueDashes(c))))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = glueDashes(highlight ? value.value : value);
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DisciplineVenn.jsx
try { (() => {
/** Three disciplines and the work that only exists where they overlap. Drawn as a registration
 *  diagram — hairline circles, no fills, the intersection marked the way the system marks a
 *  registration point — so it reads as a technical figure rather than a slide graphic. */
function DisciplineVenn({
  fields = [],
  intersection,
  caption,
  tone = "story",
  style,
  ...rest
}) {
  const ink = tone === "stage" ? "var(--stage-ink)" : "var(--story-ink)";
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const muted = tone === "stage" ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = tone === "stage" ? "var(--signal-stage)" : "var(--story-accent)";

  /* Three circles on an equilateral arrangement, centred on 150,142. */
  const circles = [{
    cx: 150,
    cy: 96,
    label: fields[0]
  }, {
    cx: 111,
    cy: 164,
    label: fields[1]
  }, {
    cx: 189,
    cy: 164,
    label: fields[2]
  }];
  /* Labels sit outside the circle field so a long discipline name cannot crowd its neighbour. */
  const anchors = [{
    x: 150,
    y: 20,
    align: "middle"
  }, {
    x: -34,
    y: 240,
    align: "start"
  }, {
    x: 334,
    y: 240,
    align: "end"
  }];
  const leaders = [{
    x1: 150,
    y1: 27,
    x2: 150,
    y2: 36
  }, {
    x1: 62,
    y1: 233,
    x2: 84,
    y2: 213
  }, {
    x1: 238,
    y1: 233,
    x2: 216,
    y2: 213
  }];
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      padding: "var(--space-7) 0",
      border: "1px solid " + line,
      background: tone === "stage" ? "transparent" : "var(--story-raised)",
      boxShadow: tone === "stage" ? "none" : "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "-40 8 380 244",
    width: "100%",
    style: {
      maxWidth: 440,
      height: "auto",
      overflow: "visible"
    },
    role: "img",
    "aria-label": fields.join(", ") + " — " + intersection
  }, circles.map(c => /*#__PURE__*/React.createElement("circle", {
    key: c.label,
    cx: c.cx,
    cy: c.cy,
    r: "62",
    fill: "none",
    stroke: line,
    strokeWidth: "1"
  })), leaders.map((l, i) => /*#__PURE__*/React.createElement("line", {
    key: i,
    x1: l.x1,
    y1: l.y1,
    x2: l.x2,
    y2: l.y2,
    stroke: line,
    strokeWidth: "1"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "15",
    fill: "none",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "150",
    y1: "120",
    x2: "150",
    y2: "164",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "128",
    y1: "142",
    x2: "172",
    y2: "142",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "3.5",
    fill: accent
  }), circles.map((c, i) => /*#__PURE__*/React.createElement("text", {
    key: c.label,
    x: anchors[i].x,
    y: anchors[i].y,
    textAnchor: anchors[i].align,
    fill: muted,
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c.label)))), (intersection || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "13px 0 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.4
    }
  }, "Overlap"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, intersection && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, intersection), caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: intersection ? "8px 0 0" : 0,
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, caption))));
}
Object.assign(__ds_scope, { DisciplineVenn });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DisciplineVenn.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FingerprintBars.jsx
try { (() => {
/** A measured voice fingerprint: one row per dimension, the reading drawn against the band a
 *  surface is allowed to sit in. The band is the point — a fingerprint is a tolerance, not a
 *  score, and a reading outside it is the only thing that counts as a miss. */
function FingerprintBars({
  dimensions = [],
  scale = 100,
  showBand = true,
  style,
  ...rest
}) {
  const pct = n => Math.max(0, Math.min(100, n / scale * 100));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, dimensions.map((d, i) => {
    const out = showBand && d.band && (d.value < d.band[0] || d.value > d.band[1]);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        display: "grid",
        gridTemplateColumns: "minmax(96px,0.9fr) minmax(0,2.4fr) 42px",
        gap: 14,
        alignItems: "center",
        padding: "9px 16px",
        borderBottom: i === dimensions.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700,
        lineHeight: 1.3
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        position: "relative",
        height: 9,
        display: "block",
        borderBottom: "1px solid var(--story-line)"
      }
    }, showBand && d.band && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 0,
        bottom: 0,
        left: pct(d.band[0]) + "%",
        width: pct(d.band[1]) - pct(d.band[0]) + "%",
        background: "var(--story-band)",
        borderLeft: "1px solid var(--story-line)",
        borderRight: "1px solid var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 2,
        bottom: 2,
        left: 0,
        width: pct(d.value) + "%",
        background: out ? "var(--story-accent)" : "var(--story-ink)"
      }
    })), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 14,
        fontWeight: 700,
        letterSpacing: "-0.04em",
        textAlign: "right",
        fontVariantNumeric: "tabular-nums"
      }
    }, d.value));
  }));
}
Object.assign(__ds_scope, { FingerprintBars });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FingerprintBars.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 14px",
      borderRight: "1px solid " + line,
      overflowWrap: "break-word"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35,
      overflowWrap: "break-word"
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content
 *  column. No height floor — a short cell is short. The padding is an instrument's, not a page margin's. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter-tight) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "var(--card-pad) 12px var(--card-pad) 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--card-pad) var(--card-pad-wide)"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 5,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 9,
      padding: "8px 10px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "6px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Label above value, so a cell is only as wide as the wider of the two and a row of four fits without
 *  stretching. Side-by-side reads fine until a value is a word rather than a number, at which point the
 *  cells go ragged and the label and the number end up at opposite ends of a void. This is the same
 *  stacking `VoiceProfile` uses, which is the system's answer for a set of small readings. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: "0 1 auto",
      padding: "12px 18px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Outcome.jsx
try { (() => {
/** Amber label over a short results list or paragraph. Sits at the end of a stage copy column. */
function Outcome({
  label = "Results",
  items,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(430px,88%)",
      marginTop: 18,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--signal-stage)",
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      fontWeight: 700
    }
  }, label), items && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: "5px 0 0",
      padding: 0,
      listStyle: "none",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, item))), children && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, children));
}
Object.assign(__ds_scope, { Outcome });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Outcome.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Pipeline.jsx
try { (() => {
/** Ordered process rows. Step number in the gutter, the rule on one line — compressed, not spaced out. */
function Pipeline({
  steps = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      listStyle: "none",
      ...style
    },
    ...rest
  }, steps.map((step, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === steps.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "14px 12px 14px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, step.step || String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "13px 18px",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(15px,1.15vw,17px)",
      fontWeight: 560,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, step.label))));
}
Object.assign(__ds_scope, { Pipeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Pipeline.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      /* Sized by the words, not by a column. A quote that wraps short leaves no dead ground. */
      width: "fit-content",
      maxWidth: 560,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResearchGrid.jsx
try { (() => {
/** Published sources behind a measurement, one row each: who, when, and the thing it makes computable.
 *  A citation row is the difference between an instrument and an opinion, so the source is never
 *  paraphrased away — it stays attributable. */
function ResearchGrid({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    style: {
      padding: "15px 18px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, item.source), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, item.year)), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "9px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.12,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, item.gives), item.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.note))));
}
Object.assign(__ds_scope, { ResearchGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResearchGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResumeEntry.jsx
try { (() => {
/** One record in a résumé timeline: dates in the label gutter, org and role, then what I did there. */
function ResumeEntry({
  when,
  org,
  role,
  bullets = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: "clamp(16px,2vw,28px)",
      padding: "var(--space-7) 0",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      paddingTop: 3,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.45
    }
  }, when), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--display-s-tracking)"
    }
  }, org), role && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, role), bullets.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 11,
      margin: "var(--space-5) 0 0",
      padding: 0,
      listStyle: "none"
    }
  }, bullets.map((b, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "14px minmax(0,1fr)",
      gap: 10,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em",
      textWrap: "pretty"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      marginTop: "0.62em",
      width: 7,
      height: 1,
      background: "var(--story-accent)"
    }
  }), b)))));
}
Object.assign(__ds_scope, { ResumeEntry });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResumeEntry.jsx", error: String((e && e.message) || e) }); }

// components/evidence/RuleSplit.jsx
try { (() => {
/** Two-column sort of a guidance set: what a model has to judge, and what code can check.
 *  The whole argument for a measurement layer fits in one of these — a reader can see the
 *  proportion before reading a single row. */
function RuleSplit({
  modelLabel = "Model judgment",
  codeLabel = "Checkable in code",
  rules = [],
  note,
  style,
  ...rest
}) {
  const model = rules.filter(r => r.kind === "model");
  const code = rules.filter(r => r.kind === "code");
  const column = (label, items, accent) => /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      borderRight: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "10px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.rule))));
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      border: "1px solid var(--story-line)",
      borderRight: 0,
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, column(modelLabel, model, "var(--story-muted)"), column(codeLabel, code, "var(--story-accent)")), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, note));
}
Object.assign(__ds_scope, { RuleSplit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/RuleSplit.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
/** Ruled inventory of inputs, sized by what it names.
 *
 *  Each entry is as wide as its own number-and-name pair and wraps when it runs out of room. An entry
 *  does not stretch to half the page and then sit alone at the left edge of it. */
function SourceMap({
  items = [],
  label = "Inputs",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 24,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "flex",
      gap: 12,
      alignItems: "baseline",
      flex: "0 1 auto",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal",
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item)))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/TenGrid.jsx
try { (() => {
/** Ten hairline cells; filled cells read as passes. A dimension meter, not a progress bar. */
function TenGrid({
  total = 10,
  pass = 0,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + total + ",1fr)",
      gap: 4,
      marginTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, Array.from({
    length: total
  }).map((_, i) => /*#__PURE__*/React.createElement("i", {
    key: i,
    style: {
      height: 10,
      border: "1px solid " + (i < pass ? "var(--story-accent)" : "var(--story-muted)"),
      background: i < pass ? "var(--story-accent)" : "transparent"
    }
  })));
}
Object.assign(__ds_scope, { TenGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/TenGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Toolchain.jsx
try { (() => {
/** What a system was actually built out of, named and linked so a reader can go and check.
 *
 *  The engineering counterpart to `ResearchGrid`: same anatomy, same register. Deliberately no logos —
 *  a row of third-party brand marks would be the one place this system's ink-and-hairline language
 *  breaks, and it is the oldest tell in portfolio design. A name, a licence, and what it actually
 *  provides is more credible than a badge, because it can be verified. */
function Toolchain({
  items = [],
  label = "Built with",
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: item.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(140px,0.9fr) minmax(0,2fr) auto",
      gap: "clamp(12px,2vw,26px)",
      alignItems: "baseline",
      padding: "12px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, item.href ? /*#__PURE__*/React.createElement("a", {
    href: item.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em",
      textDecoration: "none",
      borderBottom: "1px solid currentColor",
      width: "fit-content",
      paddingBottom: 1
    }
  }, item.name) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em"
    }
  }, item.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.provides), item.meta && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      textAlign: "right"
    }
  }, item.meta)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 640,
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45
    }
  }, note));
}
Object.assign(__ds_scope, { Toolchain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Toolchain.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceProfile.jsx
try { (() => {
/** The target a surface is written to, published so a team can argue with it: each dimension's
 *  setting and the one sentence saying why it sits there. A profile without its reasoning is a
 *  number nobody will honour. */
function VoiceProfile({
  product,
  surface,
  dimensions = [],
  notes = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Voice profile"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, [product, surface].filter(Boolean).join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit,minmax(128px,1fr))",
      borderBottom: notes.length ? "1px solid var(--story-line)" : 0
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      padding: "13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 8,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      height: 3,
      display: "block",
      marginTop: 9,
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: d.value + "%",
      background: "var(--story-accent)"
    }
  }))))), notes.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === notes.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, n.name), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, n.why)))));
}
Object.assign(__ds_scope, { VoiceProfile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceProfile.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceScoreRing.jsx
try { (() => {
/** How a reading reaches the person who has to act on it: one alignment figure, the dimensions
 *  underneath it, and a verdict in words. The arc draws itself on mount rather than fading in —
 *  a measurement should look like it was taken, not like it was already there. */
function VoiceScoreRing({
  score = 0,
  outOf = 100,
  label = "Voice alignment",
  verdict,
  scope = [],
  dimensions = [],
  size = 148,
  style,
  ...rest
}) {
  const [drawn, setDrawn] = React.useState(false);
  React.useEffect(() => {
    /* Two frames, so the browser paints the zero state before the arc animates to its value.
       Both ids are tracked — cancelling only the outer one leaves the inner frame live. */
    let inner;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setDrawn(true));
    });
    return () => {
      cancelAnimationFrame(outer);
      if (inner) cancelAnimationFrame(inner);
    };
  }, []);
  const r = (size - 18) / 2;
  const c = 2 * Math.PI * r;
  const filled = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope.length > 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, scope.join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(18px,2.4vw,32px)",
      alignItems: "center",
      padding: "18px 16px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: "7",
    strokeLinecap: "butt",
    strokeDasharray: c,
    strokeDashoffset: drawn ? c * (1 - filled) : c,
    style: {
      transition: "stroke-dashoffset var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "of ", outOf))), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, verdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 13px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)"
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(80px,1fr) minmax(0,1.6fr) 34px",
      gap: 12,
      alignItems: "center",
      padding: "7px 0",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 5,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: (drawn ? d.value : 0) + "%",
      background: "var(--story-ink)",
      transition: "width var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value)))))));
}
Object.assign(__ds_scope, { VoiceScoreRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceScoreRing.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Eyebrow.jsx
try { (() => {
const TONES = {
  muted: "var(--story-muted, var(--muted-ink))",
  signal: "var(--signal)",
  action: "var(--signal-stage)",
  stage: "var(--stage-muted)"
};

/** Small mono label that opens a scene, a section, or a card. */
function Eyebrow({
  tone = "muted",
  size = "eyebrow",
  as = "p",
  children,
  style,
  ...rest
}) {
  const sizes = {
    eyebrow: {
      fontSize: "var(--eyebrow-size)",
      fontWeight: "var(--eyebrow-weight)",
      letterSpacing: "var(--eyebrow-tracking)",
      fontFamily: "var(--font-sans)"
    },
    meta: {
      fontSize: "var(--meta-size)",
      fontWeight: "var(--meta-weight)",
      letterSpacing: "-0.01em",
      fontFamily: "var(--font-mono)"
    },
    micro: {
      fontSize: "var(--micro-size)",
      fontWeight: "var(--micro-weight)",
      letterSpacing: "0",
      fontFamily: "var(--font-mono)"
    }
  };
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      margin: 0,
      lineHeight: "var(--eyebrow-leading)",
      color: TONES[tone],
      ...sizes[size],
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/LineArrow.jsx
try { (() => {
/** The drawn arrow glyph: 1px rule plus a rotated corner. Inherits currentColor. */
function LineArrow({
  reverse = false,
  width = 34,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      display: "inline-block",
      flex: "0 0 " + width + "px",
      width,
      height: 12,
      transform: reverse ? "rotate(180deg)" : undefined,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 5,
      right: 0,
      left: 0,
      height: 1,
      background: "currentColor"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 2,
      right: 0,
      width: 7,
      height: 7,
      borderTop: "1px solid currentColor",
      borderRight: "1px solid currentColor",
      transform: "rotate(45deg)"
    }
  }));
}
Object.assign(__ds_scope, { LineArrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/LineArrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/ProvenanceNote.jsx
try { (() => {
/** Hairline-topped note that states what an artifact is and is not. */
function ProvenanceNote({
  label = "Provenance",
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      marginTop: "var(--space-6)",
      paddingTop: "var(--space-4)",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 700,
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.5,
      letterSpacing: "-0.015em"
    }
  }, children));
}
Object.assign(__ds_scope, { ProvenanceNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/ProvenanceNote.jsx", error: String((e && e.message) || e) }); }

// components/foundation/RegistrationMark.jsx
try { (() => {
/** Corner crosshair with a gold centre — the print-registration device used on every stage. */
function RegistrationMark({
  size = 22,
  color = "var(--stage-ink)",
  dot = "var(--signal-stage)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      top: 22,
      right: 22,
      width: size,
      height: size,
      opacity: 0.65,
      pointerEvents: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: size / 2 - 1,
      right: 0,
      left: 0,
      height: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: size / 2 - 1,
      width: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 4,
      border: "1px solid " + color,
      borderRadius: "var(--radius-round)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 8,
      borderRadius: "var(--radius-round)",
      background: dot
    }
  }));
}
Object.assign(__ds_scope, { RegistrationMark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/RegistrationMark.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Tag.jsx
try { (() => {
/** Hairline capability tag. Square corners, no fill. */
function Tag({
  on = "stage",
  children,
  style,
  ...rest
}) {
  const border = on === "stage" ? "var(--stage-line)" : "var(--soft-line)";
  return /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "9px 11px",
      border: "1px solid " + border,
      fontFamily: "var(--font-sans)",
      fontSize: 10,
      letterSpacing: "-0.01em",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Tag.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum.
 *  `as` sets the element, so a row inside an ordered list can be a real `li` rather than a div. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  as = "div",
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const Tag = as;
  if (!gutter) {
    return /*#__PURE__*/React.createElement(Tag, {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      listStyle: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel, PanelRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/layout/PaperFrame.jsx
try { (() => {
/** The bounded document: a paper card on a darker desk, hairline border, hard offset shadow, grain overlay. */
function PaperFrame({
  children,
  grain = true,
  columns = "var(--rail-width) minmax(0,1fr)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100%",
      background: "var(--surface-desk)",
      fontFamily: "var(--font-body)",
      color: "var(--ink)"
    }
  }, grain && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      zIndex: 5000,
      inset: 0,
      pointerEvents: "none",
      opacity: "var(--texture-grain-opacity)",
      mixBlendMode: "multiply",
      backgroundImage: "var(--texture-grain)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "min(calc(100% - 24px)," + "var(--frame-max))",
      height: "calc(100svh - 24px)",
      minHeight: 700,
      display: "grid",
      gridTemplateColumns: columns,
      margin: "var(--frame-inset) auto",
      overflow: "hidden",
      border: "1px solid var(--ink)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-4)",
      ...style
    },
    ...rest
  }, children));
}
Object.assign(__ds_scope, { PaperFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/PaperFrame.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stage.jsx
try { (() => {
/** The pine authored stage: gradient field, clipped, with an optional registration mark. */
function Stage({
  children,
  variant = "field",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    style: {
      position: "relative",
      zIndex: 1,
      minWidth: 0,
      minHeight: 0,
      overflow: "hidden",
      isolation: "isolate",
      background: variant === "section" ? "var(--stage-field-section)" : "var(--stage-field)",
      color: "var(--stage-ink)",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Stage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stage.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title &&
  /*#__PURE__*/
  /* data-doc-heading + tabIndex={-1}: the one place per document the app moves focus to
     after a destination change (see app.jsx). Focusable without joining the tab order;
     the ring itself is drawn by :focus-visible in tokens/interaction.css, so a mouse
     click never shows one and a keyboard move always does. */
  React.createElement("h1", {
    "data-doc-heading": true,
    tabIndex: -1,
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      transform: direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)",
      willChange: "transform",
      animation: plate,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }

// components/layout/StorySection.jsx
try { (() => {
/** A tone-scoped chapter of a case document: index, kicker, title, intro, then content. */
function StorySection({
  tone = "paper",
  index,
  chapterLabel,
  kicker,
  title,
  intro,
  children,
  id,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      display: "grid",
      gridTemplateColumns: "70px minmax(0,1fr)",
      gap: "clamp(28px,5vw,82px)",
      padding: "var(--section-pad) var(--gutter)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700
    }
  }, chapterLabel && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, chapterLabel), index && /*#__PURE__*/React.createElement("b", {
    style: {
      display: "block",
      color: "var(--story-accent)"
    }
  }, index)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 980
    }
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 16px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-m-size)",
      fontWeight: "var(--display-m-weight)",
      lineHeight: "var(--display-m-leading)",
      letterSpacing: "var(--display-m-tracking)",
      textWrap: "balance"
    }
  }, title), intro && /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 760,
      marginTop: 20,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      minWidth: 0,
      gridColumn: "1 / -1",
      color: "var(--story-ink)"
    }
  }, children));
}
Object.assign(__ds_scope, { StorySection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StorySection.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ChapterNav.jsx
try { (() => {
/** Sticky chapter map. The chapter in view inverts to an ink chip. */
function ChapterNav({
  label = "Case map",
  chapters = [],
  active,
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    style: {
      position: "sticky",
      zIndex: 45,
      top: 0,
      minHeight: "var(--chapter-nav-height)",
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(22px,3vw,48px)",
      alignItems: "center",
      padding: "var(--space-2) var(--gutter-tight)",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      backdropFilter: "blur(14px)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 760,
      whiteSpace: "nowrap"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(chapters.length, 1) + ",minmax(74px,1fr))",
      gap: 4
    }
  }, chapters.map(c => {
    const on = c.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      type: "button",
      className: "ds-tab ds-tab-chapter",
      onClick: () => onSelect && onSelect(c.id),
      "aria-current": on ? "true" : undefined,
      style: {
        position: "relative",
        minHeight: 34,
        padding: "0 8px",
        border: 0,
        background: on ? "var(--ink)" : "transparent",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        cursor: "pointer",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, c.label);
  })));
}
Object.assign(__ds_scope, { ChapterNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ChapterNav.jsx", error: String((e && e.message) || e) }); }




// components/case/CaseSection.jsx
try { (() => {
/** A single-baseline case section: a small numeral beside its name, then content at native width —
 *  never the tone-scoped banner `StorySection` uses. The header is one line, not a persistent
 *  side-gutter running the height of the section, which is most of the density this buys back:
 *  a chapter costs a baseline, not a margin column. Content scrolls inside the section rather than
 *  the page if a nested block ever runs wider than the frame, so one over-eager grid can never turn
 *  into page-level horizontal scroll. */
function CaseSection({
  tone = "paper",
  index,
  title,
  id,
  scrollMarginTop = 56,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      scrollMarginTop,
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      padding: "var(--section-pad-tight) var(--gutter)",
      ...style
    },
    ...rest
  }, (index || title) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: 12,
      marginBottom: "var(--space-7)"
    }
  }, index && /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "0 0 auto",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, index), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: 650,
      lineHeight: 1.1,
      letterSpacing: "var(--headline-tracking)"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "100%",
      overflowX: "auto"
    }
  }, children));
}
Object.assign(__ds_scope, { CaseSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseSection.jsx", error: String((e && e.message) || e) }); }

// components/case/StoryBeat.jsx
try { (() => {
/** A chapter's opening beat: a micro-label kicker, a headline claim, and a lede paragraph — the
 *  argument a `CaseSection` exists to make, stated before its evidence. Where the bakeoff's
 *  Continuum entry shares one beat label across four short paragraphs, a case document's chapters
 *  are each already a considered, separately titled beat, so this renders one beat per section
 *  rather than several stacked under a shared heading — the micro-label-over-statement grammar
 *  survives the translation; the stacking count adapts to what the chapter actually is. */
function StoryBeat({
  kicker,
  title,
  intro,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(100%, 780px)",
      ...style
    },
    ...rest
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 14px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      letterSpacing: "0.01em"
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: "var(--display-s-weight)",
      lineHeight: "var(--display-s-leading)",
      letterSpacing: "var(--display-s-tracking)",
      textWrap: "balance",
      color: "var(--story-ink)"
    }
  }, title), intro && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 680,
      margin: title ? "16px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro), children);
}
Object.assign(__ds_scope, { StoryBeat });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/StoryBeat.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseQuote.jsx
try { (() => {
/** The pull-quote as a full-width interlude: hairline top and bottom, nothing else — no ground fill,
 *  no shadow, no left bar. It is the one moment allowed to breathe precisely because everything
 *  else on the page is compact. Distinct from `PullQuote`, which is a boxed ink specimen for a
 *  quote sitting beside other evidence; this one stands alone in the reading column. */
function CaseQuote({
  children,
  attribution,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: "clamp(30px,4vw,46px) 0",
      padding: "clamp(24px,3.4vw,38px) 0",
      borderTop: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      textAlign: "center",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: "0 auto",
      maxWidth: "52ch",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      color: "var(--story-ink)",
      textWrap: "balance"
    }
  }, children), attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      marginTop: 14,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      letterSpacing: "0.01em"
    }
  }, attribution));
}
Object.assign(__ds_scope, { CaseQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseQuote.jsx", error: String((e && e.message) || e) }); }

// components/case/MetricStrip.jsx
try { (() => {
/** The Overview metric row: unboxed, native width, a single ink rule above it and a hairline
 *  between cells — no ground, no shadow, no card padding. `Ledger` carries the same value-above-
 *  label instrument grammar but is a raised, labelled panel; this is deliberately the unframed
 *  opposite, because entry A's own density doctrine keeps the argument-first strip flush with the
 *  page rather than boxed like the evidence beneath it. Renders only the cells it is given — pass
 *  only entries that already carry a real number; padding a slot with an invented figure is exactly
 *  what this component exists to refuse. */
function MetricStrip({
  items = [],
  style,
  ...rest
}) {
  const count = Math.max(items.length, 1);
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "metric-strip",
    "data-count": count,
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + count + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-ink)",
      paddingTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      minWidth: 0,
      padding: i === 0 ? "0 clamp(16px,2vw,26px) 0 0" : "0 clamp(16px,2vw,26px)",
      borderLeft: i === 0 ? "none" : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginTop: 8,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, item.label))));
}
Object.assign(__ds_scope, { MetricStrip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/MetricStrip.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseNav.jsx
try { (() => {
/** The sticky, scroll-spied jump nav from the bakeoff's entry A, translated: a horizontally
 *  scrollable row of anchors (so a long stop list degrades to a swipe on a phone rather than
 *  crushing every label), an `IntersectionObserver` keeping the in-view stop honest as the reader
 *  scrolls, and a plain `href="#id"` under every stop so navigation still works with the observer
 *  entirely absent. The observer's `root` is `scrollRoot.current` rather than the viewport, because
 *  this product's document scrolls inside a nested pane, not the window. The active stop inverts to
 *  an ink chip — this system's one selection language — rather than entry A's vermillion underline;
 *  the underline survives as the hover mark, reusing the same `.ds-tab` rule every other tab in the
 *  system already draws with. */
function CaseNav({
  label = "Case map",
  stops = [],
  scrollRoot,
  style,
  ...rest
}) {
  const [active, setActive] = React.useState(stops[0] ? stops[0].id : null);
  React.useEffect(() => {
    const root = scrollRoot && scrollRoot.current;
    if (!root || typeof IntersectionObserver === "undefined" || !stops.length) return undefined;
    const targets = stops.map(s => document.getElementById(s.id)).filter(Boolean);
    if (!targets.length) return undefined;
    const io = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) setActive(entry.target.id);
      });
    }, {
      root,
      rootMargin: "-45% 0px -50% 0px",
      threshold: 0
    });
    targets.forEach(t => io.observe(t));
    return () => io.disconnect();
  }, [scrollRoot, stops]);
  const jump = id => event => {
    const target = document.getElementById(id);
    const root = scrollRoot && scrollRoot.current;
    if (!target || !root) return; // no JS home for the jump — the href="#id" still does the job
    event.preventDefault();
    const reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    target.scrollIntoView({
      behavior: reduce ? "auto" : "smooth",
      block: "start"
    });
  };
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "case-nav",
    style: {
      position: "sticky",
      top: 0,
      zIndex: 40,
      display: "flex",
      alignItems: "stretch",
      overflowX: "auto",
      scrollbarWidth: "none",
      WebkitOverflowScrolling: "touch",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      backdropFilter: "blur(14px)",
      boxShadow: "var(--shadow-sticky)",
      ...style
    },
    ...rest
  }, stops.map(s => {
    const on = s.id === active;
    return /*#__PURE__*/React.createElement("a", {
      key: s.id,
      href: "#" + s.id,
      onClick: jump(s.id),
      "aria-current": on ? "true" : undefined,
      className: "ds-tab ds-tab-chapter",
      style: {
        flex: "0 0 auto",
        display: "flex",
        alignItems: "center",
        minHeight: 44,
        padding: "0 clamp(13px,1.7vw,20px)",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        background: on ? "var(--ink)" : "transparent",
        fontFamily: "var(--font-sans)",
        fontSize: 12,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        textDecoration: "none",
        whiteSpace: "nowrap",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, s.label);
  }));
}
Object.assign(__ds_scope, { CaseNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseNav.jsx", error: String((e && e.message) || e) }); }


// components/layout/PaperFrame.jsx
try { (() => {
/** The bounded document: a paper card on a darker desk, hairline border, hard offset shadow, grain overlay. */
function PaperFrame({
  children,
  grain = true,
  columns = "var(--rail-width) minmax(0,1fr)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100%",
      background: "var(--surface-desk)",
      fontFamily: "var(--font-body)",
      color: "var(--ink)"
    }
  }, grain && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      zIndex: 5000,
      inset: 0,
      pointerEvents: "none",
      opacity: "var(--texture-grain-opacity)",
      mixBlendMode: "multiply",
      backgroundImage: "var(--texture-grain)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    "data-ds": "frame",
    style: {
      width: "min(calc(100% - 24px)," + "var(--frame-max))",
      height: "calc(100svh - 24px)",
      minHeight: 700,
      display: "grid",
      gridTemplateColumns: columns,
      margin: "var(--frame-inset) auto",
      overflow: "hidden",
      border: "1px solid var(--ink)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-4)",
      ...style
    },
    ...rest
  }, children));
}
Object.assign(__ds_scope, { PaperFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/PaperFrame.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stage.jsx
try { (() => {
/** The pine authored stage: gradient field, clipped, with an optional registration mark. */
function Stage({
  children,
  variant = "field",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    "data-ds": "stage",
    style: {
      position: "relative",
      zIndex: 1,
      minWidth: 0,
      minHeight: 0,
      overflow: "hidden",
      isolation: "isolate",
      background: variant === "section" ? "var(--stage-field-section)" : "var(--stage-field)",
      color: "var(--stage-ink)",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Stage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stage.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "stage-copy",
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title &&
  /*#__PURE__*/
  /* data-doc-heading + tabIndex={-1}: the one place per document the app moves focus to
     after a destination change (see app.jsx). Focusable without joining the tab order;
     the ring itself is drawn by :focus-visible in tokens/interaction.css, so a mouse
     click never shows one and a keyboard move always does. */
  React.createElement("h1", {
    "data-doc-heading": true,
    tabIndex: -1,
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    "data-ds": "stage-curtain",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      transform: direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)",
      willChange: "transform",
      animation: plate,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }


// components/navigation/ChapterNav.jsx
try { (() => {
/** Sticky chapter map. The chapter in view inverts to an ink chip. */
function ChapterNav({
  label = "Case map",
  chapters = [],
  active,
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "chapter-nav",
    style: {
      position: "sticky",
      zIndex: 45,
      top: 0,
      minHeight: "var(--chapter-nav-height)",
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(22px,3vw,48px)",
      alignItems: "center",
      padding: "var(--space-2) var(--gutter-tight)",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      backdropFilter: "blur(14px)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 760,
      whiteSpace: "nowrap"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    "data-ds": "chapter-nav-track",
    style: {
      minWidth: 0,
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(chapters.length, 1) + ",minmax(74px,1fr))",
      gap: 4
    }
  }, chapters.map(c => {
    const on = c.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      type: "button",
      className: "ds-tab ds-tab-chapter",
      onClick: () => onSelect && onSelect(c.id),
      "aria-current": on ? "true" : undefined,
      style: {
        position: "relative",
        minHeight: 34,
        padding: "0 8px",
        border: 0,
        background: on ? "var(--ink)" : "transparent",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        cursor: "pointer",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, c.label);
  })));
}
Object.assign(__ds_scope, { ChapterNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ChapterNav.jsx", error: String((e && e.message) || e) }); }



// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "facts-panel",
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseNav.jsx
try { (() => {
/** The sticky, scroll-spied jump nav from the bakeoff's entry A, translated: a horizontally
 *  scrollable row of anchors (so a long stop list degrades to a swipe on a phone rather than
 *  crushing every label), an `IntersectionObserver` keeping the in-view stop honest as the reader
 *  scrolls, and a plain `href="#id"` under every stop so navigation still works with the observer
 *  entirely absent. The observer's `root` is `scrollRoot.current` rather than the viewport, because
 *  this product's document scrolls inside a nested pane, not the window. The active stop inverts to
 *  an ink chip — this system's one selection language — rather than entry A's vermillion underline;
 *  the underline survives as the hover mark, reusing the same `.ds-tab` rule every other tab in the
 *  system already draws with. */
function CaseNav({
  label = "Case map",
  stops = [],
  scrollRoot,
  style,
  ...rest
}) {
  const [active, setActive] = React.useState(stops[0] ? stops[0].id : null);
  React.useEffect(() => {
    const node = scrollRoot && scrollRoot.current;
    if (!node || typeof IntersectionObserver === "undefined" || !stops.length) return undefined;
    /* Below the shell breakpoint this node stops being a scroll container at all — the page
       scrolls instead (tokens/shell.css). A `root` that cannot scroll its own content makes
       IntersectionObserver's geometry meaningless, so fall back to the viewport (`root: null`)
       whenever the node's content no longer overflows it, rather than branching on width. */
    const root = node.scrollHeight > node.clientHeight + 1 ? node : null;
    const targets = stops.map(s => document.getElementById(s.id)).filter(Boolean);
    if (!targets.length) return undefined;
    const io = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) setActive(entry.target.id);
      });
    }, {
      root,
      rootMargin: "-45% 0px -50% 0px",
      threshold: 0
    });
    targets.forEach(t => io.observe(t));
    return () => io.disconnect();
  }, [scrollRoot, stops]);
  const jump = id => event => {
    const target = document.getElementById(id);
    const root = scrollRoot && scrollRoot.current;
    if (!target || !root) return; // no JS home for the jump — the href="#id" still does the job
    event.preventDefault();
    const reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    target.scrollIntoView({
      behavior: reduce ? "auto" : "smooth",
      block: "start"
    });
  };
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "case-nav",
    style: {
      position: "sticky",
      top: 0,
      zIndex: 40,
      display: "flex",
      alignItems: "stretch",
      overflowX: "auto",
      scrollbarWidth: "none",
      WebkitOverflowScrolling: "touch",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      backdropFilter: "blur(14px)",
      boxShadow: "var(--shadow-sticky)",
      ...style
    },
    ...rest
  }, stops.map(s => {
    const on = s.id === active;
    return /*#__PURE__*/React.createElement("a", {
      key: s.id,
      href: "#" + s.id,
      onClick: jump(s.id),
      "aria-current": on ? "true" : undefined,
      className: "ds-tab ds-tab-chapter",
      style: {
        flex: "0 0 auto",
        display: "flex",
        alignItems: "center",
        minHeight: 44,
        padding: "0 clamp(13px,1.7vw,20px)",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        background: on ? "var(--ink)" : "transparent",
        fontFamily: "var(--font-sans)",
        fontSize: 12,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        textDecoration: "none",
        whiteSpace: "nowrap",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, s.label);
  }));
}
Object.assign(__ds_scope, { CaseNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseNav.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    "data-ds": "stage-curtain",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      /* The off-screen resting transform only matters while a transition is actually playing —
         `visibility: hidden` already fully hides the plate at idle, so idle can rest at
         `none` instead of parking a real, positioned box outside the viewport. That box used to
         be silently clipped by the desktop reader's `overflow: hidden`; once the phone reader
         gives that up (tokens/shell.css, needed to keep `position: sticky` tracking the real
         page scroll), the same idle box was inflating the document's own horizontal scroll
         width by exactly its own overhang. The cover/reveal keyframes define their own `from`
         transform the instant they start, so dropping the idle-only offset changes nothing
         about how a transition actually looks. */
      transform: active ? direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)" : "none",
      willChange: "transform",
      animation: plate,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }




// components/case/CaseMeta.jsx
try { (() => {
const HAIRLINE = "1px solid color-mix(in srgb, var(--story-ink) 16%, transparent)";
const CELL = {
  color: "var(--story-muted)",
  fontFamily: "var(--font-sans)",
  fontSize: 11,
  lineHeight: 1.45
};

/** The case's metadata: Role, Team, Timeline, Surface, sitting directly below the cover as the
 *  scroll's first element. Caption-size text (CLAUDE.md's "Three text sizes" law makes this the
 *  one legitimate home for the third size inside a case scroll) — sentence case, muted ink on
 *  the dark ground, a hairline between rows and nothing else. No box, no mono, no colored label:
 *  quiet data, not chrome. Every value is sourced straight from kit-data; a project with nothing
 *  real to say for a field (Word Math has no team — it's solo research) renders fewer rows, never
 *  an invented one. Distinct from `FactsPanel`, which this replaces inside a case scroll:
 *  `FactsPanel` is a raised bright-paper masthead with a 9px mono label and a headline-size value;
 *  this is unframed, caption-size throughout, and built for the dark ground every case now shares. */
function CaseMeta({
  role,
  team,
  timeline,
  surface,
  style,
  ...rest
}) {
  const rows = [{
    label: "Role",
    value: role
  }, {
    label: "Team",
    value: team
  }, {
    label: "Timeline",
    value: timeline
  }, {
    label: "Surface",
    value: surface
  }].filter(row => row.value);
  if (!rows.length) return null;
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "case-meta",
    style: {
      ...style
    },
    ...rest
  }, rows.map((row, i) => /*#__PURE__*/React.createElement("div", {
    key: row.label,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: 12,
      padding: "9px 0",
      borderTop: i === 0 ? "none" : HAIRLINE
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: CELL
  }, row.label), /*#__PURE__*/React.createElement("span", {
    style: {
      ...CELL,
      color: "var(--story-ink)"
    }
  }, row.value))));
}
Object.assign(__ds_scope, { CaseMeta });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseMeta.jsx", error: String((e && e.message) || e) }); }

// components/actions/ScrollCue.jsx
try { (() => {
/** Circled arrow plus a short promise, telling the reader the document continues. */
function ScrollCue({
  label = "The case study continues",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "center",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", null, label), /*#__PURE__*/React.createElement("i", {
    style: {
      width: 26,
      height: 26,
      display: "grid",
      placeItems: "center",
      border: "1px solid var(--stage-line)",
      borderRadius: "var(--radius-round)",
      color: "var(--signal-stage)",
      animation: "scroll-nudge var(--dur-nudge) ease-in-out infinite",
      fontStyle: "normal"
    }
  }, "↓"));
}
Object.assign(__ds_scope, { ScrollCue });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ScrollCue.jsx", error: String((e && e.message) || e) }); }

// components/actions/StageAction.jsx
try { (() => {
const ACCENTS = {
  action: "var(--signal-stage)",
  signal: "var(--signal)",
  ink: "var(--ink)",
  stage: "var(--stage-ink)"
};

/** The system's only button: a text label on a 1px underline with a drawn arrow. */
function StageAction({
  accent = "action",
  href,
  onClick,
  reverse = false,
  children,
  style,
  ...rest
}) {
  const color = ACCENTS[accent];
  const shared = {
    width: "fit-content",
    display: "inline-flex",
    alignItems: "center",
    gap: 17,
    padding: "0 0 7px",
    border: 0,
    borderBottom: "1px solid " + color,
    background: "transparent",
    color,
    fontFamily: "var(--font-sans)",
    fontSize: 13,
    fontWeight: 700,
    letterSpacing: "-0.02em",
    whiteSpace: "nowrap",
    cursor: "pointer",
    ...style
  };
  const content = reverse ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, {
    reverse: true
  }), children) : /*#__PURE__*/React.createElement(React.Fragment, null, children, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, null));
  if (href) return /*#__PURE__*/React.createElement("a", {
    href: href,
    style: shared,
    ...rest
  }, content);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    style: shared,
    ...rest
  }, content);
}
Object.assign(__ds_scope, { StageAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/StageAction.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseMeta.jsx
try { (() => {
const HAIRLINE = "1px solid color-mix(in srgb, var(--story-ink) 16%, transparent)";
const CELL = {
  color: "var(--story-muted)",
  fontFamily: "var(--font-sans)",
  fontSize: 11,
  lineHeight: 1.45
};

/** The case's metadata: Role, Team, Timeline, Surface, sitting directly below the cover as the
 *  scroll's first element. Caption-size text (CLAUDE.md's "Three text sizes" law makes this the
 *  one legitimate home for the third size inside a case scroll) — sentence case, muted ink on
 *  the dark ground, a hairline between rows and nothing else. No box, no mono, no colored label:
 *  quiet data, not chrome. Every value is sourced straight from kit-data; a project with nothing
 *  real to say for a field (Word Math has no team — it's solo research) renders fewer rows, never
 *  an invented one. Distinct from `FactsPanel`, which this replaces inside a case scroll:
 *  `FactsPanel` is a raised bright-paper masthead with a 9px mono label and a headline-size value;
 *  this is unframed, caption-size throughout, and built for the dark ground every case now shares. */
function CaseMeta({
  role,
  team,
  timeline,
  surface,
  style,
  ...rest
}) {
  const rows = [{
    label: "Role",
    value: role
  }, {
    label: "Team",
    value: team
  }, {
    label: "Timeline",
    value: timeline
  }, {
    label: "Surface",
    value: surface
  }].filter(row => row.value);
  if (!rows.length) return null;
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "case-meta",
    style: {
      ...style
    },
    ...rest
  }, rows.map((row, i) => /*#__PURE__*/React.createElement("div", {
    key: row.label,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: 12,
      padding: "9px 0",
      borderTop: i === 0 ? "none" : HAIRLINE
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: CELL
  }, row.label), /*#__PURE__*/React.createElement("span", {
    style: {
      ...CELL,
      color: "var(--story-ink)"
    }
  }, row.value))));
}
Object.assign(__ds_scope, { CaseMeta });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseMeta.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseNav.jsx
try { (() => {
/** The sticky, scroll-spied jump nav from the bakeoff's entry A, translated: a horizontally
 *  scrollable row of anchors (so a long stop list degrades to a swipe on a phone rather than
 *  crushing every label), an `IntersectionObserver` keeping the in-view stop honest as the reader
 *  scrolls, and a plain `href="#id"` under every stop so navigation still works with the observer
 *  entirely absent. The observer's `root` is `scrollRoot.current` rather than the viewport, because
 *  this product's document scrolls inside a nested pane, not the window. The active stop inverts to
 *  an ink chip — this system's one selection language — rather than entry A's vermillion underline;
 *  the underline survives as the hover mark, reusing the same `.ds-tab` rule every other tab in the
 *  system already draws with. */
function CaseNav({
  label = "Case map",
  stops = [],
  scrollRoot,
  style,
  ...rest
}) {
  const [active, setActive] = React.useState(stops[0] ? stops[0].id : null);
  React.useEffect(() => {
    const node = scrollRoot && scrollRoot.current;
    if (!node || typeof IntersectionObserver === "undefined" || !stops.length) return undefined;
    /* Below the shell breakpoint this node stops being a scroll container at all — the page
       scrolls instead (tokens/shell.css). A `root` that cannot scroll its own content makes
       IntersectionObserver's geometry meaningless, so fall back to the viewport (`root: null`)
       whenever the node's content no longer overflows it, rather than branching on width. */
    const root = node.scrollHeight > node.clientHeight + 1 ? node : null;
    const targets = stops.map(s => document.getElementById(s.id)).filter(Boolean);
    if (!targets.length) return undefined;
    const io = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) setActive(entry.target.id);
      });
    }, {
      root,
      rootMargin: "-45% 0px -50% 0px",
      threshold: 0
    });
    targets.forEach(t => io.observe(t));
    return () => io.disconnect();
  }, [scrollRoot, stops]);
  const jump = id => event => {
    const target = document.getElementById(id);
    const root = scrollRoot && scrollRoot.current;
    if (!target || !root) return; // no JS home for the jump — the href="#id" still does the job
    event.preventDefault();
    const reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    target.scrollIntoView({
      behavior: reduce ? "auto" : "smooth",
      block: "start"
    });
  };
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "case-nav",
    style: {
      position: "sticky",
      top: 0,
      zIndex: 40,
      display: "flex",
      alignItems: "stretch",
      overflowX: "auto",
      scrollbarWidth: "none",
      WebkitOverflowScrolling: "touch",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      backdropFilter: "blur(14px)",
      boxShadow: "var(--shadow-sticky)",
      ...style
    },
    ...rest
  }, stops.map(s => {
    const on = s.id === active;
    return /*#__PURE__*/React.createElement("a", {
      key: s.id,
      href: "#" + s.id,
      onClick: jump(s.id),
      "aria-current": on ? "true" : undefined,
      className: "ds-tab ds-tab-chapter",
      style: {
        flex: "0 0 auto",
        display: "flex",
        alignItems: "center",
        minHeight: 44,
        padding: "0 clamp(13px,1.7vw,20px)",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        background: on ? "var(--ink)" : "transparent",
        fontFamily: "var(--font-sans)",
        fontSize: 12,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        textDecoration: "none",
        whiteSpace: "nowrap",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, s.label);
  }));
}
Object.assign(__ds_scope, { CaseNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseNav.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseQuote.jsx
try { (() => {
/** The pull-quote as a full-width interlude: hairline top and bottom, nothing else — no ground fill,
 *  no shadow, no left bar. It is the one moment allowed to breathe precisely because everything
 *  else on the page is compact. Distinct from `PullQuote`, which is a boxed ink specimen for a
 *  quote sitting beside other evidence; this one stands alone in the reading column. */
function CaseQuote({
  children,
  attribution,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: "clamp(30px,4vw,46px) 0",
      padding: "clamp(24px,3.4vw,38px) 0",
      borderTop: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      textAlign: "center",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: "0 auto",
      maxWidth: "52ch",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      color: "var(--story-ink)",
      textWrap: "balance"
    }
  }, children), attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      marginTop: 14,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      letterSpacing: "0.01em"
    }
  }, attribution));
}
Object.assign(__ds_scope, { CaseQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseQuote.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseSection.jsx
try { (() => {
/** A single-baseline case section: a small numeral beside its name, then content at native width —
 *  never the tone-scoped banner `StorySection` uses. The header is one line, not a persistent
 *  side-gutter running the height of the section, which is most of the density this buys back:
 *  a chapter costs a baseline, not a margin column. Content scrolls inside the section rather than
 *  the page if a nested block ever runs wider than the frame, so one over-eager grid can never turn
 *  into page-level horizontal scroll. */
function CaseSection({
  tone = "paper",
  index,
  title,
  id,
  scrollMarginTop = 56,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      scrollMarginTop,
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      padding: "var(--section-pad-tight) var(--gutter)",
      ...style
    },
    ...rest
  }, (index || title) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: 12,
      marginBottom: "var(--space-7)"
    }
  }, index && /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "0 0 auto",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, index), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: 650,
      lineHeight: 1.1,
      letterSpacing: "var(--headline-tracking)"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "100%",
      overflowX: "auto"
    }
  }, children));
}
Object.assign(__ds_scope, { CaseSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseSection.jsx", error: String((e && e.message) || e) }); }

// components/case/MetricStrip.jsx
try { (() => {
/** The Overview metric row: unboxed, native width, a single ink rule above it and a hairline
 *  between cells — no ground, no shadow, no card padding. `Ledger` carries the same value-above-
 *  label instrument grammar but is a raised, labelled panel; this is deliberately the unframed
 *  opposite, because entry A's own density doctrine keeps the argument-first strip flush with the
 *  page rather than boxed like the evidence beneath it. Renders only the cells it is given — pass
 *  only entries that already carry a real number; padding a slot with an invented figure is exactly
 *  what this component exists to refuse. */
function MetricStrip({
  items = [],
  style,
  ...rest
}) {
  const count = Math.max(items.length, 1);
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "metric-strip",
    "data-count": count,
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + count + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-ink)",
      paddingTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      minWidth: 0,
      padding: i === 0 ? "0 clamp(16px,2vw,26px) 0 0" : "0 clamp(16px,2vw,26px)",
      borderLeft: i === 0 ? "none" : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginTop: 8,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, item.label))));
}
Object.assign(__ds_scope, { MetricStrip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/MetricStrip.jsx", error: String((e && e.message) || e) }); }

// components/case/StoryBeat.jsx
try { (() => {
/** A chapter's opening beat: a micro-label kicker, a headline claim, and a lede paragraph — the
 *  argument a `CaseSection` exists to make, stated before its evidence. Where the bakeoff's
 *  Continuum entry shares one beat label across four short paragraphs, a case document's chapters
 *  are each already a considered, separately titled beat, so this renders one beat per section
 *  rather than several stacked under a shared heading — the micro-label-over-statement grammar
 *  survives the translation; the stacking count adapts to what the chapter actually is. */
function StoryBeat({
  kicker,
  title,
  intro,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(100%, 780px)",
      ...style
    },
    ...rest
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 14px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      letterSpacing: "0.01em"
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: "var(--display-s-weight)",
      lineHeight: "var(--display-s-leading)",
      letterSpacing: "var(--display-s-tracking)",
      textWrap: "balance",
      color: "var(--story-ink)"
    }
  }, title), intro && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 680,
      margin: title ? "16px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro), children);
}
Object.assign(__ds_scope, { StoryBeat });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/StoryBeat.jsx", error: String((e && e.message) || e) }); }

// components/document/DocFigure.jsx
try { (() => {
/** A numbered figure or table. Two things a report needs that an exhibition does not: a number a
 *  reader can cite, and permission to break out past the reading measure — a wide table is
 *  unreadable squeezed into a prose column.
 *
 *  Any of the system's instruments can sit inside one. In exhibition mode an instrument IS the
 *  moment; here it is evidence with a number attached, which is the whole difference in register. */
function DocFigure({
  kind = "figure",
  caption,
  source,
  wide = false,
  children,
  id,
  style,
  ...rest
}) {
  const isTable = kind === "table";
  return /*#__PURE__*/React.createElement("figure", {
    id: id,
    className: isTable ? "ds-doc-table" : "ds-doc-figure",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: wide ? "none" : "var(--doc-measure)",
      marginLeft: wide ? "0" : "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, children), /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginTop: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: isTable ? "ds-doc-tablabel" : "ds-doc-figlabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, caption), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "break-word"
    }
  }, source))));
}
Object.assign(__ds_scope, { DocFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/DocFigure.jsx", error: String((e && e.message) || e) }); }

// components/document/Endnotes.jsx
try { (() => {
/** Endnotes and references — the two things that let a reader check you.
 *
 *  `NoteRef` marks a claim in the prose; `Endnotes` lists them at the end. The numbering is a CSS
 *  counter shared with the marks, so a note can never be numbered wrong. References are separate
 *  because they are a different kind of object: a note qualifies a claim, a reference names a source.
 *  A report that measures things needs both, and their absence is what makes an argument look like
 *  an assertion. */
function NoteRef({
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-noteref",
    "aria-hidden": "true",
    style: style,
    ...rest
  });
}
function Endnotes({
  notes = [],
  label = "Notes",
  style,
  ...rest
}) {
  if (!notes.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    style: {
      marginTop: "var(--space-11)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      padding: "9px 0",
      borderBottom: i === notes.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, i + 1), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5
    }
  }, n)))));
}
function References({
  items = [],
  label = "References",
  style,
  ...rest
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    className: "ds-doc-refs",
    style: {
      marginTop: "var(--space-10)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ul", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "9px 0",
      borderBottom: i === items.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5,
      textIndent: "-1.2em",
      paddingLeft: "1.2em"
    }
  }, r.href ? /*#__PURE__*/React.createElement("a", {
    href: r.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "inherit",
      textDecoration: "none",
      borderBottom: "1px solid var(--story-accent)"
    }
  }, r.text) : r.text))));
}
Object.assign(__ds_scope, { NoteRef, Endnotes, References });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Endnotes.jsx", error: String((e && e.message) || e) }); }

// components/document/Finding.jsx
try { (() => {
/** A finding stated twice: once in English, once in numbers.
 *
 *  A report that only speaks in measurements excludes everyone who has not read the method, and a
 *  report that only speaks in plain language cannot be checked. So both, adjacent, with the plain
 *  sentence leading — a reader travelling fast reads only those and still gets the argument; a
 *  reader who wants proof drops one line.
 *
 *  This is the receiver-side / producer-side distinction as a typographic device. "Help headings
 *  repeat themselves" is what a reader experiences. "Lexical diversity 1.45 against a 4.9–7.2 band"
 *  is what the writer did. Neither one alone is the finding. */
function Finding({
  children,
  measured,
  verdict,
  style,
  ...rest
}) {
  const tone = verdict === "noise" ? "var(--story-muted)" : verdict === "underpowered" ? "var(--story-muted)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      margin: "0 0 var(--space-6)",
      paddingLeft: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(17px,1.5vw,21px)",
      fontWeight: 620,
      lineHeight: 1.34,
      letterSpacing: "-0.032em",
      textWrap: "pretty"
    }
  }, children), (measured || verdict) && /*#__PURE__*/React.createElement("p", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "0 14px",
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      lineHeight: 1.5
    }
  }, measured && /*#__PURE__*/React.createElement("span", null, measured), verdict && /*#__PURE__*/React.createElement("span", {
    style: {
      color: tone
    }
  }, verdict)));
}
Object.assign(__ds_scope, { Finding });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Finding.jsx", error: String((e && e.message) || e) }); }

// components/document/PaperTable.jsx
try { (() => {
/** A citation or URL in `source` is a long, unbroken run of characters, and the box it sits in narrows
 *  with the viewport. A blunt `word-break: break-word` last-resort breaker WILL split that run once it
 *  stops fitting its line -- including mid-letter, with nothing at the split to explain it ("th" / "is"
 *  torn across a line break). A real URL already carries its own natural break points (/, _, ., :, ?,
 *  &, =, ,); <wbr/> offers those to the browser as OPTIONAL line-break opportunities -- invisible in
 *  the rendered page, absent from a copy-paste, and only ever used when a break is actually needed --
 *  so a long source wraps at its own punctuation instead of fracturing an arbitrary character run.
 *  Ordinary short source text is untouched: it already wraps at its spaces like any other prose. */
const URL_BREAK_CHARS = /([/_.:?&=,])/;
function withBreakOpportunities(text) {
  if (typeof text !== "string") return text;
  return text.split(URL_BREAK_CHARS).map((part, i) => i % 2 === 1 ? /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, part, /*#__PURE__*/React.createElement("wbr", null)) : part);
}

/** A table set the way papers set tables: rules, not boxes.
 *
 *  A heavy rule above the header, a hairline under it, a heavy rule under the last row, and nothing
 *  else — no vertical rules, no cell borders, no fill. This is the booktabs convention and it is the
 *  single strongest visual signal that a page is a paper rather than an interface. Boxed grids read
 *  as UI; ruled tables read as evidence.
 *
 *  The caption sits ABOVE, which is also the convention and is genuinely better: a reader meets the
 *  claim before the numbers instead of after them. Figures caption below; tables caption above. */
function PaperTable({
  caption,
  source,
  columns = [],
  rows = [],
  align = [],
  note,
  style,
  ...rest
}) {
  const cellAlign = i => align[i] || (i === 0 ? "left" : "right");
  return /*#__PURE__*/React.createElement("figure", {
    className: "ds-doc-table",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: "none",
      ...style
    },
    ...rest
  }, caption && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginBottom: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-tablabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.45
    }
  }, caption)), /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))"
    }
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      borderCollapse: "collapse",
      fontFamily: "var(--font-sans)",
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      padding: "9px 14px 8px 0",
      textAlign: cellAlign(i),
      verticalAlign: "bottom",
      borderTop: "2px solid var(--story-ink)",
      borderBottom: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap"
    }
  }, c)))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r
  }, row.map((v, c) => /*#__PURE__*/React.createElement("td", {
    key: c,
    style: {
      padding: "8px 14px 8px 0",
      textAlign: cellAlign(c),
      verticalAlign: "baseline",
      borderBottom: r === rows.length - 1 ? "2px solid var(--story-ink)" : "none",
      color: c === 0 ? "var(--story-ink)" : "var(--story-muted)",
      fontSize: 13,
      lineHeight: 1.5,
      fontWeight: c === 0 ? 560 : 400
    }
  }, v))))))), (note || source) && /*#__PURE__*/React.createElement("div", {
    style: {
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: 9
    }
  }, note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, note), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: note ? "5px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "normal",
      overflowWrap: "normal"
    }
  }, withBreakOpportunities(source))));
}
Object.assign(__ds_scope, { PaperTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/PaperTable.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  plain,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), plain && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 10,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "In plain terms"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(18px,1.6vw,23px)",
      fontWeight: 620,
      lineHeight: 1.36,
      letterSpacing: "-0.033em",
      textWrap: "pretty"
    }
  }, plain)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Method and scope"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/document/ReportSection.jsx
try { (() => {
/** A numbered, citable section. The number hangs in the gutter — the same anatomy the evidence
 *  components use for their labels, so document mode reads as the same system rather than a
 *  different one. Numbering is done by CSS counters, so inserting a section never renumbers by hand
 *  and a section can never claim a number it does not have. */
function ReportSection({
  level = 1,
  title,
  children,
  id,
  style,
  ...rest
}) {
  const sub = level > 1;
  const Heading = sub ? "h3" : "h2";
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: sub ? "ds-doc-subsection" : "ds-doc-section",
    style: {
      marginTop: sub ? "var(--space-9)" : "var(--space-11)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-num",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: sub ? 10 : 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement(Heading, {
    style: {
      margin: 0,
      maxWidth: "var(--doc-measure)",
      fontFamily: "var(--font-sans)",
      fontSize: sub ? "clamp(16px,1.35vw,19px)" : "clamp(20px,1.9vw,26px)",
      fontWeight: sub ? 650 : 700,
      lineHeight: 1.2,
      letterSpacing: "-0.035em",
      textWrap: "balance"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: "var(--space-5)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, children));
}
Object.assign(__ds_scope, { ReportSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/ReportSection.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ArtifactFigure.jsx
try { (() => {
/** Framed artifact with a gutter-aligned provenance caption. No height floor by default — the frame is
 *  the artifact's own edge, so a short artifact gets a short frame. Pass `minHeight` only when a set of
 *  figures genuinely has to align to each other. */
function ArtifactFigure({
  src,
  alt = "",
  label,
  caption,
  contain = false,
  zoom = false,
  minHeight,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    className: zoom ? "ds-figure-zoom" : undefined,
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight,
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--surface-mat)",
      boxShadow: "var(--story-shadow)",
      padding: contain ? "clamp(14px,1.8vw,24px)" : 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: "100%",
      height: minHeight ? "100%" : "auto",
      minHeight: minHeight ? minHeight - 40 : undefined,
      display: "block",
      objectFit: contain ? "contain" : "cover"
    }
  })), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { ArtifactFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ArtifactFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/BeforeAfter.jsx
try { (() => {
/** A rewrite delivered as a diff with its measurements attached, so the person deciding whether to
 *  take it can see what moved and by how much. The deltas are the argument — without them this is
 *  just someone else's preferred wording. */
function BeforeAfter({
  before,
  after,
  beforeLabel = "Before",
  afterLabel = "After",
  deltas = [],
  actions,
  style,
  ...rest
}) {
  const pane = (label, text, tone) => /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "14px 16px",
      borderRight: tone === "before" ? "1px solid var(--story-line)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: tone === "after" ? "var(--story-accent)" : "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: tone === "before" ? "var(--story-muted)" : "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: tone === "after" ? 560 : 480,
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, text));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      borderBottom: "1px solid var(--story-line)"
    }
  }, pane(beforeLabel, before, "before"), pane(afterLabel, after, "after")), deltas.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(deltas.length, 1) + ",minmax(0,1fr))",
      borderBottom: actions ? "1px solid var(--story-line)" : 0
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        padding: "11px 14px",
        borderRight: i === deltas.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "block",
        marginBottom: 6,
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "baseline",
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 13,
        fontWeight: 560,
        letterSpacing: "-0.03em",
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-accent)",
        fontSize: 17,
        fontWeight: 700,
        letterSpacing: "-0.04em"
      }
    }, d.to)));
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-8)",
      alignItems: "center",
      padding: "12px 16px"
    }
  }, actions));
}
Object.assign(__ds_scope, { BeforeAfter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/BeforeAfter.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CaptureFigure.jsx
try { (() => {
/** A capture of shipped behaviour, in a frame that says what you are looking at.
 *
 *  Video is the only honest way to show an interaction that happens in time, so this plays inline —
 *  muted, looping, playsinline, with controls available. Autoplay is off by default: the accessibility
 *  floor forbids a loop that plays without the reader asking for it. */
function CaptureFigure({
  src,
  poster,
  label,
  caption,
  quote,
  autoPlay = false,
  controls = true,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--ink)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("video", {
    src: src,
    poster: poster,
    muted: true,
    loop: true,
    playsInline: true,
    autoPlay: autoPlay,
    controls: controls,
    preload: "metadata",
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), quote && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "The string"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, quote)), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { CaptureFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CaptureFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CardGrid.jsx
try { (() => {
/** Hairline grid block, on the instrument grammar: an optional silkscreen header naming what the set is
 *  and how many are in it, then cells sized by their contents. Without a label it stays a bare grid —
 *  but if you cannot name the set, ask whether it needed a frame at all. */
function CardGrid({
  columns = 2,
  raised = true,
  label,
  scope,
  children,
  style,
  ...rest
}) {
  const count = React.Children.count(children);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, scope || String(count).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, children));
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
/** A hyphen or dash already in the text is a standard line-break point (Unicode UAX #14) in every
 *  browser, with no special CSS involved, and MOST of the time that is correct: "help-centre" wrapping
 *  to "help-" / "centre" at a narrow column is ordinary, everyday typography — books do it every day —
 *  and gluing it shut would make the word un-searchable and un-copyable (the rendered string would
 *  carry an invisible character a reader can't see or type back). Two narrow cases are genuinely wrong
 *  rather than merely unlucky, because there the two sides of the dash are not independently meaningful
 *  words: a digit range ("2025-2026" splitting to "2025-" / "2026" reads as a typo, not a range) and a
 *  dash-joined token inside a parenthetical statistical label ("p (BH-adj)" splitting to "p (BH-" /
 *  "adj)" garbles the abbreviation). Only those two get a zero-width word joiner (U+2060) glued to the
 *  dash; every other hyphenated compound is left exactly as written. */
const DASH = "\\u002D\\u2010-\\u2015"; // hyphen-minus; hyphen through horizontal bar (covers en/em dash)
const WJ = "⁠"; // word joiner — zero-width, forbids a line break at this point, otherwise invisible
const DIGIT_RANGE_DASH = new RegExp("(\\d)([" + DASH + "])(?=\\d)", "g");
const PAREN_GROUP = /\(([^()]*)\)/g;
const DASH_IN_PAREN = new RegExp("([" + DASH + "])(?=\\S)", "g");
const glueDashes = v => {
  if (typeof v !== "string") return v;
  const rangedNumbers = v.replace(DIGIT_RANGE_DASH, "$1$2" + WJ);
  return rangedNumbers.replace(PAREN_GROUP, (m, inner) => "(" + inner.replace(DASH_IN_PAREN, "$1" + WJ) + ")");
};

/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, glueDashes(c))))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = glueDashes(highlight ? value.value : value);
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DisciplineVenn.jsx
try { (() => {
/** Three disciplines and the work that only exists where they overlap. Drawn as a registration
 *  diagram — hairline circles, no fills, the intersection marked the way the system marks a
 *  registration point — so it reads as a technical figure rather than a slide graphic. */
function DisciplineVenn({
  fields = [],
  intersection,
  caption,
  tone = "story",
  style,
  ...rest
}) {
  const ink = tone === "stage" ? "var(--stage-ink)" : "var(--story-ink)";
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const muted = tone === "stage" ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = tone === "stage" ? "var(--signal-stage)" : "var(--story-accent)";

  /* Three circles on an equilateral arrangement, centred on 150,142. */
  const circles = [{
    cx: 150,
    cy: 96,
    label: fields[0]
  }, {
    cx: 111,
    cy: 164,
    label: fields[1]
  }, {
    cx: 189,
    cy: 164,
    label: fields[2]
  }];
  /* Labels sit outside the circle field so a long discipline name cannot crowd its neighbour. */
  const anchors = [{
    x: 150,
    y: 20,
    align: "middle"
  }, {
    x: -34,
    y: 240,
    align: "start"
  }, {
    x: 334,
    y: 240,
    align: "end"
  }];
  const leaders = [{
    x1: 150,
    y1: 27,
    x2: 150,
    y2: 36
  }, {
    x1: 62,
    y1: 233,
    x2: 84,
    y2: 213
  }, {
    x1: 238,
    y1: 233,
    x2: 216,
    y2: 213
  }];
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      padding: "var(--space-7) 0",
      border: "1px solid " + line,
      background: tone === "stage" ? "transparent" : "var(--story-raised)",
      boxShadow: tone === "stage" ? "none" : "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "-40 8 380 244",
    width: "100%",
    style: {
      maxWidth: 440,
      height: "auto",
      overflow: "visible"
    },
    role: "img",
    "aria-label": fields.join(", ") + " — " + intersection
  }, circles.map(c => /*#__PURE__*/React.createElement("circle", {
    key: c.label,
    cx: c.cx,
    cy: c.cy,
    r: "62",
    fill: "none",
    stroke: line,
    strokeWidth: "1"
  })), leaders.map((l, i) => /*#__PURE__*/React.createElement("line", {
    key: i,
    x1: l.x1,
    y1: l.y1,
    x2: l.x2,
    y2: l.y2,
    stroke: line,
    strokeWidth: "1"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "15",
    fill: "none",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "150",
    y1: "120",
    x2: "150",
    y2: "164",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "128",
    y1: "142",
    x2: "172",
    y2: "142",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "3.5",
    fill: accent
  }), circles.map((c, i) => /*#__PURE__*/React.createElement("text", {
    key: c.label,
    x: anchors[i].x,
    y: anchors[i].y,
    textAnchor: anchors[i].align,
    fill: muted,
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c.label)))), (intersection || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "13px 0 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.4
    }
  }, "Overlap"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, intersection && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, intersection), caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: intersection ? "8px 0 0" : 0,
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, caption))));
}
Object.assign(__ds_scope, { DisciplineVenn });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DisciplineVenn.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "facts-panel",
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FingerprintBars.jsx
try { (() => {
/** A measured voice fingerprint: one row per dimension, the reading drawn against the band a
 *  surface is allowed to sit in. The band is the point — a fingerprint is a tolerance, not a
 *  score, and a reading outside it is the only thing that counts as a miss. */
function FingerprintBars({
  dimensions = [],
  scale = 100,
  showBand = true,
  style,
  ...rest
}) {
  const pct = n => Math.max(0, Math.min(100, n / scale * 100));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, dimensions.map((d, i) => {
    const out = showBand && d.band && (d.value < d.band[0] || d.value > d.band[1]);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        display: "grid",
        gridTemplateColumns: "minmax(96px,0.9fr) minmax(0,2.4fr) 42px",
        gap: 14,
        alignItems: "center",
        padding: "9px 16px",
        borderBottom: i === dimensions.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700,
        lineHeight: 1.3
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        position: "relative",
        height: 9,
        display: "block",
        borderBottom: "1px solid var(--story-line)"
      }
    }, showBand && d.band && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 0,
        bottom: 0,
        left: pct(d.band[0]) + "%",
        width: pct(d.band[1]) - pct(d.band[0]) + "%",
        background: "var(--story-band)",
        borderLeft: "1px solid var(--story-line)",
        borderRight: "1px solid var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 2,
        bottom: 2,
        left: 0,
        width: pct(d.value) + "%",
        background: out ? "var(--story-accent)" : "var(--story-ink)"
      }
    })), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 14,
        fontWeight: 700,
        letterSpacing: "-0.04em",
        textAlign: "right",
        fontVariantNumeric: "tabular-nums"
      }
    }, d.value));
  }));
}
Object.assign(__ds_scope, { FingerprintBars });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FingerprintBars.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 14px",
      borderRight: "1px solid " + line,
      overflowWrap: "break-word"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35,
      overflowWrap: "break-word"
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content
 *  column. No height floor — a short cell is short. The padding is an instrument's, not a page margin's. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter-tight) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "var(--card-pad) 12px var(--card-pad) 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--card-pad) var(--card-pad-wide)"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 5,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 9,
      padding: "8px 10px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "6px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Label above value, so a cell is only as wide as the wider of the two and a row of four fits without
 *  stretching. Side-by-side reads fine until a value is a word rather than a number, at which point the
 *  cells go ragged and the label and the number end up at opposite ends of a void. This is the same
 *  stacking `VoiceProfile` uses, which is the system's answer for a set of small readings. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: "0 1 auto",
      padding: "12px 18px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Outcome.jsx
try { (() => {
/** Amber label over a short results list or paragraph. Sits at the end of a stage copy column. */
function Outcome({
  label = "Results",
  items,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(430px,88%)",
      marginTop: 18,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--signal-stage)",
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      fontWeight: 700
    }
  }, label), items && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: "5px 0 0",
      padding: 0,
      listStyle: "none",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, item))), children && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, children));
}
Object.assign(__ds_scope, { Outcome });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Outcome.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Pipeline.jsx
try { (() => {
/** Ordered process rows. Step number in the gutter, the rule on one line — compressed, not spaced out. */
function Pipeline({
  steps = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      listStyle: "none",
      ...style
    },
    ...rest
  }, steps.map((step, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === steps.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "14px 12px 14px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, step.step || String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "13px 18px",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(15px,1.15vw,17px)",
      fontWeight: 560,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, step.label))));
}
Object.assign(__ds_scope, { Pipeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Pipeline.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      /* Sized by the words, not by a column. A quote that wraps short leaves no dead ground. */
      width: "fit-content",
      maxWidth: 560,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResearchGrid.jsx
try { (() => {
/** Published sources behind a measurement, one row each: who, when, and the thing it makes computable.
 *  A citation row is the difference between an instrument and an opinion, so the source is never
 *  paraphrased away — it stays attributable. */
function ResearchGrid({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    style: {
      padding: "15px 18px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, item.source), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, item.year)), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "9px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.12,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, item.gives), item.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.note))));
}
Object.assign(__ds_scope, { ResearchGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResearchGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResumeEntry.jsx
try { (() => {
/** One record in a résumé timeline: dates in the label gutter, org and role, then what I did there. */
function ResumeEntry({
  when,
  org,
  role,
  bullets = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: "clamp(16px,2vw,28px)",
      padding: "var(--space-7) 0",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      paddingTop: 3,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.45
    }
  }, when), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--display-s-tracking)"
    }
  }, org), role && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, role), bullets.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 11,
      margin: "var(--space-5) 0 0",
      padding: 0,
      listStyle: "none"
    }
  }, bullets.map((b, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "14px minmax(0,1fr)",
      gap: 10,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em",
      textWrap: "pretty"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      marginTop: "0.62em",
      width: 7,
      height: 1,
      background: "var(--story-accent)"
    }
  }), b)))));
}
Object.assign(__ds_scope, { ResumeEntry });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResumeEntry.jsx", error: String((e && e.message) || e) }); }

// components/evidence/RuleSplit.jsx
try { (() => {
/** Two-column sort of a guidance set: what a model has to judge, and what code can check.
 *  The whole argument for a measurement layer fits in one of these — a reader can see the
 *  proportion before reading a single row. */
function RuleSplit({
  modelLabel = "Model judgment",
  codeLabel = "Checkable in code",
  rules = [],
  note,
  style,
  ...rest
}) {
  const model = rules.filter(r => r.kind === "model");
  const code = rules.filter(r => r.kind === "code");
  const column = (label, items, accent) => /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      borderRight: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "10px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.rule))));
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      border: "1px solid var(--story-line)",
      borderRight: 0,
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, column(modelLabel, model, "var(--story-muted)"), column(codeLabel, code, "var(--story-accent)")), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, note));
}
Object.assign(__ds_scope, { RuleSplit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/RuleSplit.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
/** Ruled inventory of inputs, sized by what it names.
 *
 *  Each entry is as wide as its own number-and-name pair and wraps when it runs out of room. An entry
 *  does not stretch to half the page and then sit alone at the left edge of it. */
function SourceMap({
  items = [],
  label = "Inputs",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 24,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "flex",
      gap: 12,
      alignItems: "baseline",
      flex: "0 1 auto",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal",
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item)))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/TenGrid.jsx
try { (() => {
/** Ten hairline cells; filled cells read as passes. A dimension meter, not a progress bar. */
function TenGrid({
  total = 10,
  pass = 0,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + total + ",1fr)",
      gap: 4,
      marginTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, Array.from({
    length: total
  }).map((_, i) => /*#__PURE__*/React.createElement("i", {
    key: i,
    style: {
      height: 10,
      border: "1px solid " + (i < pass ? "var(--story-accent)" : "var(--story-muted)"),
      background: i < pass ? "var(--story-accent)" : "transparent"
    }
  })));
}
Object.assign(__ds_scope, { TenGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/TenGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Toolchain.jsx
try { (() => {
/** What a system was actually built out of, named and linked so a reader can go and check.
 *
 *  The engineering counterpart to `ResearchGrid`: same anatomy, same register. Deliberately no logos —
 *  a row of third-party brand marks would be the one place this system's ink-and-hairline language
 *  breaks, and it is the oldest tell in portfolio design. A name, a licence, and what it actually
 *  provides is more credible than a badge, because it can be verified. */
function Toolchain({
  items = [],
  label = "Built with",
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: item.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(140px,0.9fr) minmax(0,2fr) auto",
      gap: "clamp(12px,2vw,26px)",
      alignItems: "baseline",
      padding: "12px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, item.href ? /*#__PURE__*/React.createElement("a", {
    href: item.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em",
      textDecoration: "none",
      borderBottom: "1px solid currentColor",
      width: "fit-content",
      paddingBottom: 1
    }
  }, item.name) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em"
    }
  }, item.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.provides), item.meta && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      textAlign: "right"
    }
  }, item.meta)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 640,
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45
    }
  }, note));
}
Object.assign(__ds_scope, { Toolchain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Toolchain.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceProfile.jsx
try { (() => {
/** The target a surface is written to, published so a team can argue with it: each dimension's
 *  setting and the one sentence saying why it sits there. A profile without its reasoning is a
 *  number nobody will honour. */
function VoiceProfile({
  product,
  surface,
  dimensions = [],
  notes = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Voice profile"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, [product, surface].filter(Boolean).join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit,minmax(128px,1fr))",
      borderBottom: notes.length ? "1px solid var(--story-line)" : 0
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      padding: "13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 8,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      height: 3,
      display: "block",
      marginTop: 9,
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: d.value + "%",
      background: "var(--story-accent)"
    }
  }))))), notes.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === notes.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, n.name), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, n.why)))));
}
Object.assign(__ds_scope, { VoiceProfile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceProfile.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceScoreRing.jsx
try { (() => {
/** How a reading reaches the person who has to act on it: one alignment figure, the dimensions
 *  underneath it, and a verdict in words. The arc draws itself on mount rather than fading in —
 *  a measurement should look like it was taken, not like it was already there. */
function VoiceScoreRing({
  score = 0,
  outOf = 100,
  label = "Voice alignment",
  verdict,
  scope = [],
  dimensions = [],
  size = 148,
  style,
  ...rest
}) {
  const [drawn, setDrawn] = React.useState(false);
  React.useEffect(() => {
    /* Two frames, so the browser paints the zero state before the arc animates to its value.
       Both ids are tracked — cancelling only the outer one leaves the inner frame live. */
    let inner;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setDrawn(true));
    });
    return () => {
      cancelAnimationFrame(outer);
      if (inner) cancelAnimationFrame(inner);
    };
  }, []);
  const r = (size - 18) / 2;
  const c = 2 * Math.PI * r;
  const filled = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope.length > 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, scope.join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(18px,2.4vw,32px)",
      alignItems: "center",
      padding: "18px 16px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: "7",
    strokeLinecap: "butt",
    strokeDasharray: c,
    strokeDashoffset: drawn ? c * (1 - filled) : c,
    style: {
      transition: "stroke-dashoffset var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "of ", outOf))), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, verdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 13px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)"
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(80px,1fr) minmax(0,1.6fr) 34px",
      gap: 12,
      alignItems: "center",
      padding: "7px 0",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 5,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: (drawn ? d.value : 0) + "%",
      background: "var(--story-ink)",
      transition: "width var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value)))))));
}
Object.assign(__ds_scope, { VoiceScoreRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceScoreRing.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Eyebrow.jsx
try { (() => {
const TONES = {
  muted: "var(--story-muted, var(--muted-ink))",
  signal: "var(--signal)",
  action: "var(--signal-stage)",
  stage: "var(--stage-muted)"
};

/** Small mono label that opens a scene, a section, or a card. */
function Eyebrow({
  tone = "muted",
  size = "eyebrow",
  as = "p",
  children,
  style,
  ...rest
}) {
  const sizes = {
    eyebrow: {
      fontSize: "var(--eyebrow-size)",
      fontWeight: "var(--eyebrow-weight)",
      letterSpacing: "var(--eyebrow-tracking)",
      fontFamily: "var(--font-sans)"
    },
    meta: {
      fontSize: "var(--meta-size)",
      fontWeight: "var(--meta-weight)",
      letterSpacing: "-0.01em",
      fontFamily: "var(--font-mono)"
    },
    micro: {
      fontSize: "var(--micro-size)",
      fontWeight: "var(--micro-weight)",
      letterSpacing: "0",
      fontFamily: "var(--font-mono)"
    }
  };
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      margin: 0,
      lineHeight: "var(--eyebrow-leading)",
      color: TONES[tone],
      ...sizes[size],
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/LineArrow.jsx
try { (() => {
/** The drawn arrow glyph: 1px rule plus a rotated corner. Inherits currentColor. */
function LineArrow({
  reverse = false,
  width = 34,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      display: "inline-block",
      flex: "0 0 " + width + "px",
      width,
      height: 12,
      transform: reverse ? "rotate(180deg)" : undefined,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 5,
      right: 0,
      left: 0,
      height: 1,
      background: "currentColor"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 2,
      right: 0,
      width: 7,
      height: 7,
      borderTop: "1px solid currentColor",
      borderRight: "1px solid currentColor",
      transform: "rotate(45deg)"
    }
  }));
}
Object.assign(__ds_scope, { LineArrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/LineArrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/ProvenanceNote.jsx
try { (() => {
/** Hairline-topped note that states what an artifact is and is not. */
function ProvenanceNote({
  label = "Provenance",
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      marginTop: "var(--space-6)",
      paddingTop: "var(--space-4)",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 700,
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.5,
      letterSpacing: "-0.015em"
    }
  }, children));
}
Object.assign(__ds_scope, { ProvenanceNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/ProvenanceNote.jsx", error: String((e && e.message) || e) }); }

// components/foundation/RegistrationMark.jsx
try { (() => {
/** Corner crosshair with a gold centre — the print-registration device used on every stage. */
function RegistrationMark({
  size = 22,
  color = "var(--stage-ink)",
  dot = "var(--signal-stage)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      top: 22,
      right: 22,
      width: size,
      height: size,
      opacity: 0.65,
      pointerEvents: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: size / 2 - 1,
      right: 0,
      left: 0,
      height: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: size / 2 - 1,
      width: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 4,
      border: "1px solid " + color,
      borderRadius: "var(--radius-round)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 8,
      borderRadius: "var(--radius-round)",
      background: dot
    }
  }));
}
Object.assign(__ds_scope, { RegistrationMark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/RegistrationMark.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Tag.jsx
try { (() => {
/** Hairline capability tag. Square corners, no fill. */
function Tag({
  on = "stage",
  children,
  style,
  ...rest
}) {
  const border = on === "stage" ? "var(--stage-line)" : "var(--soft-line)";
  return /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "9px 11px",
      border: "1px solid " + border,
      fontFamily: "var(--font-sans)",
      fontSize: 10,
      letterSpacing: "-0.01em",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Tag.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum.
 *  `as` sets the element, so a row inside an ordered list can be a real `li` rather than a div. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  as = "div",
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const Tag = as;
  if (!gutter) {
    return /*#__PURE__*/React.createElement(Tag, {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      listStyle: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel, PanelRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/layout/PaperFrame.jsx
try { (() => {
/** The bounded document: a paper card on a darker desk, hairline border, hard offset shadow, grain overlay. */
function PaperFrame({
  children,
  grain = true,
  columns = "var(--rail-width) minmax(0,1fr)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100%",
      background: "var(--surface-desk)",
      fontFamily: "var(--font-body)",
      color: "var(--ink)"
    }
  }, grain && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      zIndex: 5000,
      inset: 0,
      pointerEvents: "none",
      opacity: "var(--texture-grain-opacity)",
      mixBlendMode: "multiply",
      backgroundImage: "var(--texture-grain)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    "data-ds": "frame",
    style: {
      width: "min(calc(100% - 24px)," + "var(--frame-max))",
      height: "calc(100svh - 24px)",
      minHeight: 700,
      display: "grid",
      gridTemplateColumns: columns,
      margin: "var(--frame-inset) auto",
      overflow: "hidden",
      border: "1px solid var(--ink)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-4)",
      ...style
    },
    ...rest
  }, children));
}
Object.assign(__ds_scope, { PaperFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/PaperFrame.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stage.jsx
try { (() => {
/** The pine authored stage: gradient field, clipped, with an optional registration mark. */
function Stage({
  children,
  variant = "field",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    "data-ds": "stage",
    style: {
      position: "relative",
      zIndex: 1,
      minWidth: 0,
      minHeight: 0,
      overflow: "hidden",
      isolation: "isolate",
      background: variant === "section" ? "var(--stage-field-section)" : "var(--stage-field)",
      color: "var(--stage-ink)",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Stage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stage.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "stage-copy",
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title &&
  /*#__PURE__*/
  /* data-doc-heading + tabIndex={-1}: the one place per document the app moves focus to
     after a destination change (see app.jsx). Focusable without joining the tab order;
     the ring itself is drawn by :focus-visible in tokens/interaction.css, so a mouse
     click never shows one and a keyboard move always does. */
  React.createElement("h1", {
    "data-doc-heading": true,
    tabIndex: -1,
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    "data-ds": "stage-curtain",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      /* The off-screen resting transform only matters while a transition is actually playing —
         `visibility: hidden` already fully hides the plate at idle, so idle can rest at
         `none` instead of parking a real, positioned box outside the viewport. That box used to
         be silently clipped by the desktop reader's `overflow: hidden`; once the phone reader
         gives that up (tokens/shell.css, needed to keep `position: sticky` tracking the real
         page scroll), the same idle box was inflating the document's own horizontal scroll
         width by exactly its own overhang. The cover/reveal keyframes define their own `from`
         transform the instant they start, so dropping the idle-only offset changes nothing
         about how a transition actually looks. */
      transform: active ? direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)" : "none",
      willChange: "transform",
      animation: plate,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }

// components/layout/StorySection.jsx
try { (() => {
/** A tone-scoped chapter of a case document: index, kicker, title, intro, then content. */
function StorySection({
  tone = "paper",
  index,
  chapterLabel,
  kicker,
  title,
  intro,
  children,
  id,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      display: "grid",
      gridTemplateColumns: "70px minmax(0,1fr)",
      gap: "clamp(28px,5vw,82px)",
      padding: "var(--section-pad) var(--gutter)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700
    }
  }, chapterLabel && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, chapterLabel), index && /*#__PURE__*/React.createElement("b", {
    style: {
      display: "block",
      color: "var(--story-accent)"
    }
  }, index)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 980
    }
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 16px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-m-size)",
      fontWeight: "var(--display-m-weight)",
      lineHeight: "var(--display-m-leading)",
      letterSpacing: "var(--display-m-tracking)",
      textWrap: "balance"
    }
  }, title), intro && /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 760,
      marginTop: 20,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      minWidth: 0,
      gridColumn: "1 / -1",
      color: "var(--story-ink)"
    }
  }, children));
}
Object.assign(__ds_scope, { StorySection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StorySection.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ChapterNav.jsx
try { (() => {
/** Sticky chapter map. The chapter in view inverts to an ink chip. */
function ChapterNav({
  label = "Case map",
  chapters = [],
  active,
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "chapter-nav",
    style: {
      position: "sticky",
      zIndex: 45,
      top: 0,
      minHeight: "var(--chapter-nav-height)",
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(22px,3vw,48px)",
      alignItems: "center",
      padding: "var(--space-2) var(--gutter-tight)",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      backdropFilter: "blur(14px)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 760,
      whiteSpace: "nowrap"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    "data-ds": "chapter-nav-track",
    style: {
      minWidth: 0,
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(chapters.length, 1) + ",minmax(74px,1fr))",
      gap: 4
    }
  }, chapters.map(c => {
    const on = c.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      type: "button",
      className: "ds-tab ds-tab-chapter",
      onClick: () => onSelect && onSelect(c.id),
      "aria-current": on ? "true" : undefined,
      style: {
        position: "relative",
        minHeight: 34,
        padding: "0 8px",
        border: 0,
        background: on ? "var(--ink)" : "transparent",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        cursor: "pointer",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, c.label);
  })));
}
Object.assign(__ds_scope, { ChapterNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ChapterNav.jsx", error: String((e && e.message) || e) }); }






// components/actions/ScrollCue.jsx
try { (() => {
/** Circled arrow plus a short promise, telling the reader the document continues. */
function ScrollCue({
  label = "The case study continues",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      gap: "var(--space-3)",
      alignItems: "center",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", null, label), /*#__PURE__*/React.createElement("i", {
    style: {
      width: 26,
      height: 26,
      display: "grid",
      placeItems: "center",
      border: "1px solid var(--stage-line)",
      borderRadius: "var(--radius-round)",
      color: "var(--signal-stage)",
      animation: "scroll-nudge var(--dur-nudge) ease-in-out infinite",
      fontStyle: "normal"
    }
  }, "↓"));
}
Object.assign(__ds_scope, { ScrollCue });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/ScrollCue.jsx", error: String((e && e.message) || e) }); }

// components/actions/StageAction.jsx
try { (() => {
const ACCENTS = {
  action: "var(--signal-stage)",
  signal: "var(--signal)",
  ink: "var(--ink)",
  stage: "var(--stage-ink)"
};

/** The system's only button: a text label on a 1px underline with a drawn arrow. */
function StageAction({
  accent = "action",
  href,
  onClick,
  reverse = false,
  children,
  style,
  ...rest
}) {
  const color = ACCENTS[accent];
  const shared = {
    width: "fit-content",
    display: "inline-flex",
    alignItems: "center",
    gap: 17,
    padding: "0 0 7px",
    border: 0,
    borderBottom: "1px solid " + color,
    background: "transparent",
    color,
    fontFamily: "var(--font-sans)",
    fontSize: 13,
    fontWeight: 700,
    letterSpacing: "-0.02em",
    whiteSpace: "nowrap",
    cursor: "pointer",
    ...style
  };
  const content = reverse ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, {
    reverse: true
  }), children) : /*#__PURE__*/React.createElement(React.Fragment, null, children, /*#__PURE__*/React.createElement(__ds_scope.LineArrow, null));
  if (href) return /*#__PURE__*/React.createElement("a", {
    href: href,
    style: shared,
    ...rest
  }, content);
  return /*#__PURE__*/React.createElement("button", {
    type: "button",
    onClick: onClick,
    style: shared,
    ...rest
  }, content);
}
Object.assign(__ds_scope, { StageAction });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/actions/StageAction.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseMeta.jsx
try { (() => {
const HAIRLINE = "1px solid color-mix(in srgb, var(--story-ink) 16%, transparent)";
const CELL = {
  color: "var(--story-muted)",
  fontFamily: "var(--font-sans)",
  fontSize: 11,
  lineHeight: 1.45
};

/** The case's metadata: Role, Team, Timeline, Surface, sitting directly below the cover as the
 *  scroll's first element. Caption-size text (CLAUDE.md's "Three text sizes" law makes this the
 *  one legitimate home for the third size inside a case scroll) — sentence case, muted ink on
 *  the dark ground, a hairline between rows and nothing else. No box, no mono, no colored label:
 *  quiet data, not chrome. Every value is sourced straight from kit-data; a project with nothing
 *  real to say for a field (Word Math has no team — it's solo research) renders fewer rows, never
 *  an invented one. Distinct from `FactsPanel`, which this replaces inside a case scroll:
 *  `FactsPanel` is a raised bright-paper masthead with a 9px mono label and a headline-size value;
 *  this is unframed, caption-size throughout, and built for the dark ground every case now shares. */
function CaseMeta({
  role,
  team,
  timeline,
  surface,
  style,
  ...rest
}) {
  const rows = [{
    label: "Role",
    value: role
  }, {
    label: "Team",
    value: team
  }, {
    label: "Timeline",
    value: timeline
  }, {
    label: "Surface",
    value: surface
  }].filter(row => row.value);
  if (!rows.length) return null;
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "case-meta",
    style: {
      ...style
    },
    ...rest
  }, rows.map((row, i) => /*#__PURE__*/React.createElement("div", {
    key: row.label,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: 12,
      padding: "9px 0",
      borderTop: i === 0 ? "none" : HAIRLINE
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: CELL
  }, row.label), /*#__PURE__*/React.createElement("span", {
    style: {
      ...CELL,
      color: "var(--story-ink)"
    }
  }, row.value))));
}
Object.assign(__ds_scope, { CaseMeta });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseMeta.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseNav.jsx
try { (() => {
/** The sticky, scroll-spied jump nav from the bakeoff's entry A, translated: a horizontally
 *  scrollable row of anchors (so a long stop list degrades to a swipe on a phone rather than
 *  crushing every label), an `IntersectionObserver` keeping the in-view stop honest as the reader
 *  scrolls, and a plain `href="#id"` under every stop so navigation still works with the observer
 *  entirely absent. The observer's `root` is `scrollRoot.current` rather than the viewport, because
 *  this product's document scrolls inside a nested pane, not the window. The active stop inverts to
 *  an ink chip — this system's one selection language — rather than entry A's vermillion underline;
 *  the underline survives as the hover mark, reusing the same `.ds-tab` rule every other tab in the
 *  system already draws with. */
function CaseNav({
  label = "Case map",
  stops = [],
  scrollRoot,
  style,
  ...rest
}) {
  const [active, setActive] = React.useState(stops[0] ? stops[0].id : null);
  React.useEffect(() => {
    const node = scrollRoot && scrollRoot.current;
    if (!node || typeof IntersectionObserver === "undefined" || !stops.length) return undefined;
    /* Below the shell breakpoint this node stops being a scroll container at all — the page
       scrolls instead (tokens/shell.css). A `root` that cannot scroll its own content makes
       IntersectionObserver's geometry meaningless, so fall back to the viewport (`root: null`)
       whenever the node's content no longer overflows it, rather than branching on width. */
    const root = node.scrollHeight > node.clientHeight + 1 ? node : null;
    const targets = stops.map(s => document.getElementById(s.id)).filter(Boolean);
    if (!targets.length) return undefined;
    const io = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (entry.isIntersecting) setActive(entry.target.id);
      });
    }, {
      root,
      rootMargin: "-45% 0px -50% 0px",
      threshold: 0
    });
    targets.forEach(t => io.observe(t));
    return () => io.disconnect();
  }, [scrollRoot, stops]);
  const jump = id => event => {
    const target = document.getElementById(id);
    const root = scrollRoot && scrollRoot.current;
    if (!target || !root) return; // no JS home for the jump — the href="#id" still does the job
    event.preventDefault();
    const reduce = window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
    target.scrollIntoView({
      behavior: reduce ? "auto" : "smooth",
      block: "start"
    });
  };
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "case-nav",
    style: {
      position: "sticky",
      top: 0,
      zIndex: 40,
      display: "flex",
      alignItems: "stretch",
      overflowX: "auto",
      scrollbarWidth: "none",
      WebkitOverflowScrolling: "touch",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      backdropFilter: "blur(14px)",
      boxShadow: "var(--shadow-sticky)",
      ...style
    },
    ...rest
  }, stops.map(s => {
    const on = s.id === active;
    return /*#__PURE__*/React.createElement("a", {
      key: s.id,
      href: "#" + s.id,
      onClick: jump(s.id),
      "aria-current": on ? "true" : undefined,
      className: "ds-tab ds-tab-chapter",
      style: {
        flex: "0 0 auto",
        display: "flex",
        alignItems: "center",
        minHeight: 44,
        padding: "0 clamp(13px,1.7vw,20px)",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        background: on ? "var(--ink)" : "transparent",
        fontFamily: "var(--font-sans)",
        fontSize: 12,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        textDecoration: "none",
        whiteSpace: "nowrap",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, s.label);
  }));
}
Object.assign(__ds_scope, { CaseNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseNav.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseQuote.jsx
try { (() => {
/** The pull-quote as a full-width interlude: hairline top and bottom, nothing else — no ground fill,
 *  no shadow, no left bar. It is the one moment allowed to breathe precisely because everything
 *  else on the page is compact. Distinct from `PullQuote`, which is a boxed ink specimen for a
 *  quote sitting beside other evidence; this one stands alone in the reading column. */
function CaseQuote({
  children,
  attribution,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: "clamp(30px,4vw,46px) 0",
      padding: "clamp(24px,3.4vw,38px) 0",
      borderTop: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      textAlign: "center",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: "0 auto",
      maxWidth: "52ch",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      color: "var(--story-ink)",
      textWrap: "balance"
    }
  }, children), attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      marginTop: 14,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      letterSpacing: "0.01em"
    }
  }, attribution));
}
Object.assign(__ds_scope, { CaseQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseQuote.jsx", error: String((e && e.message) || e) }); }

// components/case/CaseSection.jsx
try { (() => {
/** A single-baseline case section: a small numeral beside its name, then content at native width —
 *  never the tone-scoped banner `StorySection` uses. The header is one line, not a persistent
 *  side-gutter running the height of the section, which is most of the density this buys back:
 *  a chapter costs a baseline, not a margin column. Content scrolls inside the section rather than
 *  the page if a nested block ever runs wider than the frame, so one over-eager grid can never turn
 *  into page-level horizontal scroll. */
function CaseSection({
  tone = "paper",
  index,
  title,
  id,
  scrollMarginTop = 56,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      scrollMarginTop,
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      padding: "var(--section-pad-tight) var(--gutter)",
      ...style
    },
    ...rest
  }, (index || title) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: 12,
      marginBottom: "var(--space-7)"
    }
  }, index && /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "0 0 auto",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, index), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: 650,
      lineHeight: 1.1,
      letterSpacing: "var(--headline-tracking)"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "100%",
      overflowX: "auto"
    }
  }, children));
}
Object.assign(__ds_scope, { CaseSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/CaseSection.jsx", error: String((e && e.message) || e) }); }

// components/case/MetricStrip.jsx
try { (() => {
/** The Overview metric row: unboxed, native width, a single ink rule above it and a hairline
 *  between cells — no ground, no shadow, no card padding. `Ledger` carries the same value-above-
 *  label instrument grammar but is a raised, labelled panel; this is deliberately the unframed
 *  opposite, because entry A's own density doctrine keeps the argument-first strip flush with the
 *  page rather than boxed like the evidence beneath it. Renders only the cells it is given — pass
 *  only entries that already carry a real number; padding a slot with an invented figure is exactly
 *  what this component exists to refuse. */
function MetricStrip({
  items = [],
  style,
  ...rest
}) {
  const count = Math.max(items.length, 1);
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "metric-strip",
    "data-count": count,
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + count + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-ink)",
      paddingTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      minWidth: 0,
      padding: i === 0 ? "0 clamp(16px,2vw,26px) 0 0" : "0 clamp(16px,2vw,26px)",
      borderLeft: i === 0 ? "none" : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginTop: 8,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, item.label))));
}
Object.assign(__ds_scope, { MetricStrip });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/MetricStrip.jsx", error: String((e && e.message) || e) }); }

// components/case/StoryBeat.jsx
try { (() => {
/** A chapter's opening beat: a micro-label kicker, a headline claim, and a lede paragraph — the
 *  argument a `CaseSection` exists to make, stated before its evidence. Where the bakeoff's
 *  Continuum entry shares one beat label across four short paragraphs, a case document's chapters
 *  are each already a considered, separately titled beat, so this renders one beat per section
 *  rather than several stacked under a shared heading — the micro-label-over-statement grammar
 *  survives the translation; the stacking count adapts to what the chapter actually is. */
function StoryBeat({
  kicker,
  title,
  intro,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(100%, 780px)",
      ...style
    },
    ...rest
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 14px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      letterSpacing: "0.01em"
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: "var(--display-s-weight)",
      lineHeight: "var(--display-s-leading)",
      letterSpacing: "var(--display-s-tracking)",
      textWrap: "balance",
      color: "var(--story-ink)"
    }
  }, title), intro && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 680,
      margin: title ? "16px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro), children);
}
Object.assign(__ds_scope, { StoryBeat });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/case/StoryBeat.jsx", error: String((e && e.message) || e) }); }

// components/document/DocFigure.jsx
try { (() => {
/** A numbered figure or table. Two things a report needs that an exhibition does not: a number a
 *  reader can cite, and permission to break out past the reading measure — a wide table is
 *  unreadable squeezed into a prose column.
 *
 *  Any of the system's instruments can sit inside one. In exhibition mode an instrument IS the
 *  moment; here it is evidence with a number attached, which is the whole difference in register. */
function DocFigure({
  kind = "figure",
  caption,
  source,
  wide = false,
  children,
  id,
  style,
  ...rest
}) {
  const isTable = kind === "table";
  return /*#__PURE__*/React.createElement("figure", {
    id: id,
    className: isTable ? "ds-doc-table" : "ds-doc-figure",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: wide ? "none" : "var(--doc-measure)",
      marginLeft: wide ? "0" : "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto"
    }
  }, children), /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginTop: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: isTable ? "ds-doc-tablabel" : "ds-doc-figlabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, caption), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "break-word"
    }
  }, source))));
}
Object.assign(__ds_scope, { DocFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/DocFigure.jsx", error: String((e && e.message) || e) }); }

// components/document/Endnotes.jsx
try { (() => {
/** Endnotes and references — the two things that let a reader check you.
 *
 *  `NoteRef` marks a claim in the prose; `Endnotes` lists them at the end. The numbering is a CSS
 *  counter shared with the marks, so a note can never be numbered wrong. References are separate
 *  because they are a different kind of object: a note qualifies a claim, a reference names a source.
 *  A report that measures things needs both, and their absence is what makes an argument look like
 *  an assertion. */
function NoteRef({
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-noteref",
    "aria-hidden": "true",
    style: style,
    ...rest
  });
}
function Endnotes({
  notes = [],
  label = "Notes",
  style,
  ...rest
}) {
  if (!notes.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    style: {
      marginTop: "var(--space-11)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ol", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      padding: "9px 0",
      borderBottom: i === notes.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, i + 1), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5
    }
  }, n)))));
}
function References({
  items = [],
  label = "References",
  style,
  ...rest
}) {
  if (!items.length) return null;
  return /*#__PURE__*/React.createElement("section", {
    className: "ds-doc-refs",
    style: {
      marginTop: "var(--space-10)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: "var(--space-5)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("ul", {
    style: {
      maxWidth: "var(--doc-measure)",
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "9px 0",
      borderBottom: i === items.length - 1 ? 0 : "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.5,
      textIndent: "-1.2em",
      paddingLeft: "1.2em"
    }
  }, r.href ? /*#__PURE__*/React.createElement("a", {
    href: r.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "inherit",
      textDecoration: "none",
      borderBottom: "1px solid var(--story-accent)"
    }
  }, r.text) : r.text))));
}
Object.assign(__ds_scope, { NoteRef, Endnotes, References });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Endnotes.jsx", error: String((e && e.message) || e) }); }

// components/document/Finding.jsx
try { (() => {
/** A finding stated twice: once in English, once in numbers.
 *
 *  A report that only speaks in measurements excludes everyone who has not read the method, and a
 *  report that only speaks in plain language cannot be checked. So both, adjacent, with the plain
 *  sentence leading — a reader travelling fast reads only those and still gets the argument; a
 *  reader who wants proof drops one line.
 *
 *  This is the receiver-side / producer-side distinction as a typographic device. "Help headings
 *  repeat themselves" is what a reader experiences. "Lexical diversity 1.45 against a 4.9–7.2 band"
 *  is what the writer did. Neither one alone is the finding. */
function Finding({
  children,
  measured,
  verdict,
  style,
  ...rest
}) {
  const tone = verdict === "noise" ? "var(--story-muted)" : verdict === "underpowered" ? "var(--story-muted)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      margin: "0 0 var(--space-6)",
      paddingLeft: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(17px,1.5vw,21px)",
      fontWeight: 620,
      lineHeight: 1.34,
      letterSpacing: "-0.032em",
      textWrap: "pretty"
    }
  }, children), (measured || verdict) && /*#__PURE__*/React.createElement("p", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "0 14px",
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700,
      lineHeight: 1.5
    }
  }, measured && /*#__PURE__*/React.createElement("span", null, measured), verdict && /*#__PURE__*/React.createElement("span", {
    style: {
      color: tone
    }
  }, verdict)));
}
Object.assign(__ds_scope, { Finding });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Finding.jsx", error: String((e && e.message) || e) }); }

// components/document/PaperTable.jsx
try { (() => {
/** A citation or URL in `source` is a long, unbroken run of characters, and the box it sits in narrows
 *  with the viewport. A blunt `word-break: break-word` last-resort breaker WILL split that run once it
 *  stops fitting its line -- including mid-letter, with nothing at the split to explain it ("th" / "is"
 *  torn across a line break). A real URL already carries its own natural break points (/, _, ., :, ?,
 *  &, =, ,); <wbr/> offers those to the browser as OPTIONAL line-break opportunities -- invisible in
 *  the rendered page, absent from a copy-paste, and only ever used when a break is actually needed --
 *  so a long source wraps at its own punctuation instead of fracturing an arbitrary character run.
 *  Ordinary short source text is untouched: it already wraps at its spaces like any other prose. */
const URL_BREAK_CHARS = /([/_.:?&=,])/;
function withBreakOpportunities(text) {
  if (typeof text !== "string") return text;
  return text.split(URL_BREAK_CHARS).map((part, i) => i % 2 === 1 ? /*#__PURE__*/React.createElement(React.Fragment, {
    key: i
  }, part, /*#__PURE__*/React.createElement("wbr", null)) : part);
}

/** A table set the way papers set tables: rules, not boxes.
 *
 *  A heavy rule above the header, a hairline under it, a heavy rule under the last row, and nothing
 *  else — no vertical rules, no cell borders, no fill. This is the booktabs convention and it is the
 *  single strongest visual signal that a page is a paper rather than an interface. Boxed grids read
 *  as UI; ruled tables read as evidence.
 *
 *  The caption sits ABOVE, which is also the convention and is genuinely better: a reader meets the
 *  claim before the numbers instead of after them. Figures caption below; tables caption above. */
function PaperTable({
  caption,
  source,
  columns = [],
  rows = [],
  align = [],
  note,
  style,
  ...rest
}) {
  const cellAlign = i => align[i] || (i === 0 ? "left" : "right");
  return /*#__PURE__*/React.createElement("figure", {
    className: "ds-doc-table",
    style: {
      margin: "var(--space-8) 0",
      maxWidth: "none",
      ...style
    },
    ...rest
  }, caption && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      marginBottom: 11
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-tablabel",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.45
    }
  }, caption)), /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))"
    }
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      borderCollapse: "collapse",
      fontFamily: "var(--font-sans)",
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      padding: "9px 14px 8px 0",
      textAlign: cellAlign(i),
      verticalAlign: "bottom",
      borderTop: "2px solid var(--story-ink)",
      borderBottom: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap"
    }
  }, c)))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r
  }, row.map((v, c) => /*#__PURE__*/React.createElement("td", {
    key: c,
    style: {
      padding: "8px 14px 8px 0",
      textAlign: cellAlign(c),
      verticalAlign: "baseline",
      borderBottom: r === rows.length - 1 ? "2px solid var(--story-ink)" : "none",
      color: c === 0 ? "var(--story-ink)" : "var(--story-muted)",
      fontSize: 13,
      lineHeight: 1.5,
      fontWeight: c === 0 ? 560 : 400
    }
  }, v))))))), (note || source) && /*#__PURE__*/React.createElement("div", {
    style: {
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: 9
    }
  }, note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45
    }
  }, note), source && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: note ? "5px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      wordBreak: "normal",
      overflowWrap: "normal"
    }
  }, withBreakOpportunities(source))));
}
Object.assign(__ds_scope, { PaperTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/PaperTable.jsx", error: String((e && e.message) || e) }); }

// components/document/Report.jsx
try { (() => {
/** The document register: a report, not an exhibition.
 *
 *  One column at a reading measure, a title block that states what this is and when it was made,
 *  and an optional abstract. Everything inside is numbered and citable. The running head is fixed
 *  at the top so a reader twenty screens down still knows what they are reading — and it is marked
 *  `ds-doc-noprint`, because on paper the page header does that job. */
function Report({
  title,
  plain,
  subtitle,
  author,
  date,
  status,
  abstract,
  runningHead,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    className: "ds-doc",
    style: {
      background: "var(--paper)",
      color: "var(--ink)",
      ...style
    },
    ...rest
  }, (runningHead || title) && /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-noprint",
    style: {
      position: "sticky",
      zIndex: 40,
      top: 0,
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "9px var(--doc-gutter)",
      borderBottom: "var(--doc-rule) solid var(--story-line)",
      background: "rgba(248,243,232,0.94)",
      backdropFilter: "blur(14px)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("span", null, runningHead || title), status && /*#__PURE__*/React.createElement("span", null, status)), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "clamp(34px,5vw,72px) var(--doc-gutter) clamp(48px,6vw,96px)"
    }
  }, /*#__PURE__*/React.createElement("header", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginBottom: "var(--space-10)"
    }
  }, /*#__PURE__*/React.createElement("h1", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,3.1vw,42px)",
      fontWeight: 700,
      lineHeight: 1.08,
      letterSpacing: "-0.045em",
      textWrap: "balance"
    }
  }, title), subtitle && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em"
    }
  }, subtitle), (author || date) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "clamp(16px,2.4vw,34px)",
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-4)",
      borderTop: "var(--doc-rule) solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, author && /*#__PURE__*/React.createElement("span", null, author), date && /*#__PURE__*/React.createElement("span", null, date)), plain && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 10,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "In plain terms"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(18px,1.6vw,23px)",
      fontWeight: 620,
      lineHeight: 1.36,
      letterSpacing: "-0.033em",
      textWrap: "pretty"
    }
  }, plain)), abstract && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: "var(--space-7)",
      paddingTop: "var(--space-6)",
      borderTop: "var(--doc-rule) solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Method and scope"), /*#__PURE__*/React.createElement("div", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, abstract))), /*#__PURE__*/React.createElement("div", {
    className: "ds-doc-body"
  }, children)));
}
Object.assign(__ds_scope, { Report });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/Report.jsx", error: String((e && e.message) || e) }); }

// components/document/ReportSection.jsx
try { (() => {
/** A numbered, citable section. The number hangs in the gutter — the same anatomy the evidence
 *  components use for their labels, so document mode reads as the same system rather than a
 *  different one. Numbering is done by CSS counters, so inserting a section never renumbers by hand
 *  and a section can never claim a number it does not have. */
function ReportSection({
  level = 1,
  title,
  children,
  id,
  style,
  ...rest
}) {
  const sub = level > 1;
  const Heading = sub ? "h3" : "h2";
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    className: sub ? "ds-doc-subsection" : "ds-doc-section",
    style: {
      marginTop: sub ? "var(--space-9)" : "var(--space-11)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--doc-gutter) minmax(0,1fr)",
      gap: "clamp(10px,1.4vw,20px)",
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-doc-num",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: sub ? 10 : 11,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }), /*#__PURE__*/React.createElement(Heading, {
    style: {
      margin: 0,
      maxWidth: "var(--doc-measure)",
      fontFamily: "var(--font-sans)",
      fontSize: sub ? "clamp(16px,1.35vw,19px)" : "clamp(20px,1.9vw,26px)",
      fontWeight: sub ? 650 : 700,
      lineHeight: 1.2,
      letterSpacing: "-0.035em",
      textWrap: "balance"
    }
  }, title)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "var(--doc-measure)",
      marginLeft: "calc(var(--doc-gutter) + clamp(10px,1.4vw,20px))",
      marginTop: "var(--space-5)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: "var(--doc-lead)",
      letterSpacing: "var(--prose-tracking)"
    }
  }, children));
}
Object.assign(__ds_scope, { ReportSection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/document/ReportSection.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ArtifactFigure.jsx
try { (() => {
/** Framed artifact with a gutter-aligned provenance caption. No height floor by default — the frame is
 *  the artifact's own edge, so a short artifact gets a short frame. Pass `minHeight` only when a set of
 *  figures genuinely has to align to each other. */
function ArtifactFigure({
  src,
  alt = "",
  label,
  caption,
  contain = false,
  zoom = false,
  minHeight,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    className: zoom ? "ds-figure-zoom" : undefined,
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight,
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--surface-mat)",
      boxShadow: "var(--story-shadow)",
      padding: contain ? "clamp(14px,1.8vw,24px)" : 0
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: src,
    alt: alt,
    style: {
      width: "100%",
      height: minHeight ? "100%" : "auto",
      minHeight: minHeight ? minHeight - 40 : undefined,
      display: "block",
      objectFit: contain ? "contain" : "cover"
    }
  })), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { ArtifactFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ArtifactFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/BeforeAfter.jsx
try { (() => {
/** A rewrite delivered as a diff with its measurements attached, so the person deciding whether to
 *  take it can see what moved and by how much. The deltas are the argument — without them this is
 *  just someone else's preferred wording. */
function BeforeAfter({
  before,
  after,
  beforeLabel = "Before",
  afterLabel = "After",
  deltas = [],
  actions,
  style,
  ...rest
}) {
  const pane = (label, text, tone) => /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "14px 16px",
      borderRight: tone === "before" ? "1px solid var(--story-line)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: tone === "after" ? "var(--story-accent)" : "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: tone === "before" ? "var(--story-muted)" : "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: tone === "after" ? 560 : 480,
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, text));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      borderBottom: "1px solid var(--story-line)"
    }
  }, pane(beforeLabel, before, "before"), pane(afterLabel, after, "after")), deltas.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(deltas.length, 1) + ",minmax(0,1fr))",
      borderBottom: actions ? "1px solid var(--story-line)" : 0
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        padding: "11px 14px",
        borderRight: i === deltas.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "block",
        marginBottom: 6,
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "baseline",
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 13,
        fontWeight: 560,
        letterSpacing: "-0.03em",
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-accent)",
        fontSize: 17,
        fontWeight: 700,
        letterSpacing: "-0.04em"
      }
    }, d.to)));
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-8)",
      alignItems: "center",
      padding: "12px 16px"
    }
  }, actions));
}
Object.assign(__ds_scope, { BeforeAfter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/BeforeAfter.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CaptureFigure.jsx
try { (() => {
/** A capture of shipped behaviour, in a frame that says what you are looking at.
 *
 *  Video is the only honest way to show an interaction that happens in time, so this plays inline —
 *  muted, looping, playsinline, with controls available. Autoplay is off by default: the accessibility
 *  floor forbids a loop that plays without the reader asking for it. */
function CaptureFigure({
  src,
  poster,
  label,
  caption,
  quote,
  autoPlay = false,
  controls = true,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      overflow: "hidden",
      border: "1px solid var(--story-rule)",
      background: "var(--ink)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("video", {
    src: src,
    poster: poster,
    muted: true,
    loop: true,
    playsInline: true,
    autoPlay: autoPlay,
    controls: controls,
    preload: "metadata",
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), quote && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "The string"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, quote)), (label || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "11px 0 0",
      color: "var(--story-muted)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      lineHeight: 1.45
    }
  }, caption)));
}
Object.assign(__ds_scope, { CaptureFigure });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CaptureFigure.jsx", error: String((e && e.message) || e) }); }

// components/evidence/CardGrid.jsx
try { (() => {
/** Hairline grid block, on the instrument grammar: an optional silkscreen header naming what the set is
 *  and how many are in it, then cells sized by their contents. Without a label it stays a bare grid —
 *  but if you cannot name the set, ask whether it needed a frame at all. */
function CardGrid({
  columns = 2,
  raised = true,
  label,
  scope,
  children,
  style,
  ...rest
}) {
  const count = React.Children.count(children);
  return /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, scope || String(count).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, children));
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DataTable.jsx
try { (() => {
/** A hyphen or dash already in the text is a standard line-break point (Unicode UAX #14) in every
 *  browser, with no special CSS involved, and MOST of the time that is correct: "help-centre" wrapping
 *  to "help-" / "centre" at a narrow column is ordinary, everyday typography — books do it every day —
 *  and gluing it shut would make the word un-searchable and un-copyable (the rendered string would
 *  carry an invisible character a reader can't see or type back). Two narrow cases are genuinely wrong
 *  rather than merely unlucky, because there the two sides of the dash are not independently meaningful
 *  words: a digit range ("2025-2026" splitting to "2025-" / "2026" reads as a typo, not a range) and a
 *  dash-joined token inside a parenthetical statistical label ("p (BH-adj)" splitting to "p (BH-" /
 *  "adj)" garbles the abbreviation). Only those two get a zero-width word joiner (U+2060) glued to the
 *  dash; every other hyphenated compound is left exactly as written. */
const DASH = "\\u002D\\u2010-\\u2015"; // hyphen-minus; hyphen through horizontal bar (covers en/em dash)
const WJ = "⁠"; // word joiner — zero-width, forbids a line break at this point, otherwise invisible
const DIGIT_RANGE_DASH = new RegExp("(\\d)([" + DASH + "])(?=\\d)", "g");
const PAREN_GROUP = /\(([^()]*)\)/g;
const DASH_IN_PAREN = new RegExp("([" + DASH + "])(?=\\S)", "g");
const glueDashes = v => {
  if (typeof v !== "string") return v;
  const rangedNumbers = v.replace(DIGIT_RANGE_DASH, "$1$2" + WJ);
  return rangedNumbers.replace(PAREN_GROUP, (m, inner) => "(" + inner.replace(DASH_IN_PAREN, "$1" + WJ) + ")");
};

/** Ruled table. Ink header band, zebra rows, an accent-tinted cell for the finding. */
function DataTable({
  columns = [],
  rows = [],
  variant = "meta",
  rowHeader = true,
  style,
  ...rest
}) {
  const cell = {
    padding: variant === "result" ? "13px 14px" : "11px 14px",
    borderRight: "1px solid var(--story-line)",
    textAlign: "left",
    verticalAlign: "baseline",
    fontFamily: "var(--font-mono)",
    fontSize: 10,
    lineHeight: 1.4
  };
  return /*#__PURE__*/React.createElement("div", {
    style: {
      overflowX: "auto",
      border: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("table", {
    style: {
      width: "100%",
      minWidth: 620,
      borderCollapse: "collapse",
      color: "var(--story-ink)"
    }
  }, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, columns.map((c, i) => /*#__PURE__*/React.createElement("th", {
    key: i,
    scope: "col",
    style: {
      ...cell,
      padding: "9px 14px",
      background: "var(--ink)",
      color: "var(--paper-bright)",
      fontSize: 9,
      fontWeight: 700
    }
  }, glueDashes(c))))), /*#__PURE__*/React.createElement("tbody", null, rows.map((row, r) => /*#__PURE__*/React.createElement("tr", {
    key: r,
    style: {
      background: r % 2 ? "var(--story-band)" : "transparent",
      borderTop: "1px solid var(--story-line)"
    }
  }, row.map((value, c) => {
    const highlight = value && typeof value === "object" && value.highlight;
    const text = glueDashes(highlight ? value.value : value);
    if (c === 0 && rowHeader) {
      return /*#__PURE__*/React.createElement("th", {
        key: c,
        scope: "row",
        style: {
          ...cell,
          width: "26%",
          fontFamily: "var(--font-sans)",
          fontSize: 13,
          fontWeight: 620
        }
      }, text);
    }
    return /*#__PURE__*/React.createElement("td", {
      key: c,
      style: {
        ...cell,
        ...(variant === "result" ? {
          fontFamily: "var(--font-sans)",
          fontSize: 16,
          fontWeight: 650
        } : null),
        ...(highlight ? {
          background: "var(--signal-tint)",
          color: "var(--ink)"
        } : null)
      }
    }, text);
  }))))));
}
Object.assign(__ds_scope, { DataTable });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DataTable.jsx", error: String((e && e.message) || e) }); }

// components/evidence/DisciplineVenn.jsx
try { (() => {
/** Three disciplines and the work that only exists where they overlap. Drawn as a registration
 *  diagram — hairline circles, no fills, the intersection marked the way the system marks a
 *  registration point — so it reads as a technical figure rather than a slide graphic. */
function DisciplineVenn({
  fields = [],
  intersection,
  caption,
  tone = "story",
  style,
  ...rest
}) {
  const ink = tone === "stage" ? "var(--stage-ink)" : "var(--story-ink)";
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const muted = tone === "stage" ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = tone === "stage" ? "var(--signal-stage)" : "var(--story-accent)";

  /* Three circles on an equilateral arrangement, centred on 150,142. */
  const circles = [{
    cx: 150,
    cy: 96,
    label: fields[0]
  }, {
    cx: 111,
    cy: 164,
    label: fields[1]
  }, {
    cx: 189,
    cy: 164,
    label: fields[2]
  }];
  /* Labels sit outside the circle field so a long discipline name cannot crowd its neighbour. */
  const anchors = [{
    x: 150,
    y: 20,
    align: "middle"
  }, {
    x: -34,
    y: 240,
    align: "start"
  }, {
    x: 334,
    y: 240,
    align: "end"
  }];
  const leaders = [{
    x1: 150,
    y1: 27,
    x2: 150,
    y2: 36
  }, {
    x1: 62,
    y1: 233,
    x2: 84,
    y2: 213
  }, {
    x1: 238,
    y1: 233,
    x2: 216,
    y2: 213
  }];
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      margin: 0,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "center",
      padding: "var(--space-7) 0",
      border: "1px solid " + line,
      background: tone === "stage" ? "transparent" : "var(--story-raised)",
      boxShadow: tone === "stage" ? "none" : "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("svg", {
    viewBox: "-40 8 380 244",
    width: "100%",
    style: {
      maxWidth: 440,
      height: "auto",
      overflow: "visible"
    },
    role: "img",
    "aria-label": fields.join(", ") + " — " + intersection
  }, circles.map(c => /*#__PURE__*/React.createElement("circle", {
    key: c.label,
    cx: c.cx,
    cy: c.cy,
    r: "62",
    fill: "none",
    stroke: line,
    strokeWidth: "1"
  })), leaders.map((l, i) => /*#__PURE__*/React.createElement("line", {
    key: i,
    x1: l.x1,
    y1: l.y1,
    x2: l.x2,
    y2: l.y2,
    stroke: line,
    strokeWidth: "1"
  })), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "15",
    fill: "none",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "150",
    y1: "120",
    x2: "150",
    y2: "164",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("line", {
    x1: "128",
    y1: "142",
    x2: "172",
    y2: "142",
    stroke: accent,
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: "150",
    cy: "142",
    r: "3.5",
    fill: accent
  }), circles.map((c, i) => /*#__PURE__*/React.createElement("text", {
    key: c.label,
    x: anchors[i].x,
    y: anchors[i].y,
    textAnchor: anchors[i].align,
    fill: muted,
    style: {
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, c.label)))), (intersection || caption) && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "13px 0 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.4
    }
  }, "Overlap"), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, intersection && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, intersection), caption && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: intersection ? "8px 0 0" : 0,
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, caption))));
}
Object.assign(__ds_scope, { DisciplineVenn });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/DisciplineVenn.jsx", error: String((e && e.message) || e) }); }

// components/evidence/EvidenceTrace.jsx
try { (() => {
/** The audit trail a governed answer carries: what was loaded, what was queried, what was measured,
 *  what changed, and what it cited. Ordered, numbered, and readable by someone who was not there —
 *  which is the whole difference between a governed answer and a confident one. */
function EvidenceTrace({
  steps = [],
  label = "Evidence trace",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, steps.map((s, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    gutter: String(i + 1).padStart(2, "0"),
    last: i === steps.length - 1,
    as: "li"
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.15,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, s.step), s.detail && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, s.detail)))));
}
Object.assign(__ds_scope, { EvidenceTrace });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/EvidenceTrace.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FactsPanel.jsx
try { (() => {
/** Contribution facts, as the document's masthead strip: mono label in the gutter, the claim at fact
 *  size beside it. Padding is the instrument's — the strip is as tall as the longest claim, not a band
 *  of reserved air across the top of the page. */
function FactsPanel({
  facts = [],
  columns = "0.9fr 1.1fr",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "facts-panel",
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      ...style
    },
    ...rest
  }, facts.map((fact, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      padding: "var(--panel-pad) 0",
      borderRight: i === facts.length - 1 ? 0 : "1px solid var(--ink)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "0 12px 0 clamp(20px,2.4vw,34px)",
      color: "var(--signal)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, fact.label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 620,
      margin: 0,
      paddingRight: "clamp(20px,2.4vw,34px)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--fact-size)",
      fontWeight: 560,
      lineHeight: 1.18,
      letterSpacing: "-0.045em"
    }
  }, fact.value))));
}
Object.assign(__ds_scope, { FactsPanel });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FactsPanel.jsx", error: String((e && e.message) || e) }); }

// components/evidence/FingerprintBars.jsx
try { (() => {
/** A measured voice fingerprint: one row per dimension, the reading drawn against the band a
 *  surface is allowed to sit in. The band is the point — a fingerprint is a tolerance, not a
 *  score, and a reading outside it is the only thing that counts as a miss. */
function FingerprintBars({
  dimensions = [],
  scale = 100,
  showBand = true,
  style,
  ...rest
}) {
  const pct = n => Math.max(0, Math.min(100, n / scale * 100));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, dimensions.map((d, i) => {
    const out = showBand && d.band && (d.value < d.band[0] || d.value > d.band[1]);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        display: "grid",
        gridTemplateColumns: "minmax(96px,0.9fr) minmax(0,2.4fr) 42px",
        gap: 14,
        alignItems: "center",
        padding: "9px 16px",
        borderBottom: i === dimensions.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700,
        lineHeight: 1.3
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        position: "relative",
        height: 9,
        display: "block",
        borderBottom: "1px solid var(--story-line)"
      }
    }, showBand && d.band && /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 0,
        bottom: 0,
        left: pct(d.band[0]) + "%",
        width: pct(d.band[1]) - pct(d.band[0]) + "%",
        background: "var(--story-band)",
        borderLeft: "1px solid var(--story-line)",
        borderRight: "1px solid var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        top: 2,
        bottom: 2,
        left: 0,
        width: pct(d.value) + "%",
        background: out ? "var(--story-accent)" : "var(--story-ink)"
      }
    })), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: out ? "var(--story-accent)" : "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 14,
        fontWeight: 700,
        letterSpacing: "-0.04em",
        textAlign: "right",
        fontVariantNumeric: "tabular-nums"
      }
    }, d.value));
  }));
}
Object.assign(__ds_scope, { FingerprintBars });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/FingerprintBars.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GateRun.jsx
try { (() => {
/** What somebody has to get through before they reach the thing they came for.
 *
 *  A figure on the left, numbered gates across the middle, and the destination on the right — drawn as a
 *  run rather than a list, because the point is the distance, not the items. Each gate can carry a cost,
 *  and the run states its total, which is usually the whole argument. */
function GateRun({
  figure,
  figureAlt = "",
  gates = [],
  destination,
  destinationNote,
  total,
  label = "The run",
  tone = "story",
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const accent = stage ? "var(--signal-stage)" : "var(--story-accent)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 16,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), total && /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, total)), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      alignItems: "stretch"
    }
  }, figure && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "flex-end",
      justifyContent: "center",
      flex: "0 0 auto",
      width: 120,
      padding: "12px 10px 0",
      borderRight: "1px solid " + line
    }
  }, /*#__PURE__*/React.createElement("img", {
    src: figure,
    alt: figureAlt,
    style: {
      width: "100%",
      height: "auto",
      display: "block"
    }
  })), gates.map((gate, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "space-between",
      gap: 14,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 14px",
      borderRight: "1px solid " + line,
      overflowWrap: "break-word"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.03em"
    }
  }, gate.label)), gate.cost && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, gate.cost))), destination && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 6,
      flex: "1 1 0",
      minWidth: 0,
      padding: "13px 16px",
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: ink,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 700,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, destination), destinationNote && /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.35,
      overflowWrap: "break-word"
    }
  }, destinationNote))));
}
Object.assign(__ds_scope, { GateRun });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GateRun.jsx", error: String((e && e.message) || e) }); }

// components/evidence/GridCard.jsx
try { (() => {
/** One ruled cell, sized by its content: mono label in a left gutter, everything else in the content
 *  column. No height floor — a short cell is short. The padding is an instrument's, not a page margin's. */
function GridCard({
  label,
  metric,
  title,
  body,
  code,
  minHeight,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter-tight) minmax(0,1fr)",
      minHeight,
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "var(--card-pad) 12px var(--card-pad) 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "var(--card-pad) var(--card-pad-wide)"
    }
  }, metric && /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      marginBottom: 5,
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)"
    }
  }, metric), code && /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      marginBottom: 9,
      padding: "8px 10px",
      background: "var(--story-band)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.5,
      whiteSpace: "normal"
    }
  }, code), title && /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.1,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, title), body && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: title || metric || code ? "6px 0 0" : 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, body), children));
}
Object.assign(__ds_scope, { GridCard });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/GridCard.jsx", error: String((e && e.message) || e) }); }

// components/evidence/IndexRows.jsx
try { (() => {
/** Rows from a content index, shown as the schema rather than the contents: a versioned record id,
 *  the thing it asserts, and the governance metadata that lets a tool route it. Publishing the shape
 *  discloses nothing and is the part worth reading anyway. */
function IndexRows({
  rows = [],
  label = "Record shape",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement(__ds_scope.Panel, {
    label: label,
    flush: true,
    style: {
      overflowX: "auto",
      ...style
    },
    ...rest
  }, rows.map((r, i) => /*#__PURE__*/React.createElement(__ds_scope.PanelRow, {
    key: i,
    last: i === rows.length - 1
  }, /*#__PURE__*/React.createElement("code", {
    style: {
      display: "block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      lineHeight: 1.45,
      wordBreak: "break-word"
    }
  }, r.id), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.asserts), r.meta && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: 14,
      marginTop: 9
    }
  }, r.meta.map(m => /*#__PURE__*/React.createElement("span", {
    key: m,
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, m))))));
}
Object.assign(__ds_scope, { IndexRows });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/IndexRows.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Label above value, so a cell is only as wide as the wider of the two and a row of four fits without
 *  stretching. Side-by-side reads fine until a value is a word rather than a number, at which point the
 *  cells go ragged and the label and the number end up at opposite ends of a void. This is the same
 *  stacking `VoiceProfile` uses, which is the system's answer for a set of small readings. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: "0 1 auto",
      padding: "12px 18px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Outcome.jsx
try { (() => {
/** Amber label over a short results list or paragraph. Sits at the end of a stage copy column. */
function Outcome({
  label = "Results",
  items,
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: "min(430px,88%)",
      marginTop: 18,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--signal-stage)",
      fontFamily: "var(--font-sans)",
      fontSize: 11,
      fontWeight: 700
    }
  }, label), items && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: "5px 0 0",
      padding: 0,
      listStyle: "none",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("li", {
    key: i
  }, item))), children && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "5px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, children));
}
Object.assign(__ds_scope, { Outcome });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Outcome.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Pipeline.jsx
try { (() => {
/** Ordered process rows. Step number in the gutter, the rule on one line — compressed, not spaced out. */
function Pipeline({
  steps = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("ol", {
    style: {
      margin: 0,
      padding: 0,
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      listStyle: "none",
      ...style
    },
    ...rest
  }, steps.map((step, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === steps.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "14px 12px 14px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, step.step || String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "13px 18px",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(15px,1.15vw,17px)",
      fontWeight: 560,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, step.label))));
}
Object.assign(__ds_scope, { Pipeline });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Pipeline.jsx", error: String((e && e.message) || e) }); }

// components/evidence/PullQuote.jsx
try { (() => {
/** A quote as a specimen, not a slab.
 *
 *  The old plate was a large reversed field with the line floating somewhere inside it — presence bought
 *  with area. This buys it with register instead: a silkscreen header naming what the line is, the quote
 *  set on the ink ground at its own size, and the frame stopping where the words stop. Attribution moves
 *  to the header, where an instrument puts its label, rather than a second bar underneath. */
function PullQuote({
  children,
  attribution,
  on = "ink",
  style,
  ...rest
}) {
  const dark = on === "ink";
  const ground = dark ? "var(--ink)" : "var(--stage)";
  const type = dark ? "var(--paper-bright)" : "var(--stage-ink)";
  const line = dark ? "var(--dark-line)" : "var(--stage-line)";
  const meta = dark ? "var(--paper-dim)" : "var(--stage-muted)";
  return /*#__PURE__*/React.createElement("figure", {
    style: {
      /* Sized by the words, not by a column. A quote that wraps short leaves no dead ground. */
      width: "fit-content",
      maxWidth: 560,
      margin: "clamp(32px,4vw,52px) 0 0",
      border: "1px solid " + ground,
      background: ground,
      color: type,
      boxShadow: dark ? "var(--shadow-3)" : "var(--shadow-on-stage-2)",
      ...style
    },
    ...rest
  }, attribution && /*#__PURE__*/React.createElement("figcaption", {
    style: {
      display: "flex",
      gap: 9,
      alignItems: "center",
      padding: "10px 20px",
      borderBottom: "1px solid " + line,
      color: meta,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      flex: "0 0 6px",
      background: "var(--signal-lift)"
    }
  }), attribution), /*#__PURE__*/React.createElement("blockquote", {
    style: {
      margin: 0,
      padding: "17px 20px 19px",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--quote-size)",
      fontWeight: "var(--quote-weight)",
      lineHeight: "var(--quote-leading)",
      letterSpacing: "var(--quote-tracking)",
      textWrap: "balance"
    }
  }, children));
}
Object.assign(__ds_scope, { PullQuote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/PullQuote.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResearchGrid.jsx
try { (() => {
/** Published sources behind a measurement, one row each: who, when, and the thing it makes computable.
 *  A citation row is the difference between an instrument and an opinion, so the source is never
 *  paraphrased away — it stays attributable. */
function ResearchGrid({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    style: {
      padding: "15px 18px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, item.source), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, item.year)), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "9px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.12,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, item.gives), item.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.note))));
}
Object.assign(__ds_scope, { ResearchGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResearchGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResumeEntry.jsx
try { (() => {
/** One record in a résumé timeline: dates in the label gutter, org and role, then what I did there. */
function ResumeEntry({
  when,
  org,
  role,
  bullets = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("article", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      gap: "clamp(16px,2vw,28px)",
      padding: "var(--space-7) 0",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      paddingTop: 3,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.45
    }
  }, when), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: 0,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-s-size)",
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "var(--display-s-tracking)"
    }
  }, org), role && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "9px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 15,
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "-0.03em"
    }
  }, role), bullets.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 11,
      margin: "var(--space-5) 0 0",
      padding: 0,
      listStyle: "none"
    }
  }, bullets.map((b, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "14px minmax(0,1fr)",
      gap: 10,
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--prose-size)",
      lineHeight: 1.45,
      letterSpacing: "-0.025em",
      textWrap: "pretty"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      marginTop: "0.62em",
      width: 7,
      height: 1,
      background: "var(--story-accent)"
    }
  }), b)))));
}
Object.assign(__ds_scope, { ResumeEntry });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResumeEntry.jsx", error: String((e && e.message) || e) }); }

// components/evidence/RuleSplit.jsx
try { (() => {
/** Two-column sort of a guidance set: what a model has to judge, and what code can check.
 *  The whole argument for a measurement layer fits in one of these — a reader can see the
 *  proportion before reading a single row. */
function RuleSplit({
  modelLabel = "Model judgment",
  codeLabel = "Checkable in code",
  rules = [],
  note,
  style,
  ...rest
}) {
  const model = rules.filter(r => r.kind === "model");
  const code = rules.filter(r => r.kind === "code");
  const column = (label, items, accent) => /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      borderRight: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "10px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.rule))));
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      border: "1px solid var(--story-line)",
      borderRight: 0,
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, column(modelLabel, model, "var(--story-muted)"), column(codeLabel, code, "var(--story-accent)")), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, note));
}
Object.assign(__ds_scope, { RuleSplit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/RuleSplit.jsx", error: String((e && e.message) || e) }); }

// components/evidence/SourceMap.jsx
try { (() => {
/** Ruled inventory of inputs, sized by what it names.
 *
 *  Each entry is as wide as its own number-and-name pair and wraps when it runs out of room. An entry
 *  does not stretch to half the page and then sit alone at the left edge of it. */
function SourceMap({
  items = [],
  label = "Inputs",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-rule)",
      borderLeft: "1px solid var(--story-rule)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 24,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("span", {
    key: i,
    style: {
      display: "flex",
      gap: 12,
      alignItems: "baseline",
      flex: "0 1 auto",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-rule)",
      borderBottom: "1px solid var(--story-rule)"
    }
  }, /*#__PURE__*/React.createElement("i", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontStyle: "normal",
      fontVariantNumeric: "tabular-nums"
    }
  }, String(i + 1).padStart(2, "0")), /*#__PURE__*/React.createElement("b", {
    style: {
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 600,
      lineHeight: 1.3,
      letterSpacing: "-0.025em"
    }
  }, item)))));
}
Object.assign(__ds_scope, { SourceMap });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/SourceMap.jsx", error: String((e && e.message) || e) }); }

// components/evidence/TenGrid.jsx
try { (() => {
/** Ten hairline cells; filled cells read as passes. A dimension meter, not a progress bar. */
function TenGrid({
  total = 10,
  pass = 0,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + total + ",1fr)",
      gap: 4,
      marginTop: "var(--space-5)",
      ...style
    },
    ...rest
  }, Array.from({
    length: total
  }).map((_, i) => /*#__PURE__*/React.createElement("i", {
    key: i,
    style: {
      height: 10,
      border: "1px solid " + (i < pass ? "var(--story-accent)" : "var(--story-muted)"),
      background: i < pass ? "var(--story-accent)" : "transparent"
    }
  })));
}
Object.assign(__ds_scope, { TenGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/TenGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Toolchain.jsx
try { (() => {
/** What a system was actually built out of, named and linked so a reader can go and check.
 *
 *  The engineering counterpart to `ResearchGrid`: same anatomy, same register. Deliberately no logos —
 *  a row of third-party brand marks would be the one place this system's ink-and-hairline language
 *  breaks, and it is the oldest tell in portfolio design. A name, a licence, and what it actually
 *  provides is more credible than a badge, because it can be verified. */
function Toolchain({
  items = [],
  label = "Built with",
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: item.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(140px,0.9fr) minmax(0,2fr) auto",
      gap: "clamp(12px,2vw,26px)",
      alignItems: "baseline",
      padding: "12px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, item.href ? /*#__PURE__*/React.createElement("a", {
    href: item.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em",
      textDecoration: "none",
      borderBottom: "1px solid currentColor",
      width: "fit-content",
      paddingBottom: 1
    }
  }, item.name) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em"
    }
  }, item.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.provides), item.meta && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      textAlign: "right"
    }
  }, item.meta)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 640,
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45
    }
  }, note));
}
Object.assign(__ds_scope, { Toolchain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Toolchain.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceProfile.jsx
try { (() => {
/** The target a surface is written to, published so a team can argue with it: each dimension's
 *  setting and the one sentence saying why it sits there. A profile without its reasoning is a
 *  number nobody will honour. */
function VoiceProfile({
  product,
  surface,
  dimensions = [],
  notes = [],
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "Voice profile"), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, [product, surface].filter(Boolean).join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(auto-fit,minmax(128px,1fr))",
      borderBottom: notes.length ? "1px solid var(--story-line)" : 0
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      padding: "13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 8,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value), /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      height: 3,
      display: "block",
      marginTop: 9,
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: d.value + "%",
      background: "var(--story-accent)"
    }
  }))))), notes.length > 0 && /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, notes.map((n, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: i === notes.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "11px 12px 11px 16px",
      borderRight: "1px solid var(--story-line)",
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, n.name), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      padding: "10px 18px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, n.why)))));
}
Object.assign(__ds_scope, { VoiceProfile });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceProfile.jsx", error: String((e && e.message) || e) }); }

// components/evidence/VoiceScoreRing.jsx
try { (() => {
/** How a reading reaches the person who has to act on it: one alignment figure, the dimensions
 *  underneath it, and a verdict in words. The arc draws itself on mount rather than fading in —
 *  a measurement should look like it was taken, not like it was already there. */
function VoiceScoreRing({
  score = 0,
  outOf = 100,
  label = "Voice alignment",
  verdict,
  scope = [],
  dimensions = [],
  size = 148,
  style,
  ...rest
}) {
  const [drawn, setDrawn] = React.useState(false);
  React.useEffect(() => {
    /* Two frames, so the browser paints the zero state before the arc animates to its value.
       Both ids are tracked — cancelling only the outer one leaves the inner frame live. */
    let inner;
    const outer = requestAnimationFrame(() => {
      inner = requestAnimationFrame(() => setDrawn(true));
    });
    return () => {
      cancelAnimationFrame(outer);
      if (inner) cancelAnimationFrame(inner);
    };
  }, []);
  const r = (size - 18) / 2;
  const c = 2 * Math.PI * r;
  const filled = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope.length > 0 && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, scope.join(" · "))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(18px,2.4vw,32px)",
      alignItems: "center",
      padding: "18px 16px"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: "1"
  }), /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: "7",
    strokeLinecap: "butt",
    strokeDasharray: c,
    strokeDashoffset: drawn ? c * (1 - filled) : c,
    style: {
      transition: "stroke-dashoffset var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      alignItems: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-xl-size)",
      fontWeight: 700,
      lineHeight: 0.85,
      letterSpacing: "var(--metric-xl-tracking)",
      fontVariantNumeric: "tabular-nums"
    }
  }, score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, "of ", outOf))), /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0
    }
  }, verdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 13px",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.18,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, verdict), /*#__PURE__*/React.createElement("div", {
    style: {
      borderTop: "1px solid var(--story-line)"
    }
  }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(80px,1fr) minmax(0,1.6fr) 34px",
      gap: 12,
      alignItems: "center",
      padding: "7px 0",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 5,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 auto 0 0",
      width: (drawn ? d.value : 0) + "%",
      background: "var(--story-ink)",
      transition: "width var(--dur-character) var(--ease-authored)"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, d.value)))))));
}
Object.assign(__ds_scope, { VoiceScoreRing });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/VoiceScoreRing.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Eyebrow.jsx
try { (() => {
const TONES = {
  muted: "var(--story-muted, var(--muted-ink))",
  signal: "var(--signal)",
  action: "var(--signal-stage)",
  stage: "var(--stage-muted)"
};

/** Small mono label that opens a scene, a section, or a card. */
function Eyebrow({
  tone = "muted",
  size = "eyebrow",
  as = "p",
  children,
  style,
  ...rest
}) {
  const sizes = {
    eyebrow: {
      fontSize: "var(--eyebrow-size)",
      fontWeight: "var(--eyebrow-weight)",
      letterSpacing: "var(--eyebrow-tracking)",
      fontFamily: "var(--font-sans)"
    },
    meta: {
      fontSize: "var(--meta-size)",
      fontWeight: "var(--meta-weight)",
      letterSpacing: "-0.01em",
      fontFamily: "var(--font-mono)"
    },
    micro: {
      fontSize: "var(--micro-size)",
      fontWeight: "var(--micro-weight)",
      letterSpacing: "0",
      fontFamily: "var(--font-mono)"
    }
  };
  const Tag = as;
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      margin: 0,
      lineHeight: "var(--eyebrow-leading)",
      color: TONES[tone],
      ...sizes[size],
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Eyebrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Eyebrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/LineArrow.jsx
try { (() => {
/** The drawn arrow glyph: 1px rule plus a rotated corner. Inherits currentColor. */
function LineArrow({
  reverse = false,
  width = 34,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "relative",
      display: "inline-block",
      flex: "0 0 " + width + "px",
      width,
      height: 12,
      transform: reverse ? "rotate(180deg)" : undefined,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 5,
      right: 0,
      left: 0,
      height: 1,
      background: "currentColor"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 2,
      right: 0,
      width: 7,
      height: 7,
      borderTop: "1px solid currentColor",
      borderRight: "1px solid currentColor",
      transform: "rotate(45deg)"
    }
  }));
}
Object.assign(__ds_scope, { LineArrow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/LineArrow.jsx", error: String((e && e.message) || e) }); }

// components/foundation/ProvenanceNote.jsx
try { (() => {
/** Hairline-topped note that states what an artifact is and is not. */
function ProvenanceNote({
  label = "Provenance",
  children,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      marginTop: "var(--space-6)",
      paddingTop: "var(--space-4)",
      borderTop: "1px solid var(--story-line)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 700,
      margin: 0,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: 12,
      lineHeight: 1.5,
      letterSpacing: "-0.015em"
    }
  }, children));
}
Object.assign(__ds_scope, { ProvenanceNote });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/ProvenanceNote.jsx", error: String((e && e.message) || e) }); }

// components/foundation/RegistrationMark.jsx
try { (() => {
/** Corner crosshair with a gold centre — the print-registration device used on every stage. */
function RegistrationMark({
  size = 22,
  color = "var(--stage-ink)",
  dot = "var(--signal-stage)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      top: 22,
      right: 22,
      width: size,
      height: size,
      opacity: 0.65,
      pointerEvents: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: size / 2 - 1,
      right: 0,
      left: 0,
      height: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      left: size / 2 - 1,
      width: 1,
      background: color
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 4,
      border: "1px solid " + color,
      borderRadius: "var(--radius-round)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 8,
      borderRadius: "var(--radius-round)",
      background: dot
    }
  }));
}
Object.assign(__ds_scope, { RegistrationMark });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/RegistrationMark.jsx", error: String((e && e.message) || e) }); }

// components/foundation/Tag.jsx
try { (() => {
/** Hairline capability tag. Square corners, no fill. */
function Tag({
  on = "stage",
  children,
  style,
  ...rest
}) {
  const border = on === "stage" ? "var(--stage-line)" : "var(--soft-line)";
  return /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "9px 11px",
      border: "1px solid " + border,
      fontFamily: "var(--font-sans)",
      fontSize: 10,
      letterSpacing: "-0.01em",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Tag });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/foundation/Tag.jsx", error: String((e && e.message) || e) }); }

// components/layout/Panel.jsx
try { (() => {
/** The instrument frame, extracted.
 *
 *  Every readout in the measurement set shares one anatomy, and it turns out to be the anatomy the
 *  whole system wanted: a silkscreen header that says what the thing is and what it is scoped to, a
 *  hairline frame, and rows that are exactly as tall as their contents.
 *
 *  The rule this encodes: a border has to earn itself. A frame is legitimate when it declares a
 *  reading — something measured, scoped, sourced, or owned. Padding around prose is not a reading, and
 *  prose should sit on the page unframed. If you cannot write the header label, you do not need the box.
 */
function Panel({
  label,
  scope,
  tone = "story",
  flush = false,
  children,
  style,
  ...rest
}) {
  const stage = tone === "stage";
  const line = stage ? "var(--stage-line)" : "var(--story-line)";
  const muted = stage ? "var(--stage-muted)" : "var(--story-muted)";
  const ink = stage ? "var(--stage-ink)" : "var(--story-ink)";
  return /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-raised)",
      boxShadow: stage ? "none" : "var(--story-shadow)",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid " + line,
      background: stage ? "transparent" : "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: muted,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), scope && /*#__PURE__*/React.createElement("span", {
    style: {
      color: ink,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      textAlign: "right"
    }
  }, Array.isArray(scope) ? scope.join(" · ") : scope)), /*#__PURE__*/React.createElement("div", {
    style: flush ? undefined : {
      padding: "14px 16px"
    }
  }, children));
}

/** One hairline-separated row inside a flush Panel. Sized by its content, never by a grid minimum.
 *  `as` sets the element, so a row inside an ordered list can be a real `li` rather than a div. */
function PanelRow({
  gutter,
  tone = "story",
  last = false,
  as = "div",
  children,
  style,
  ...rest
}) {
  const line = tone === "stage" ? "var(--stage-line)" : "var(--story-line)";
  const Tag = as;
  if (!gutter) {
    return /*#__PURE__*/React.createElement(Tag, {
      style: {
        padding: "12px 16px",
        borderBottom: last ? 0 : "1px solid " + line,
        ...style
      },
      ...rest
    }, children);
  }
  return /*#__PURE__*/React.createElement(Tag, {
    style: {
      display: "grid",
      gridTemplateColumns: "var(--label-gutter) minmax(0,1fr)",
      alignItems: "baseline",
      borderBottom: last ? 0 : "1px solid " + line,
      listStyle: "none",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      padding: "12px 12px 12px 16px",
      borderRight: "1px solid " + line,
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, gutter), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 18px"
    }
  }, children));
}
Object.assign(__ds_scope, { Panel, PanelRow });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Panel.jsx", error: String((e && e.message) || e) }); }

// components/layout/PaperFrame.jsx
try { (() => {
/** The bounded document: a paper card on a darker desk, hairline border, hard offset shadow, grain overlay. */
function PaperFrame({
  children,
  grain = true,
  columns = "var(--rail-width) minmax(0,1fr)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    style: {
      minHeight: "100%",
      background: "var(--surface-desk)",
      fontFamily: "var(--font-body)",
      color: "var(--ink)"
    }
  }, grain && /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      position: "fixed",
      zIndex: 5000,
      inset: 0,
      pointerEvents: "none",
      opacity: "var(--texture-grain-opacity)",
      mixBlendMode: "multiply",
      backgroundImage: "var(--texture-grain)"
    }
  }), /*#__PURE__*/React.createElement("div", {
    "data-ds": "frame",
    style: {
      width: "min(calc(100% - 24px)," + "var(--frame-max))",
      height: "calc(100svh - 24px)",
      minHeight: 700,
      display: "grid",
      gridTemplateColumns: columns,
      margin: "var(--frame-inset) auto",
      overflow: "hidden",
      border: "1px solid var(--ink)",
      background: "var(--paper)",
      boxShadow: "var(--shadow-4)",
      ...style
    },
    ...rest
  }, children));
}
Object.assign(__ds_scope, { PaperFrame });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/PaperFrame.jsx", error: String((e && e.message) || e) }); }

// components/layout/Stage.jsx
try { (() => {
/** The pine authored stage: gradient field, clipped, with an optional registration mark. */
function Stage({
  children,
  variant = "field",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    "data-ds": "stage",
    style: {
      position: "relative",
      zIndex: 1,
      minWidth: 0,
      minHeight: 0,
      overflow: "hidden",
      isolation: "isolate",
      background: variant === "section" ? "var(--stage-field-section)" : "var(--stage-field)",
      color: "var(--stage-ink)",
      ...style
    },
    ...rest
  }, children);
}
Object.assign(__ds_scope, { Stage });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/Stage.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCopy.jsx
try { (() => {
/** The left column of an authored scene: eyebrow, display title, statement, deck, and children. */
function StageCopy({
  eyebrow,
  title,
  accent,
  statement,
  deck,
  detail,
  children,
  width = "min(47%,560px)",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "stage-copy",
    style: {
      position: "relative",
      zIndex: 12,
      width,
      height: "100%",
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      padding: "clamp(52px,7vh,82px) clamp(18px,2.6vw,44px) clamp(38px,5vh,62px) var(--stage-pad-x)",
      ...style
    },
    ...rest
  }, eyebrow && /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    tone: "stage",
    style: {
      marginBottom: 17
    }
  }, eyebrow), title &&
  /*#__PURE__*/
  /* data-doc-heading + tabIndex={-1}: the one place per document the app moves focus to
     after a destination change (see app.jsx). Focusable without joining the tab order;
     the ring itself is drawn by :focus-visible in tokens/interaction.css, so a mouse
     click never shows one and a keyboard move always does. */
  React.createElement("h1", {
    "data-doc-heading": true,
    tabIndex: -1,
    style: {
      margin: 0,
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-xl-size)",
      fontWeight: "var(--display-xl-weight)",
      lineHeight: "var(--display-xl-leading)",
      letterSpacing: "var(--display-xl-tracking)",
      textWrap: "balance"
    }
  }, title, accent && " ", accent && /*#__PURE__*/React.createElement("em", {
    style: {
      display: "block",
      color: "var(--action)",
      fontStyle: "normal"
    }
  }, accent)), statement && /*#__PURE__*/React.createElement("h2", {
    style: {
      maxWidth: 450,
      margin: "20px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--headline-size)",
      fontWeight: "var(--headline-weight)",
      lineHeight: "var(--headline-leading)",
      letterSpacing: "var(--headline-tracking)",
      textWrap: "balance"
    }
  }, statement), deck && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "22px 0 0",
      color: "var(--stage-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, deck), detail && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 430,
      margin: "11px 0 0",
      color: "var(--stage-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-s-size)",
      lineHeight: 1.5,
      letterSpacing: "-0.025em"
    }
  }, detail), children);
}
Object.assign(__ds_scope, { StageCopy });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCopy.jsx", error: String((e && e.message) || e) }); }

// components/layout/StageCurtain.jsx
try { (() => {
const COVER = {
  forward: "stage-curtain-cover-forward",
  backward: "stage-curtain-cover-backward"
};
const REVEAL = {
  forward: "stage-curtain-reveal-forward",
  backward: "stage-curtain-reveal-backward"
};

/** The stage transition: a pine plate crosses the stage carrying the destination's name,
 *  the document swaps behind it, then it leaves the way it came. */
function StageCurtain({
  state = "idle",
  direction = "forward",
  label,
  style,
  ...rest
}) {
  const covering = state === "covering";
  const revealing = state === "revealing";
  const active = covering || revealing;
  const plate = covering ? COVER[direction] + " var(--dur-curtain-cover) var(--ease-curtain-cover) both" : revealing ? REVEAL[direction] + " var(--dur-curtain-reveal) var(--ease-authored) both" : "none";
  const text = covering ? "stage-curtain-label-in var(--dur-curtain-label-in) var(--delay-curtain-label) var(--ease-authored) both" : revealing ? "stage-curtain-label-out var(--dur-curtain-label-out) var(--ease-curtain-out) both" : "none";
  return /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    "data-ds": "stage-curtain",
    style: {
      position: "absolute",
      zIndex: 90,
      inset: 0,
      display: "grid",
      placeItems: "center",
      overflow: "hidden",
      visibility: active ? "visible" : "hidden",
      pointerEvents: "none",
      color: "var(--stage-ink)",
      background: "var(--stage-field)",
      /* The off-screen resting transform only matters while a transition is actually playing —
         `visibility: hidden` already fully hides the plate at idle, so idle can rest at
         `none` instead of parking a real, positioned box outside the viewport. That box used to
         be silently clipped by the desktop reader's `overflow: hidden`; once the phone reader
         gives that up (tokens/shell.css, needed to keep `position: sticky` tracking the real
         page scroll), the same idle box was inflating the document's own horizontal scroll
         width by exactly its own overhang. The cover/reveal keyframes define their own `from`
         transform the instant they start, so dropping the idle-only offset changes nothing
         about how a transition actually looks. */
      transform: active ? direction === "forward" ? "translate3d(104%,0,0)" : "translate3d(-104%,0,0)" : "none",
      willChange: "transform",
      animation: plate,
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      inset: 0,
      opacity: 0.22,
      background: "var(--texture-graph)",
      backgroundSize: "25% 25%"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "absolute",
      top: 0,
      bottom: 0,
      width: 13,
      left: direction === "forward" ? -13 : "auto",
      right: direction === "forward" ? "auto" : -13,
      background: "linear-gradient(90deg,var(--signal) 0 54%,var(--signal-stage) 54% 100%)",
      boxShadow: (direction === "forward" ? "-12px" : "12px") + " 0 28px rgba(4,26,22,0.42)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      zIndex: 2,
      opacity: 0,
      transform: "translateY(20px)",
      animation: text,
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(52px,8vw,128px)",
      fontWeight: 760,
      lineHeight: 0.9,
      letterSpacing: "-0.075em"
    }
  }, label));
}
Object.assign(__ds_scope, { StageCurtain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StageCurtain.jsx", error: String((e && e.message) || e) }); }

// components/layout/StorySection.jsx
try { (() => {
/** A tone-scoped chapter of a case document: index, kicker, title, intro, then content. */
function StorySection({
  tone = "paper",
  index,
  chapterLabel,
  kicker,
  title,
  intro,
  children,
  id,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("section", {
    id: id,
    "data-tone": tone,
    style: {
      display: "grid",
      gridTemplateColumns: "70px minmax(0,1fr)",
      gap: "clamp(28px,5vw,82px)",
      padding: "var(--section-pad) var(--gutter)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-bg)",
      color: "var(--story-ink)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      paddingTop: 5,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 11,
      fontWeight: 700
    }
  }, chapterLabel && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block"
    }
  }, chapterLabel), index && /*#__PURE__*/React.createElement("b", {
    style: {
      display: "block",
      color: "var(--story-accent)"
    }
  }, index)), /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 980
    }
  }, kicker && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "0 0 16px",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 10,
      fontWeight: 700
    }
  }, kicker), title && /*#__PURE__*/React.createElement("h2", {
    style: {
      margin: 0,
      fontFamily: "var(--font-sans)",
      fontSize: "var(--display-m-size)",
      fontWeight: "var(--display-m-weight)",
      lineHeight: "var(--display-m-leading)",
      letterSpacing: "var(--display-m-tracking)",
      textWrap: "balance"
    }
  }, title), intro && /*#__PURE__*/React.createElement("div", {
    style: {
      maxWidth: 760,
      marginTop: 20,
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--lede-size)",
      lineHeight: "var(--lede-leading)",
      letterSpacing: "var(--lede-tracking)"
    }
  }, intro)), /*#__PURE__*/React.createElement("div", {
    style: {
      width: "100%",
      minWidth: 0,
      gridColumn: "1 / -1",
      color: "var(--story-ink)"
    }
  }, children));
}
Object.assign(__ds_scope, { StorySection });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/layout/StorySection.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ChapterNav.jsx
try { (() => {
/** Sticky chapter map. The chapter in view inverts to an ink chip. */
function ChapterNav({
  label = "Case map",
  chapters = [],
  active,
  onSelect,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": label,
    "data-ds": "chapter-nav",
    style: {
      position: "sticky",
      zIndex: 45,
      top: 0,
      minHeight: "var(--chapter-nav-height)",
      display: "grid",
      gridTemplateColumns: "auto minmax(0,1fr)",
      gap: "clamp(22px,3vw,48px)",
      alignItems: "center",
      padding: "var(--space-2) var(--gutter-tight)",
      borderTop: "1px solid var(--ink)",
      borderBottom: "1px solid var(--ink)",
      background: "rgba(248,243,232,0.96)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      backdropFilter: "blur(14px)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 760,
      whiteSpace: "nowrap"
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    "data-ds": "chapter-nav-track",
    style: {
      minWidth: 0,
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(chapters.length, 1) + ",minmax(74px,1fr))",
      gap: 4
    }
  }, chapters.map(c => {
    const on = c.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: c.id,
      type: "button",
      className: "ds-tab ds-tab-chapter",
      onClick: () => onSelect && onSelect(c.id),
      "aria-current": on ? "true" : undefined,
      style: {
        position: "relative",
        minHeight: 34,
        padding: "0 8px",
        border: 0,
        background: on ? "var(--ink)" : "transparent",
        color: on ? "var(--paper-bright)" : "var(--muted-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11,
        fontWeight: 600,
        letterSpacing: "-0.02em",
        cursor: "pointer",
        transition: "background var(--dur-fast) var(--ease-authored), color var(--dur-fast) var(--ease-authored)"
      }
    }, c.label);
  })));
}
Object.assign(__ds_scope, { ChapterNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ChapterNav.jsx", error: String((e && e.message) || e) }); }






// components/evidence/CardGrid.jsx
try { (() => {
/** Hairline grid block, on the instrument grammar: an optional silkscreen header naming what the set is
 *  and how many are in it, then cells sized by their contents. Without a label it stays a bare grid —
 *  but if you cannot name the set, ask whether it needed a frame at all. */
function CardGrid({
  columns = 2,
  raised = true,
  label,
  scope,
  children,
  style,
  ...rest
}) {
  const count = React.Children.count(children);
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "card-grid",
    style: {
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: raised ? "var(--story-raised)" : "transparent",
      boxShadow: raised ? "var(--story-shadow)" : "none",
      ...style
    },
    ...rest
  }, (label || scope) && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, scope || String(count).padStart(2, "0"))), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))"
    }
  }, children));
}
Object.assign(__ds_scope, { CardGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/CardGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/ResearchGrid.jsx
try { (() => {
/** Published sources behind a measurement, one row each: who, when, and the thing it makes computable.
 *  A citation row is the difference between an instrument and an opinion, so the source is never
 *  paraphrased away — it stays attributable. */
function ResearchGrid({
  items = [],
  columns = 2,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "research-grid",
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + columns + ",minmax(0,1fr))",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, items.map((item, i) => /*#__PURE__*/React.createElement("article", {
    key: i,
    style: {
      padding: "15px 18px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, item.source), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, item.year)), /*#__PURE__*/React.createElement("h3", {
    style: {
      margin: "9px 0 0",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.12,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, item.gives), item.note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "7px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.note))));
}
Object.assign(__ds_scope, { ResearchGrid });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/ResearchGrid.jsx", error: String((e && e.message) || e) }); }

// components/evidence/RuleSplit.jsx
try { (() => {
/** Two-column sort of a guidance set: what a model has to judge, and what code can check.
 *  The whole argument for a measurement layer fits in one of these — a reader can see the
 *  proportion before reading a single row. */
function RuleSplit({
  modelLabel = "Model judgment",
  codeLabel = "Checkable in code",
  rules = [],
  note,
  style,
  ...rest
}) {
  const model = rules.filter(r => r.kind === "model");
  const code = rules.filter(r => r.kind === "code");
  const column = (label, items, accent) => /*#__PURE__*/React.createElement("div", {
    style: {
      minWidth: 0,
      borderRight: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: accent,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), /*#__PURE__*/React.createElement("ul", {
    style: {
      margin: 0,
      padding: 0,
      listStyle: "none"
    }
  }, items.map((r, i) => /*#__PURE__*/React.createElement("li", {
    key: i,
    style: {
      padding: "10px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: 520,
      lineHeight: 1.35,
      letterSpacing: "var(--body-tracking)"
    }
  }, r.rule))));
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "rule-split",
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      border: "1px solid var(--story-line)",
      borderRight: 0,
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, column(modelLabel, model, "var(--story-muted)"), column(codeLabel, code, "var(--story-accent)")), note && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "13px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--subhead-size)",
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "var(--subhead-tracking)"
    }
  }, note));
}
Object.assign(__ds_scope, { RuleSplit });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/RuleSplit.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Toolchain.jsx
try { (() => {
/** What a system was actually built out of, named and linked so a reader can go and check.
 *
 *  The engineering counterpart to `ResearchGrid`: same anatomy, same register. Deliberately no logos —
 *  a row of third-party brand marks would be the one place this system's ink-and-hairline language
 *  breaks, and it is the oldest tell in portfolio design. A name, a licence, and what it actually
 *  provides is more credible than a badge, because it can be verified. */
function Toolchain({
  items = [],
  label = "Built with",
  note,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "toolchain",
    style: style,
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      alignItems: "baseline",
      padding: "11px 16px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      fontVariantNumeric: "tabular-nums"
    }
  }, String(items.length).padStart(2, "0"))), items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: item.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(140px,0.9fr) minmax(0,2fr) auto",
      gap: "clamp(12px,2vw,26px)",
      alignItems: "baseline",
      padding: "12px 16px",
      borderBottom: i === items.length - 1 ? 0 : "1px solid var(--story-line)"
    }
  }, item.href ? /*#__PURE__*/React.createElement("a", {
    href: item.href,
    target: "_blank",
    rel: "noreferrer noopener",
    style: {
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em",
      textDecoration: "none",
      borderBottom: "1px solid currentColor",
      width: "fit-content",
      paddingBottom: 1
    }
  }, item.name) : /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 14,
      fontWeight: 650,
      letterSpacing: "-0.03em"
    }
  }, item.name), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.4,
      letterSpacing: "var(--body-tracking)"
    }
  }, item.provides), item.meta && /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap",
      textAlign: "right"
    }
  }, item.meta)))), note && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: 640,
      margin: "13px 0 0",
      color: "var(--story-muted)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      lineHeight: 1.45
    }
  }, note));
}
Object.assign(__ds_scope, { Toolchain });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Toolchain.jsx", error: String((e && e.message) || e) }); }

// components/evidence/BeforeAfter.jsx
try { (() => {
/** A rewrite delivered as a diff with its measurements attached, so the person deciding whether to
 *  take it can see what moved and by how much. The deltas are the argument — without them this is
 *  just someone else's preferred wording. */
function BeforeAfter({
  before,
  after,
  beforeLabel = "Before",
  afterLabel = "After",
  deltas = [],
  actions,
  style,
  ...rest
}) {
  const pane = (label, text, tone) => /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "14px 16px",
      borderRight: tone === "before" ? "1px solid var(--story-line)" : 0
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 9,
      color: tone === "after" ? "var(--story-accent)" : "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: 0,
      color: tone === "before" ? "var(--story-muted)" : "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--body-size)",
      fontWeight: tone === "after" ? 560 : 480,
      lineHeight: 1.45,
      letterSpacing: "var(--body-tracking)"
    }
  }, text));
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "before-after",
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(2,minmax(0,1fr))",
      borderBottom: "1px solid var(--story-line)"
    }
  }, pane(beforeLabel, before, "before"), pane(afterLabel, after, "after")), deltas.length > 0 && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "grid",
      gridTemplateColumns: "repeat(" + Math.max(deltas.length, 1) + ",minmax(0,1fr))",
      borderBottom: actions ? "1px solid var(--story-line)" : 0
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        padding: "11px 14px",
        borderRight: i === deltas.length - 1 ? 0 : "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        display: "block",
        marginBottom: 6,
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        display: "flex",
        gap: 6,
        alignItems: "baseline",
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 13,
        fontWeight: 560,
        letterSpacing: "-0.03em",
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-accent)",
        fontSize: 17,
        fontWeight: 700,
        letterSpacing: "-0.04em"
      }
    }, d.to)));
  })), actions && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      gap: "var(--space-8)",
      alignItems: "center",
      padding: "12px 16px"
    }
  }, actions));
}
Object.assign(__ds_scope, { BeforeAfter });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/BeforeAfter.jsx", error: String((e && e.message) || e) }); }

// components/evidence/Ledger.jsx
try { (() => {
/** Counted facts, sized by what they say.
 *
 *  Label above value, so a cell is only as wide as the wider of the two and a row of four fits without
 *  stretching. Side-by-side reads fine until a value is a word rather than a number, at which point the
 *  cells go ragged and the label and the number end up at opposite ends of a void. This is the same
 *  stacking `VoiceProfile` uses, which is the system's answer for a set of small readings. */
function Ledger({
  items = [],
  label = "Counted",
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("div", {
    "data-ds": "ledger",
    style: {
      width: "fit-content",
      maxWidth: "100%",
      borderTop: "1px solid var(--story-line)",
      borderLeft: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)",
      ...style
    },
    ...rest
  }, label && /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "11px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexWrap: "wrap"
    }
  }, items.map((item, i) => /*#__PURE__*/React.createElement("div", {
    key: i,
    style: {
      flex: "0 1 auto",
      padding: "12px 18px 13px 16px",
      borderRight: "1px solid var(--story-line)",
      borderBottom: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 7,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      lineHeight: 1.35,
      whiteSpace: "nowrap"
    }
  }, item.label), /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: "var(--metric-size)",
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.045em",
      whiteSpace: "nowrap",
      fontVariantNumeric: "tabular-nums"
    }
  }, item.value)))));
}
Object.assign(__ds_scope, { Ledger });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/evidence/Ledger.jsx", error: String((e && e.message) || e) }); }



















// components/navigation/IndexRail.jsx
try { (() => {
/** The fixed left rail: one continuous editorial contents field for identity, work, and utilities. */
function IndexRail({
  name = "Caleb Stacy",
  title = "Senior content designer",
  differentiator = "Product systems · content engineering",
  thesis,
  projects = [],
  active,
  activeSub,
  onSelect,
  utilities = [],
  location,
  contactLabel = "Contact",
  contactHref,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("aside", {
    "aria-label": "Identity and portfolio navigation",
    "data-ds": "rail",
    style: {
      position: "relative",
      zIndex: 20,
      minWidth: 0,
      minHeight: 0,
      display: "flex",
      flexDirection: "column",
      borderRight: "1px solid var(--ink)",
      background: "var(--paper)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ds-rail-identity",
    onClick: () => onSelect && onSelect(utilities[0] ? utilities[0].id : "about"),
    style: {
      width: "100%",
      display: "block",
      padding: "clamp(28px,4.4vh,42px) var(--rail-pad-x) 14px",
      border: 0,
      background: "transparent",
      color: "var(--ink)",
      textAlign: "left",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-rail-name",
    style: {
      display: "inline",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,2.25vw,34px)",
      fontWeight: "var(--wordmark-weight)",
      lineHeight: 0.94,
      letterSpacing: "var(--wordmark-tracking)"
    }
  }, name), /*#__PURE__*/React.createElement("small", {
    style: {
      display: "block",
      marginTop: 10,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 620,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, title, /*#__PURE__*/React.createElement("i", {
    style: {
      display: "block",
      color: "var(--muted-ink)",
      fontStyle: "normal"
    }
  }, differentiator))), thesis && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: "32ch",
      margin: 0,
      padding: "0 var(--rail-pad-x) clamp(25px,3.5vh,32px)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.48,
      letterSpacing: "-0.025em"
    }
  }, thesis), /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Selected work",
    "data-ds": "rail-projects",
    style: {
      display: "flex",
      flex: "0 0 auto",
      flexDirection: "column"
    }
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      padding: "0 var(--rail-pad-x) 7px",
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 650,
      lineHeight: 1.3,
      letterSpacing: "-0.01em",
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("span", null, "Selected work"), /*#__PURE__*/React.createElement("span", null, String(projects.length).padStart(2, "0"))), projects.map(p => /*#__PURE__*/React.createElement(__ds_scope.ProjectSpineItem, {
    key: p.id,
    number: p.number,
    title: p.title,
    statement: p.statement,
    active: active === p.id,
    onSelect: () => onSelect && onSelect(p.id),
    subItems: p.subPages,
    activeSub: activeSub,
    onSelectSub: sid => onSelect && onSelect(sid)
  }))), /*#__PURE__*/React.createElement("div", {
    "data-ds": "rail-utility-cluster",
    style: {
      marginTop: "auto",
      padding: "18px var(--rail-pad-x) 20px",
      borderTop: "1px solid var(--soft-line)"
    }
  }, utilities.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 3,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 650,
      lineHeight: 1.3
    }
  }, "Profile"), /*#__PURE__*/React.createElement(__ds_scope.UtilityNav, {
    items: utilities,
    active: active,
    onSelect: onSelect
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: "var(--space-4)",
      alignItems: "end",
      marginTop: utilities.length > 0 ? 17 : 0,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 7.5,
      lineHeight: 1.4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      maxWidth: "22ch"
    }
  }, location), contactHref && /*#__PURE__*/React.createElement("a", {
    className: "ds-rail-contact",
    href: contactHref,
    style: {
      flex: "0 0 auto",
      paddingBottom: 2,
      color: "var(--ink)",
      fontSize: 9.5,
      fontWeight: 700,
      textDecoration: "none"
    }
  }, contactLabel))));
}
Object.assign(__ds_scope, { IndexRail });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/IndexRail.jsx", error: String((e && e.message) || e) }); }

// components/navigation/ProjectSpineItem.jsx
try { (() => {
/** One entry in the portfolio's editorial contents. The active project is located by one drawn
 *  rule at the rail edge; type and ground stay still. A project with sub-pages reveals them in
 *  the same continuous field while active. */
function ProjectSpineItem({
  number,
  title,
  statement,
  active = false,
  onSelect,
  subItems,
  activeSub,
  onSelectSub,
  context = "rail",
  style,
  ...rest
}) {
  const hasSubs = Array.isArray(subItems) && subItems.length > 0;
  const showSubs = active && hasSubs;
  const inMenu = context === "menu";
  const removedFromTabOrder = rest.tabIndex === -1;
  const columns = inMenu ? "38px minmax(0,1fr)" : "28px minmax(0,1fr)";
  const horizontalPadding = inMenu ? "22px" : "var(--rail-pad-x)";
  return /*#__PURE__*/React.createElement("div", {
    className: "ds-spine-item",
    "aria-current": active ? "page" : undefined,
    "data-context": context,
    style: {
      position: "relative",
      display: "flex",
      flex: "0 0 auto",
      flexDirection: "column",
      width: "100%",
      overflow: "visible",
      color: "var(--ink)",
      ...style
    }
  }, /*#__PURE__*/React.createElement("i", {
    "aria-hidden": "true",
    className: "ds-spine-active-marker",
    "data-active": active ? "true" : "false",
    "data-ds": "active-marker",
    style: {
      top: inMenu ? 23 : 17,
      width: inMenu ? 18 : 20
    }
  }), /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ds-spine-trigger",
    onClick: onSelect,
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      gap: inMenu ? 12 : 9,
      width: "100%",
      padding: inMenu ? "12px " + horizontalPadding + " 10px" : "8px " + horizontalPadding,
      border: 0,
      background: "transparent",
      color: "inherit",
      textAlign: "left",
      cursor: "pointer"
    },
    ...rest
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      paddingTop: inMenu ? 5 : 4,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: inMenu ? 10 : 9,
      fontWeight: 680,
      lineHeight: 1,
      letterSpacing: "-0.03em",
      fontVariantNumeric: "tabular-nums"
    }
  }, number), /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0
    }
  }, /*#__PURE__*/React.createElement("strong", {
    className: "ds-spine-title-label",
    style: {
      display: "inline",
      fontFamily: "var(--font-sans)",
      fontSize: inMenu ? 18 : 16,
      fontWeight: active ? 700 : 610,
      lineHeight: 1.15,
      letterSpacing: "-0.045em",
      transition: "font-weight var(--dur-base) var(--ease-authored)"
    }
  }, title), /*#__PURE__*/React.createElement("small", {
    "aria-hidden": !active,
    style: {
      display: "block",
      maxWidth: inMenu ? 274 : 230,
      overflow: "hidden",
      color: "var(--muted-ink)",
      visibility: active ? "visible" : "hidden",
      fontFamily: "var(--font-sans)",
      fontSize: inMenu ? 12 : 11,
      fontWeight: 470,
      lineHeight: 1.4,
      letterSpacing: "-0.02em",
      maxHeight: active ? 84 : 0,
      marginTop: active ? 7 : 0,
      clipPath: active ? "inset(0 0 0 0)" : "inset(0 0 100% 0)",
      transition: "max-height var(--dur-slow) var(--ease-authored), margin var(--dur-slow) var(--ease-authored), clip-path var(--dur-slow) var(--ease-authored), visibility 0s " + (active ? "0s" : "var(--dur-slow)")
    }
  }, statement))), hasSubs && /*#__PURE__*/React.createElement("div", {
    role: "group",
    "aria-label": title + " pages",
    "aria-hidden": !showSubs,
    style: {
      overflow: "hidden",
      maxHeight: showSubs ? 144 : 0,
      paddingBottom: showSubs ? inMenu ? 10 : 7 : 0,
      visibility: showSubs ? "visible" : "hidden",
      clipPath: showSubs ? "inset(0 0 0 0)" : "inset(0 0 100% 0)",
      transition: "max-height var(--dur-slow) var(--ease-authored), padding var(--dur-slow) var(--ease-authored), clip-path var(--dur-slow) var(--ease-authored), visibility 0s " + (showSubs ? "0s" : "var(--dur-slow)")
    }
  }, subItems.map(s => /*#__PURE__*/React.createElement("button", {
    key: s.id,
    type: "button",
    className: "ds-spine-sub",
    "aria-current": activeSub === s.id ? "page" : undefined,
    onClick: () => onSelectSub && onSelectSub(s.id),
    tabIndex: showSubs && !removedFromTabOrder ? undefined : -1,
    style: {
      display: "grid",
      gridTemplateColumns: columns,
      gap: inMenu ? 12 : 9,
      width: "100%",
      padding: (inMenu ? "4px " : "3px ") + horizontalPadding,
      border: 0,
      background: "transparent",
      color: "var(--muted-ink)",
      textAlign: "left",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true"
  }), /*#__PURE__*/React.createElement("span", {
    className: "ds-spine-sub-label",
    style: {
      justifySelf: "start",
      fontFamily: "var(--font-sans)",
      fontSize: inMenu ? 14 : 12.5,
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "-0.025em"
    }
  }, s.label)))));
}
Object.assign(__ds_scope, { ProjectSpineItem });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/ProjectSpineItem.jsx", error: String((e && e.message) || e) }); }

// components/navigation/UtilityNav.jsx
try { (() => {
/** A wrapping text-link cluster for secondary destinations. State is a drawn rule, never a box. */
function UtilityNav({
  items = [],
  active,
  onSelect,
  itemTabIndex,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Utilities",
    "data-ds": "utility-nav",
    style: {
      display: "flex",
      flexWrap: "wrap",
      gap: "2px 18px",
      alignItems: "baseline",
      ...style
    },
    ...rest
  }, items.map(item => {
    const on = item.id === active;
    return /*#__PURE__*/React.createElement("button", {
      key: item.id,
      type: "button",
      className: "ds-utility-link",
      onClick: () => onSelect && onSelect(item.id),
      "aria-current": on ? "page" : undefined,
      tabIndex: itemTabIndex,
      style: {
        position: "relative",
        flex: "0 0 auto",
        padding: "6px 0 5px",
        border: 0,
        background: "transparent",
        color: "var(--ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 12.5,
        fontWeight: on ? 700 : 560,
        lineHeight: 1.2,
        letterSpacing: "-0.025em",
        cursor: "pointer",
        transition: "font-weight var(--dur-base) var(--ease-authored)"
      }
    }, item.label);
  }));
}
Object.assign(__ds_scope, { UtilityNav });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/UtilityNav.jsx", error: String((e && e.message) || e) }); }

// components/navigation/MobileIdentityBar.jsx
try { (() => {
/** The persistent phone identity and index control. It is one composed bar and one tap target:
 *  identity at the start, an explicit Index / Close index action at the end. */
function MobileIdentityBar({
  name = "Caleb Stacy",
  title = "Senior content designer",
  isOpen = false,
  onToggle,
  toggleRef,
  menuId,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("header", {
    "data-ds": "mobile-bar",
    style: {
      position: "sticky",
      zIndex: 300,
      top: 0,
      paddingTop: "env(safe-area-inset-top, 0px)",
      borderBottom: "1px solid var(--ink)",
      background: "var(--paper-bright)",
      color: "var(--ink)",
      boxShadow: "var(--shadow-sticky)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ds-mobile-identity-control",
    ref: toggleRef,
    onClick: onToggle,
    "aria-expanded": isOpen,
    "aria-controls": menuId,
    "aria-label": isOpen ? "Close site index" : "Open site index",
    "data-state": isOpen ? "open" : "closed",
    style: {
      width: "100%",
      height: "var(--mobile-bar-height)",
      display: "grid",
      gridTemplateColumns: "minmax(0,1fr) auto",
      alignItems: "center",
      gap: 18,
      padding: "0 20px",
      border: 0,
      background: "transparent",
      color: "var(--ink)",
      textAlign: "left",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      minWidth: 0,
      display: "flex",
      flexDirection: "column",
      justifyContent: "center",
      gap: 3
    }
  }, /*#__PURE__*/React.createElement("strong", {
    style: {
      display: "block",
      fontFamily: "var(--font-sans)",
      fontSize: 16.5,
      fontWeight: 700,
      lineHeight: 1,
      letterSpacing: "-0.045em"
    }
  }, name), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      overflow: "hidden",
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 620,
      lineHeight: 1.2,
      letterSpacing: "-0.01em",
      textOverflow: "ellipsis",
      whiteSpace: "nowrap"
    }
  }, title)), /*#__PURE__*/React.createElement("span", {
    style: {
      flex: "0 0 auto",
      display: "flex",
      alignItems: "center",
      justifyContent: "end",
      gap: 10,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      whiteSpace: "nowrap"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-mobile-toggle-label"
  }, isOpen ? "Close index" : "Index"), /*#__PURE__*/React.createElement("i", {
    className: "ds-mobile-toggle-icon",
    "data-state": isOpen ? "open" : "closed",
    "aria-hidden": "true"
  }))));
}
Object.assign(__ds_scope, { MobileIdentityBar });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/MobileIdentityBar.jsx", error: String((e && e.message) || e) }); }

// components/navigation/MobileMenu.jsx
try { (() => {
/** The phone's full-surface editorial index. It composes the rail's real destinations in one
 *  continuous paper field, with the active project located by the same drawn rule as desktop. */
function MobileMenu({
  isOpen = false,
  onClose,
  projects = [],
  utilities = [],
  activeId,
  activeSubId,
  onSelect,
  location,
  contactLabel = "Contact",
  contactHref,
  menuId,
  returnFocusRef,
  style,
  ...rest
}) {
  const panelRef = React.useRef(null);
  const wasOpen = React.useRef(false);
  React.useEffect(() => {
    let focusTimer;
    if (isOpen) {
      /* Run after the opening gesture completes. Focusing in the same click turn lets the
         trigger reclaim focus in some browsers, leaving the modal visually open but the
         keyboard outside it. */
      focusTimer = window.setTimeout(() => {
        const first = panelRef.current && (panelRef.current.querySelector("[data-menu-initial-focus]") || panelRef.current.querySelector("a, button"));
        if (first) first.focus();
      }, 60);
    } else if (wasOpen.current && returnFocusRef && returnFocusRef.current) {
      returnFocusRef.current.focus();
    }
    wasOpen.current = isOpen;
    return () => {
      if (focusTimer) window.clearTimeout(focusTimer);
    };
  }, [isOpen, returnFocusRef]);
  React.useEffect(() => {
    if (!isOpen) return undefined;
    const onKeyDown = event => {
      if (event.key === "Escape") {
        onClose && onClose();
        return;
      }
      if (event.key !== "Tab" || !panelRef.current) return;
      const focusable = panelRef.current.querySelectorAll("a, button");
      if (!focusable.length) return;
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [isOpen, onClose]);
  const select = id => {
    onSelect && onSelect(id);
    onClose && onClose();
  };
  const tab = isOpen ? undefined : -1;
  return /*#__PURE__*/React.createElement("div", {
    ref: panelRef,
    id: menuId,
    role: "dialog",
    "aria-modal": "true",
    "aria-label": "Site navigation",
    "data-ds": "mobile-menu",
    className: "ds-mobile-menu",
    style: {
      position: "fixed",
      zIndex: 290,
      left: 0,
      right: 0,
      top: "calc(env(safe-area-inset-top, 0px) + var(--mobile-bar-height))",
      bottom: 0,
      display: "flex",
      flexDirection: "column",
      overflowY: "auto",
      background: "var(--paper)",
      color: "var(--ink)",
      clipPath: isOpen ? "inset(0 0 0% 0)" : "inset(0 0 100% 0)",
      visibility: isOpen ? "visible" : "hidden",
      transition: isOpen ? "clip-path var(--dur-fast) var(--ease-authored)" : "clip-path var(--dur-fast) var(--ease-authored), visibility 0s var(--dur-fast)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Work",
    style: {
      display: "flex",
      flex: "0 0 auto",
      flexDirection: "column",
      paddingTop: "clamp(22px,6vw,30px)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      padding: "0 22px 8px",
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8.5,
      fontWeight: 650,
      lineHeight: 1.3,
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("span", null, "Selected work"), /*#__PURE__*/React.createElement("span", null, String(projects.length).padStart(2, "0"))), projects.map((p, index) => /*#__PURE__*/React.createElement(__ds_scope.ProjectSpineItem, {
    key: p.id,
    number: p.number,
    title: p.title,
    statement: p.statement,
    active: p.id === activeId,
    onSelect: () => select(p.id),
    subItems: p.subPages,
    activeSub: activeSubId,
    onSelectSub: sid => select(sid),
    tabIndex: tab,
    context: "menu",
    "data-menu-initial-focus": index === 0 ? "true" : undefined
  }))), /*#__PURE__*/React.createElement("div", {
    "data-ds": "mobile-utility-cluster",
    style: {
      flex: "0 0 auto",
      marginTop: "auto",
      padding: "18px 22px calc(20px + env(safe-area-inset-bottom, 0px))",
      borderTop: "1px solid var(--soft-line)"
    }
  }, utilities.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 3,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8.5,
      fontWeight: 650,
      lineHeight: 1.3
    }
  }, "Profile"), /*#__PURE__*/React.createElement(__ds_scope.UtilityNav, {
    items: utilities,
    active: activeId,
    onSelect: select,
    itemTabIndex: tab,
    style: {
      flex: "0 0 auto",
      gap: "2px 24px"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: "var(--space-5)",
      alignItems: "end",
      marginTop: utilities.length > 0 ? 22 : 0,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      lineHeight: 1.4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      maxWidth: "23ch"
    }
  }, location), contactHref && /*#__PURE__*/React.createElement("a", {
    className: "ds-rail-contact",
    href: contactHref,
    tabIndex: tab,
    style: {
      flex: "0 0 auto",
      paddingBottom: 2,
      color: "var(--ink)",
      fontSize: 10,
      fontWeight: 700,
      textDecoration: "none"
    }
  }, contactLabel))));
}
Object.assign(__ds_scope, { MobileMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components/navigation/MobileMenu.jsx", error: String((e && e.message) || e) }); }

// components\navigation\IndexRail.jsx
try { (() => {
/** The fixed left rail: one continuous editorial contents field for identity, work, and utilities. */
function IndexRail({
  name = "Caleb Stacy",
  title = "Senior content designer",
  differentiator = "Product systems · content engineering",
  thesis,
  projects = [],
  active,
  activeSub,
  onSelect,
  utilities = [],
  location,
  contactLabel = "Contact",
  contactHref,
  style,
  ...rest
}) {
  return /*#__PURE__*/React.createElement("aside", {
    "aria-label": "Identity and portfolio navigation",
    "data-ds": "rail",
    style: {
      position: "relative",
      zIndex: 20,
      minWidth: 0,
      minHeight: 0,
      display: "flex",
      flexDirection: "column",
      borderRight: "1px solid var(--ink)",
      background: "var(--paper)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("button", {
    type: "button",
    className: "ds-rail-identity",
    onClick: () => onSelect && onSelect(utilities[0] ? utilities[0].id : "about"),
    style: {
      width: "100%",
      display: "block",
      padding: "clamp(28px,4.4vh,42px) var(--rail-pad-x) 14px",
      border: 0,
      background: "transparent",
      color: "var(--ink)",
      textAlign: "left",
      cursor: "pointer"
    }
  }, /*#__PURE__*/React.createElement("span", {
    className: "ds-rail-name",
    style: {
      display: "inline",
      fontFamily: "var(--font-sans)",
      fontSize: "clamp(28px,2.25vw,34px)",
      fontWeight: "var(--wordmark-weight)",
      lineHeight: 0.94,
      letterSpacing: "var(--wordmark-tracking)"
    }
  }, name), /*#__PURE__*/React.createElement("small", {
    style: {
      display: "block",
      marginTop: 10,
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 620,
      lineHeight: 1.45,
      letterSpacing: "-0.02em"
    }
  }, title, /*#__PURE__*/React.createElement("i", {
    style: {
      display: "block",
      color: "var(--muted-ink)",
      fontStyle: "normal"
    }
  }, differentiator))), thesis && /*#__PURE__*/React.createElement("p", {
    style: {
      maxWidth: "32ch",
      margin: 0,
      padding: "0 var(--rail-pad-x) clamp(25px,3.5vh,32px)",
      fontFamily: "var(--font-sans)",
      fontSize: 13,
      lineHeight: 1.48,
      letterSpacing: "-0.025em"
    }
  }, thesis), /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Selected work",
    "data-ds": "rail-projects",
    style: {
      display: "flex",
      flex: "0 0 auto",
      flexDirection: "column"
    }
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      padding: "0 var(--rail-pad-x) 7px",
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 650,
      lineHeight: 1.3,
      letterSpacing: "-0.01em",
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("span", null, "Selected work"), /*#__PURE__*/React.createElement("span", null, String(projects.length).padStart(2, "0"))), projects.map(p => /*#__PURE__*/React.createElement(__ds_scope.ProjectSpineItem, {
    key: p.id,
    number: p.number,
    title: p.title,
    statement: p.statement,
    active: active === p.id,
    onSelect: () => onSelect && onSelect(p.id),
    subItems: p.subPages,
    activeSub: activeSub,
    onSelectSub: sid => onSelect && onSelect(sid)
  }))), /*#__PURE__*/React.createElement("div", {
    "data-ds": "rail-utility-cluster",
    style: {
      marginTop: "auto",
      padding: "18px var(--rail-pad-x) 20px",
      borderTop: "1px solid var(--soft-line)"
    }
  }, utilities.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 3,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      fontWeight: 650,
      lineHeight: 1.3
    }
  }, "More"), /*#__PURE__*/React.createElement(__ds_scope.UtilityNav, {
    items: utilities,
    active: active,
    onSelect: onSelect
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: "var(--space-4)",
      alignItems: "end",
      marginTop: utilities.length > 0 ? 17 : 0,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 7.5,
      lineHeight: 1.4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      maxWidth: "22ch"
    }
  }, location), contactHref && /*#__PURE__*/React.createElement("a", {
    className: "ds-rail-contact",
    href: contactHref,
    style: {
      flex: "0 0 auto",
      paddingBottom: 2,
      color: "var(--ink)",
      fontSize: 9.5,
      fontWeight: 700,
      textDecoration: "none"
    }
  }, contactLabel))));
}
Object.assign(__ds_scope, { IndexRail });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components\\navigation\\IndexRail.jsx", error: String((e && e.message) || e) }); }

// components\navigation\MobileMenu.jsx
try { (() => {
/** The phone's full-surface editorial index. It composes the rail's real destinations in one
 *  continuous paper field, with the active project located by the same drawn rule as desktop. */
function MobileMenu({
  isOpen = false,
  onClose,
  projects = [],
  utilities = [],
  activeId,
  activeSubId,
  onSelect,
  location,
  contactLabel = "Contact",
  contactHref,
  menuId,
  returnFocusRef,
  style,
  ...rest
}) {
  const panelRef = React.useRef(null);
  const wasOpen = React.useRef(false);
  React.useEffect(() => {
    let focusTimer;
    if (isOpen) {
      /* Run after the opening gesture completes. Focusing in the same click turn lets the
         trigger reclaim focus in some browsers, leaving the modal visually open but the
         keyboard outside it. */
      focusTimer = window.setTimeout(() => {
        const first = panelRef.current && (panelRef.current.querySelector("[data-menu-initial-focus]") || panelRef.current.querySelector("a, button"));
        if (first) first.focus();
      }, 60);
    } else if (wasOpen.current && returnFocusRef && returnFocusRef.current) {
      returnFocusRef.current.focus();
    }
    wasOpen.current = isOpen;
    return () => {
      if (focusTimer) window.clearTimeout(focusTimer);
    };
  }, [isOpen, returnFocusRef]);
  React.useEffect(() => {
    if (!isOpen) return undefined;
    const onKeyDown = event => {
      if (event.key === "Escape") {
        onClose && onClose();
        return;
      }
      if (event.key !== "Tab" || !panelRef.current) return;
      const focusable = panelRef.current.querySelectorAll("a, button");
      if (!focusable.length) return;
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      if (event.shiftKey && document.activeElement === first) {
        event.preventDefault();
        last.focus();
      } else if (!event.shiftKey && document.activeElement === last) {
        event.preventDefault();
        first.focus();
      }
    };
    window.addEventListener("keydown", onKeyDown);
    return () => window.removeEventListener("keydown", onKeyDown);
  }, [isOpen, onClose]);
  const select = id => {
    onSelect && onSelect(id);
    onClose && onClose();
  };
  const tab = isOpen ? undefined : -1;
  return /*#__PURE__*/React.createElement("div", {
    ref: panelRef,
    id: menuId,
    role: "dialog",
    "aria-modal": "true",
    "aria-label": "Site navigation",
    "data-ds": "mobile-menu",
    className: "ds-mobile-menu",
    style: {
      position: "fixed",
      zIndex: 290,
      left: 0,
      right: 0,
      top: "calc(env(safe-area-inset-top, 0px) + var(--mobile-bar-height))",
      bottom: 0,
      display: "flex",
      flexDirection: "column",
      overflowY: "auto",
      background: "var(--paper)",
      color: "var(--ink)",
      clipPath: isOpen ? "inset(0 0 0% 0)" : "inset(0 0 100% 0)",
      visibility: isOpen ? "visible" : "hidden",
      transition: isOpen ? "clip-path var(--dur-fast) var(--ease-authored)" : "clip-path var(--dur-fast) var(--ease-authored), visibility 0s var(--dur-fast)",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("nav", {
    "aria-label": "Work",
    style: {
      display: "flex",
      flex: "0 0 auto",
      flexDirection: "column",
      paddingTop: "clamp(22px,6vw,30px)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: 12,
      padding: "0 22px 8px",
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8.5,
      fontWeight: 650,
      lineHeight: 1.3,
      fontVariantNumeric: "tabular-nums"
    }
  }, /*#__PURE__*/React.createElement("span", null, "Selected work"), /*#__PURE__*/React.createElement("span", null, String(projects.length).padStart(2, "0"))), projects.map((p, index) => /*#__PURE__*/React.createElement(__ds_scope.ProjectSpineItem, {
    key: p.id,
    number: p.number,
    title: p.title,
    statement: p.statement,
    active: p.id === activeId,
    onSelect: () => select(p.id),
    subItems: p.subPages,
    activeSub: activeSubId,
    onSelectSub: sid => select(sid),
    tabIndex: tab,
    context: "menu",
    "data-menu-initial-focus": index === 0 ? "true" : undefined
  }))), /*#__PURE__*/React.createElement("div", {
    "data-ds": "mobile-utility-cluster",
    style: {
      flex: "0 0 auto",
      marginTop: "auto",
      padding: "18px 22px calc(20px + env(safe-area-inset-bottom, 0px))",
      borderTop: "1px solid var(--soft-line)"
    }
  }, utilities.length > 0 && /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    style: {
      display: "block",
      marginBottom: 3,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8.5,
      fontWeight: 650,
      lineHeight: 1.3
    }
  }, "More"), /*#__PURE__*/React.createElement(__ds_scope.UtilityNav, {
    items: utilities,
    active: activeId,
    onSelect: select,
    itemTabIndex: tab,
    style: {
      flex: "0 0 auto",
      gap: "2px 24px"
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      gap: "var(--space-5)",
      alignItems: "end",
      marginTop: utilities.length > 0 ? 22 : 0,
      color: "var(--muted-ink)",
      fontFamily: "var(--font-mono)",
      fontSize: 8,
      lineHeight: 1.4
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      maxWidth: "23ch"
    }
  }, location), contactHref && /*#__PURE__*/React.createElement("a", {
    className: "ds-rail-contact",
    href: contactHref,
    tabIndex: tab,
    style: {
      flex: "0 0 auto",
      paddingBottom: 2,
      color: "var(--ink)",
      fontSize: 10,
      fontWeight: 700,
      textDecoration: "none"
    }
  }, contactLabel))));
}
Object.assign(__ds_scope, { MobileMenu });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components\\navigation\\MobileMenu.jsx", error: String((e && e.message) || e) }); }

// components\case\VersoHero.jsx
try { (() => {
/* Verso's cover instrument: a draft arrives, the checks stream and quiet into a collapsed strip,
 * the supported measurements draw themselves, the rewrite arrives with its deltas, and a
 * short self-check closes the loop. Plays once, settles, replays on request.
 *
 * Every mark is drawn, never faded: text streams in behind a left-to-right clip-path (never an
 * opacity fade), the ring's arc is a stroke-dashoffset sweep, the dimension bars extend from the
 * left edge, and the accept affordance's underline grows from nothing. The one exception the
 * house rule itself allows — "subtle opacity on top of a directional draw" — never happens either;
 * even the checks window's shimmer is a moving gradient clipped to the text, not an opacity
 * layer. `prefers-reduced-motion` (and phone width, where the two-pane stage stacks and this panel
 * has no room to run the sequence) renders the fully composed end state on the first frame and
 * hides the replay control — there is nothing to replay.
 *
 * The sequencer is a single `step` index into a named timeline (`draft`, one entry per reasoning
 * check, `think-collapse`, `reading`, `reading-verdict`, `rewrite-after`, `rewrite-deltas`,
 * `rewrite-cta`, `selflint-line`, `selflint-verdict`, `settled`). Every visual that a CSS
 * transition can own (the ring, the bars, every text reveal, the CTA underline) reads straight off
 * a `step >=` comparison, so React owns them and reduced motion's global transition-speed override
 * already makes them instant for free. Only what a stylesheet cannot animate — typed characters,
 * counted numbers, and the checks window's height, which CSS cannot tween to `auto` — runs
 * through small imperative helpers against refs. `window.__versoHeroSequencer` (last-mounted
 * instance) exposes `gotoStep`, `showReadingPartial`, `showRewritePartial`, and `replay` for
 * screenshot proof and, later, reuse — the streamed-line, optional ring/bar-draw, and counter primitives
 * here are the pieces a fuller "Language instruments" page would want back. */

const EASE = "var(--ease-authored)";
const RING_SIZE = 74;
const RING_STROKE = 6;
const RING_MS = 760; // var(--dur-character)
const BAR_STAGGER_MS = 80;
const DRAFT_TYPE_MS = 820;
const AFTER_TYPE_MS = 560;
const DELTA_MS = 640;
const DELTA_STAGGER_MS = 70;
const COLLAPSED_PX = 24;
function easeOutCubic(x) {
  return 1 - Math.pow(1 - x, 3);
}

/** Character-by-character reveal into `el.textContent`. Cancelable; used for the two moments
 *  that are genuinely typed or written (the incoming draft, the agent's rewritten line) rather
 *  than streamed reasoning. */
function typeText(el, text, duration) {
  if (!el) return () => {};
  const start = performance.now();
  const total = text.length;
  let raf = requestAnimationFrame(function step(now) {
    const p = Math.min(1, (now - start) / duration);
    el.textContent = text.slice(0, Math.round(total * p));
    if (p < 1) raf = requestAnimationFrame(step);
  });
  return () => cancelAnimationFrame(raf);
}

/** Decelerating count from `from` to `to`, written straight into `el.textContent`. Any supported
 *  aggregate, the dimension values, and the rewrite deltas all use this one helper. */
function countTo(el, from, to, duration, prefix, suffix) {
  if (!el) return () => {};
  const start = performance.now();
  let raf = requestAnimationFrame(function step(now) {
    const p = Math.min(1, (now - start) / duration);
    const v = Math.round(from + (to - from) * easeOutCubic(p));
    el.textContent = (prefix || "") + v + (suffix || "");
    if (p < 1) raf = requestAnimationFrame(step);
  });
  return () => cancelAnimationFrame(raf);
}

/** The checks window collapsing to its silkscreen strip. CSS cannot transition to/from
 *  `height:auto`, so the natural height is measured once and locked in pixels before the
 *  transition starts — the classic vanilla trick, not a library. */
function collapseThinkBox(el) {
  if (!el) return;
  const full = el.scrollHeight;
  el.style.transition = "none";
  el.style.height = full + "px";
  void el.offsetHeight; // force layout before swapping the transition back on
  el.style.transition = "height var(--dur-mid) " + EASE;
  requestAnimationFrame(() => {
    el.style.height = COLLAPSED_PX + "px";
  });
}
function expandThinkBoxInstant(el) {
  if (!el) return;
  el.style.transition = "none";
  el.style.height = "auto";
  void el.offsetHeight;
}
const revealStyle = open => open ? {
  clipPath: "inset(0 0 0 0)"
} : {
  clipPath: "inset(0 100% 0 0)"
};
const drawn = {
  animation: "verso-line-enter var(--dur-mid) " + EASE + " both"
};
const shimmerStyle = {
  backgroundImage: "linear-gradient(90deg, var(--story-muted) 38%, var(--story-ink) 50%, var(--story-muted) 62%)",
  backgroundSize: "250% 100%",
  WebkitBackgroundClip: "text",
  backgroundClip: "text",
  color: "transparent",
  animation: "verso-shimmer-sweep 1.4s linear infinite"
};
function VersoHero({
  draft,
  thinking = [],
  reading = {},
  rewrite = {},
  selfLint = {},
  label = "Verso — reviewing a draft",
  thinkingLabel = "Checks",
  replayLabel = "Watch it again",
  style,
  ...rest
}) {
  const {
    score,
    outOf = 100,
    verdict: readingVerdict,
    dimensions = []
  } = reading;
  const {
    after = "",
    deltas = [],
    acceptLabel = "Accept the rewrite"
  } = rewrite;
  const {
    line: selfLintLine,
    verdict: selfLintVerdict
  } = selfLint;
  const hasScore = Number.isFinite(score) && Number.isFinite(outOf) && outOf > 0;
  const [reduced] = React.useState(() => typeof window !== "undefined" && window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches);
  const [compact] = React.useState(() => typeof window !== "undefined" && window.matchMedia && window.matchMedia("(max-width: 768px)").matches);
  const timeline = React.useMemo(() => {
    const names = ["draft"];
    const delays = [0];
    thinking.forEach((_, i) => {
      names.push("think-" + i);
      delays.push(i === 0 ? 950 : 620);
    });
    names.push("think-collapse", "reading", "reading-verdict", "rewrite-after", "rewrite-deltas", "rewrite-cta", "selflint-line", "selflint-verdict", "settled");
    delays.push(700, 450, 980, 520, 850, 950, 500, 560, 550);
    const index = {};
    names.forEach((n, i) => index[n] = i);
    return {
      names,
      delays,
      index,
      last: names.length - 1
    };
  }, [thinking.length]);
  const [step, setStep] = React.useState(() => reduced || compact ? timeline.last : -1);
  const els = React.useRef({});
  const setRef = key => el => {
    els.current[key] = el;
  };
  const timersRef = React.useRef([]);
  const clearTimers = () => {
    timersRef.current.forEach(clearTimeout);
    timersRef.current = [];
  };

  // `typeText`/`countTo` each return a canceler for the rAF loop they start. `clearTimers` alone
  // only stops timers that have not fired yet — a `typeText` already in flight (draft typing
  // starts the instant the panel comes into view, before a jump could ever race it) keeps writing
  // `el.textContent` every frame regardless. Every real (animated) start pushes its canceler here;
  // every instant jump (`gotoStep`, `replay`) drains it first, so a jump can never lose a frame
  // later to a stray loop it didn't know was still running.
  const activeAnimRef = React.useRef([]);
  const cancelActiveAnims = () => {
    activeAnimRef.current.forEach(fn => fn());
    activeAnimRef.current = [];
  };
  const ringR = (RING_SIZE - RING_STROKE) / 2;
  const ringC = 2 * Math.PI * ringR;
  const scoreFrac = hasScore ? Math.max(0, Math.min(1, score / outOf)) : 0;

  /** Everything a stylesheet cannot own for milestone `name`: typed text, counted numbers, the
   *  reasoning window's collapse. Two callers, two speeds — `runAnimatedFor` is invoked directly
   *  by the timer that reaches a step during real playback and actually animates; `applyInstant`
   *  is invoked by the instant paths (reduced motion, compact, and the seek hook's jumps) and
   *  snaps straight to the same end state. Neither is wired through a `step`-keyed `useEffect`:
   *  a jump changes `step` too (so the declarative, CSS-transition-owned parts of the JSX — the
   *  ring, the bars, every clip-path reveal — still land correctly), and if the imperative work
   *  were *also* re-derived from `step` in an effect, a seek hook jump and a real timer landing
   *  on the same index would race and the jump would lose to a half-second of stray animation. */
  const runAnimatedFor = React.useCallback(name => {
    const e = els.current;
    const track = cancel => activeAnimRef.current.push(cancel);
    const trackDelayed = (fn, delay) => {
      const id = setTimeout(() => track(fn()), delay);
      track(() => clearTimeout(id));
    };
    if (name === "draft") track(typeText(e.draft, draft, DRAFT_TYPE_MS));else if (name === "think-collapse") collapseThinkBox(e.thinkBox);else if (name === "reading") {
      if (hasScore) track(countTo(e.score, 0, Math.round(score), RING_MS));
      dimensions.forEach((d, i) => trackDelayed(() => countTo(e["dim-" + i], 0, d.value, RING_MS - i * 40), i * BAR_STAGGER_MS));
    } else if (name === "rewrite-after") track(typeText(e.after, after, AFTER_TYPE_MS));else if (name === "rewrite-deltas") {
      deltas.forEach((d, i) => trackDelayed(() => countTo(e["delta-" + i], d.from, d.to, DELTA_MS), i * DELTA_STAGGER_MS));
    }
  }, [draft, after, score, hasScore, dimensions, deltas]);
  const applyInstant = React.useCallback(name => {
    const e = els.current;
    if (name === "draft" && e.draft) e.draft.textContent = draft;else if (name === "think-collapse" && e.thinkBox) {
      e.thinkBox.style.transition = "none";
      e.thinkBox.style.height = COLLAPSED_PX + "px";
    } else if (name === "reading") {
      if (hasScore && e.score) e.score.textContent = String(Math.round(score));
      dimensions.forEach((d, i) => {
        if (e["dim-" + i]) e["dim-" + i].textContent = String(d.value);
      });
    } else if (name === "rewrite-after" && e.after) {
      e.after.textContent = after;
    } else if (name === "rewrite-deltas") {
      deltas.forEach((d, i) => {
        if (e["delta-" + i]) e["delta-" + i].textContent = String(d.to);
      });
    }
  }, [draft, after, score, hasScore, dimensions, deltas]);
  const playFrom = React.useCallback(fromIndex => {
    clearTimers();
    let acc = 0;
    for (let i = fromIndex + 1; i <= timeline.last; i++) {
      acc += timeline.delays[i];
      const idx = i;
      const name = timeline.names[idx];
      timersRef.current.push(setTimeout(() => {
        setStep(idx);
        runAnimatedFor(name);
      }, acc));
    }
  }, [timeline, runAnimatedFor]);
  const gotoStep = React.useCallback(name => {
    const idx = timeline.index[name];
    if (idx == null) return;
    clearTimers();
    cancelActiveAnims();
    for (let i = 0; i <= idx; i++) applyInstant(timeline.names[i]);
    setStep(idx);
  }, [timeline, applyInstant]);
  const replay = React.useCallback(() => {
    clearTimers();
    cancelActiveAnims();
    const e = els.current;
    if (e.draft) e.draft.textContent = "";
    if (e.after) e.after.textContent = "";
    if (e.score) e.score.textContent = "0";
    dimensions.forEach((d, i) => {
      if (e["dim-" + i]) e["dim-" + i].textContent = "0";
    });
    deltas.forEach((d, i) => {
      if (e["delta-" + i]) e["delta-" + i].textContent = String(d.from);
    });
    expandThinkBoxInstant(e.thinkBox);
    setStep(-1);
    playFrom(-1);
  }, [playFrom, dimensions, deltas]);

  // First run: reduced motion / compact apply every milestone's end state once and never play.
  // Otherwise wait for the panel to actually be on screen, then run the timeline once.
  const wrapRef = React.useRef(null);
  React.useEffect(() => {
    if (reduced || compact) {
      timeline.names.forEach(applyInstant);
      return;
    }
    const node = wrapRef.current;
    if (!node || typeof IntersectionObserver === "undefined") {
      playFrom(-1);
      return clearTimers;
    }
    let started = false;
    const io = new IntersectionObserver(entries => {
      if (started || !entries.some(en => en.isIntersecting)) return;
      started = true;
      playFrom(-1);
      io.disconnect();
    }, {
      threshold: 0.15
    });
    io.observe(node);
    return () => {
      io.disconnect();
      clearTimers();
    };
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, []);

  // ---- the seek/test hook: last-mounted instance wins, harmless in production ----
  React.useEffect(() => {
    window.__versoHeroSequencer = {
      stepNames: timeline.names,
      gotoStep,
      replay,
      // Freezes for the two continuous draws no static jump can capture: hold everything through
      // the previous milestone at rest, then paint the ring/bars or the deltas at an exact
      // fraction with their transitions switched off, so the frame is stable under a screenshot
      // instead of a real in-flight animation a screenshot's own latency could miss or catch at
      // an unrepeatable instant.
      showReadingPartial(fraction) {
        gotoStep("think-collapse");
        const f = Math.max(0, Math.min(1, fraction));
        requestAnimationFrame(() => {
          const e = els.current;
          if (e.ring) {
            e.ring.style.transition = "none";
            e.ring.style.strokeDashoffset = String(ringC * (1 - scoreFrac * f));
          }
          if (e.score) e.score.textContent = String(Math.round(score * f));
          dimensions.forEach((d, i) => {
            if (e["bar-" + i]) {
              e["bar-" + i].style.transition = "none";
              e["bar-" + i].style.transform = "scaleX(" + f + ")";
            }
            if (e["dim-" + i]) e["dim-" + i].textContent = String(Math.round(d.value * f));
          });
        });
      },
      showRewritePartial(fraction) {
        gotoStep("rewrite-after");
        // The delta row's own container is a clip-path reveal keyed to `step` reaching
        // "rewrite-deltas" (see `at("rewrite-deltas")` in the JSX) — advancing `step` one more
        // stop, on its own, never re-triggers `runAnimatedFor` (that only ever runs from
        // `playFrom`'s timers, never from a `step`-watching effect), so this opens the row without
        // restarting its count.
        setStep(timeline.index["rewrite-deltas"]);
        const f = Math.max(0, Math.min(1, fraction));
        requestAnimationFrame(() => {
          const e = els.current;
          deltas.forEach((d, i) => {
            if (e["delta-" + i]) e["delta-" + i].textContent = String(Math.round(d.from + (d.to - d.from) * f));
          });
        });
      }
    };
    return () => {
      if (window.__versoHeroSequencer && window.__versoHeroSequencer.gotoStep === gotoStep) delete window.__versoHeroSequencer;
    };
  }, [timeline, gotoStep, replay, deltas, dimensions, score, scoreFrac, ringC]);
  const at = name => step >= timeline.index[name];
  const activeLine = (() => {
    for (let i = 0; i < thinking.length; i++) {
      if (step === timeline.index["think-" + i]) return i;
    }
    if (step === timeline.index["selflint-line"]) return "self";
    return null;
  })();
  const collapsed = at("think-collapse");
  const settled = step === timeline.last;
  if (compact) {
    return /*#__PURE__*/React.createElement("div", {
      "data-ds": "scene-character",
      "data-layout": "document",
      style: {
        position: "relative",
        width: "100%",
        ...style
      },
      ...rest
    }, /*#__PURE__*/React.createElement("div", {
      "aria-hidden": "true",
      "data-ds": "verso-hero",
      style: {
        display: "flex",
        flexDirection: "column",
        gap: 14,
        padding: "20px var(--stage-pad-x) 24px",
        border: "1px solid var(--story-line)",
        background: "var(--story-raised)",
        boxShadow: "var(--story-shadow)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, label), /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
      size: "micro",
      tone: "muted"
    }, "Draft"), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: "4px 0 0",
        color: "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 12.5,
        lineHeight: 1.4,
        letterSpacing: "-0.02em"
      }
    }, draft)), /*#__PURE__*/React.createElement("div", {
      style: {
        paddingTop: 11,
        borderTop: "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        alignItems: "center",
        gap: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        width: 6,
        height: 6,
        borderTop: "1px solid var(--story-muted)",
        borderRight: "1px solid var(--story-muted)",
        transform: "rotate(45deg)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 9,
        fontWeight: 700
      }
    }, thinkingLabel, " · ", thinking.length, " steps"))), /*#__PURE__*/React.createElement("div", {
      style: {
        paddingTop: 11,
        borderTop: "1px solid var(--story-line)",
        display: "flex",
        gap: 14,
        alignItems: "center"
      }
    }, hasScore && /*#__PURE__*/React.createElement(Ring, {
      size: 56,
      stroke: 5,
      score: score,
      outOf: outOf,
      r: (56 - 5) / 2,
      filled: true
    }), /*#__PURE__*/React.createElement("div", {
      style: {
        flex: 1,
        minWidth: 0
      }
    }, dimensions.map(d => /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        display: "grid",
        gridTemplateColumns: "minmax(68px,0.9fr) minmax(0,1.4fr) 24px",
        gap: 7,
        alignItems: "center",
        padding: "3px 0"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 8.5,
        fontWeight: 700
      }
    }, d.name), /*#__PURE__*/React.createElement("span", {
      style: {
        position: "relative",
        height: 4,
        display: "block",
        background: "var(--story-band)"
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        position: "absolute",
        inset: "0 auto 0 0",
        width: Math.max(0, Math.min(100, d.value)) + "%",
        background: "var(--story-ink)"
      }
    })), /*#__PURE__*/React.createElement("strong", {
      style: {
        color: "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 11.5,
        fontWeight: 700,
        letterSpacing: "-0.03em",
        textAlign: "right",
        fontVariantNumeric: "tabular-nums"
      }
    }, d.value))), readingVerdict && /*#__PURE__*/React.createElement("p", {
      style: {
        margin: "7px 0 0",
        color: "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 12.5,
        fontWeight: 650,
        lineHeight: 1.3,
        letterSpacing: "-0.02em"
      }
    }, readingVerdict))), /*#__PURE__*/React.createElement("div", {
      style: {
        paddingTop: 11,
        borderTop: "1px solid var(--story-line)"
      }
    }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
      size: "micro",
      tone: "signal"
    }, "After"), /*#__PURE__*/React.createElement("p", {
      style: {
        margin: "4px 0 0",
        color: "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 12.5,
        fontWeight: 560,
        lineHeight: 1.4,
        letterSpacing: "-0.02em"
      }
    }, after), /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 9,
        display: "grid",
        gridTemplateColumns: "1fr 1fr",
        gap: "5px 12px"
      }
    }, deltas.map(d => {
      const up = Number(d.to) > Number(d.from);
      return /*#__PURE__*/React.createElement("div", {
        key: d.name
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          color: "var(--story-muted)",
          fontFamily: "var(--font-mono)",
          fontSize: 8.5,
          fontWeight: 700
        }
      }, d.name, " "), /*#__PURE__*/React.createElement("span", {
        style: {
          fontFamily: "var(--font-sans)",
          fontVariantNumeric: "tabular-nums"
        }
      }, /*#__PURE__*/React.createElement("span", {
        style: {
          color: "var(--story-muted)",
          fontSize: 11,
          textDecoration: "line-through"
        }
      }, d.from), /*#__PURE__*/React.createElement("span", {
        "aria-hidden": "true",
        style: {
          margin: "0 3px",
          color: "var(--story-muted)",
          fontFamily: "var(--font-mono)",
          fontSize: 8
        }
      }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
        style: {
          color: "var(--story-accent)",
          fontSize: 12,
          fontWeight: 700
        }
      }, d.to)));
    })), /*#__PURE__*/React.createElement("div", {
      style: {
        marginTop: 10
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-accent)",
        fontFamily: "var(--font-sans)",
        fontSize: 11.5,
        fontWeight: 700,
        letterSpacing: "-0.02em",
        borderBottom: "1.5px solid var(--story-accent)",
        paddingBottom: 3
      }
    }, acceptLabel))), (selfLintLine || selfLintVerdict) && /*#__PURE__*/React.createElement("div", {
      style: {
        paddingTop: 11,
        borderTop: "1px solid var(--story-line)"
      }
    }, selfLintLine && /*#__PURE__*/React.createElement("div", {
      style: {
        display: "flex",
        alignItems: "baseline",
        gap: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        flex: "0 0 auto",
        width: 4,
        height: 4,
        borderRadius: "50%",
        background: "var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-sans)",
        fontSize: 11.5,
        lineHeight: 1.4,
        letterSpacing: "-0.015em"
      }
    }, selfLintLine)), selfLintVerdict && /*#__PURE__*/React.createElement("p", {
      style: {
        margin: "6px 0 0",
        color: "var(--story-ink)",
        fontFamily: "var(--font-sans)",
        fontSize: 12.5,
        fontWeight: 650,
        lineHeight: 1.35,
        letterSpacing: "-0.02em"
      }
    }, selfLintVerdict))));
  }
  return /*#__PURE__*/React.createElement("div", {
    ref: wrapRef,
    "data-ds": "scene-character",
    style: {
      position: "absolute",
      zIndex: 5,
      top: 0,
      right: 0,
      bottom: 0,
      width: "51%",
      overflow: "hidden",
      ...style
    },
    ...rest
  }, /*#__PURE__*/React.createElement("div", {
    "aria-hidden": "true",
    "data-ds": "verso-hero",
    style: {
      display: "flex",
      flexDirection: "column",
      height: "100%",
      justifyContent: "center",
      padding: "0 clamp(14px,2.4vw,26px)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      border: "1px solid var(--story-line)",
      background: "var(--story-raised)",
      boxShadow: "var(--story-shadow)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      justifyContent: "space-between",
      alignItems: "baseline",
      gap: 10,
      padding: "9px 13px",
      borderBottom: "1px solid var(--story-line)",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, label), !reduced && /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-block",
      ...(settled ? drawn : revealStyle(false))
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.StageAction, {
    accent: "signal",
    tabIndex: -1,
    onClick: settled ? replay : undefined,
    style: {
      fontSize: 10.5,
      gap: 7,
      padding: "0 0 3px"
    }
  }, replayLabel))), /*#__PURE__*/React.createElement("div", {
    style: {
      padding: "12px 13px 13px"
    }
  }, /*#__PURE__*/React.createElement("div", null, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    size: "micro",
    tone: "muted"
  }, "Draft"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "4px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12.5,
      lineHeight: 1.4,
      letterSpacing: "-0.02em"
    }
  }, /*#__PURE__*/React.createElement("span", {
    ref: setRef("draft")
  }))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      paddingTop: 11,
      borderTop: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "center",
      gap: 7,
      height: COLLAPSED_PX - 4
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      width: 6,
      height: 6,
      borderTop: "1px solid var(--story-muted)",
      borderRight: "1px solid var(--story-muted)",
      transform: collapsed ? "rotate(45deg)" : "rotate(135deg)",
      transition: "transform var(--dur-fast) " + EASE
    }
  }), /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, thinkingLabel), /*#__PURE__*/React.createElement("span", {
    style: {
      display: "inline-block",
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700,
      ...(collapsed ? drawn : revealStyle(false))
    }
  }, "· ", thinking.length, " steps")), /*#__PURE__*/React.createElement("div", {
    ref: setRef("thinkBox"),
    style: {
      overflow: "hidden",
      height: "auto"
    }
  }, /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      flexDirection: "column",
      gap: 5,
      paddingTop: 6
    }
  }, thinking.map((t, i) => {
    const revealed = step >= timeline.index["think-" + i];
    const active = activeLine === i;
    return /*#__PURE__*/React.createElement("div", {
      key: i,
      style: {
        display: "flex",
        alignItems: "baseline",
        gap: 7
      }
    }, /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        flex: "0 0 auto",
        width: 4,
        height: 4,
        borderRadius: "50%",
        background: active ? "var(--story-accent)" : "var(--story-line)"
      }
    }), /*#__PURE__*/React.createElement("span", {
      ref: setRef("think-" + i),
      style: {
        display: "inline-block",
        fontFamily: "var(--font-sans)",
        fontSize: 11.5,
        lineHeight: 1.4,
        letterSpacing: "-0.015em",
        ...(revealed ? drawn : revealStyle(false)),
        ...(active ? shimmerStyle : revealed ? {
          color: "var(--story-muted)"
        } : null)
      }
    }, t));
  })))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      paddingTop: 11,
      borderTop: "1px solid var(--story-line)",
      display: "flex",
      gap: 14,
      alignItems: "center"
    }
  }, hasScore && /*#__PURE__*/React.createElement(Ring, {
    size: RING_SIZE,
    stroke: RING_STROKE,
    r: ringR,
    score: score,
    outOf: outOf,
    filled: at("reading"),
    setScoreRef: setRef("score"),
    setRingRef: setRef("ring")
  }), /*#__PURE__*/React.createElement("div", {
    style: {
      flex: 1,
      minWidth: 0
    }
  }, dimensions.map((d, i) => /*#__PURE__*/React.createElement("div", {
    key: d.name,
    style: {
      display: "grid",
      gridTemplateColumns: "minmax(58px,0.8fr) minmax(0,1.4fr) 24px",
      gap: 8,
      alignItems: "center",
      padding: "3px 0"
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 9,
      fontWeight: 700
    }
  }, d.name), /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      height: 4,
      display: "block",
      background: "var(--story-band)"
    }
  }, /*#__PURE__*/React.createElement("span", {
    ref: setRef("bar-" + i),
    "aria-hidden": "true",
    style: {
      position: "absolute",
      inset: "0 0 0 0",
      transformOrigin: "left",
      background: "var(--story-ink)",
      transform: at("reading") ? "scaleX(1)" : "scaleX(0)",
      transition: "transform " + RING_MS + "ms " + EASE,
      transitionDelay: at("reading") ? i * BAR_STAGGER_MS + "ms" : "0ms"
    }
  })), /*#__PURE__*/React.createElement("strong", {
    ref: setRef("dim-" + i),
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 11.5,
      fontWeight: 700,
      letterSpacing: "-0.03em",
      textAlign: "right",
      fontVariantNumeric: "tabular-nums"
    }
  }, "0"))), readingVerdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12.5,
      fontWeight: 650,
      lineHeight: 1.3,
      letterSpacing: "-0.02em",
      ...(at("reading-verdict") ? drawn : revealStyle(false))
    }
  }, readingVerdict))), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      paddingTop: 11,
      borderTop: "1px solid var(--story-line)"
    }
  }, /*#__PURE__*/React.createElement(__ds_scope.Eyebrow, {
    size: "micro",
    tone: "signal"
  }, "After"), /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "4px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12.5,
      fontWeight: 560,
      lineHeight: 1.4,
      letterSpacing: "-0.02em"
    }
  }, /*#__PURE__*/React.createElement("span", {
    ref: setRef("after")
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 9,
      display: "grid",
      gridTemplateColumns: "1fr 1fr",
      gap: "5px 12px"
    }
  }, deltas.map((d, i) => {
    const up = Number(d.to) > Number(d.from);
    return /*#__PURE__*/React.createElement("div", {
      key: d.name,
      style: {
        ...(at("rewrite-deltas") ? drawn : revealStyle(false))
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 8.5,
        fontWeight: 700
      }
    }, d.name, " "), /*#__PURE__*/React.createElement("span", {
      style: {
        fontFamily: "var(--font-sans)",
        fontVariantNumeric: "tabular-nums"
      }
    }, /*#__PURE__*/React.createElement("span", {
      style: {
        color: "var(--story-muted)",
        fontSize: 11,
        textDecoration: "line-through"
      }
    }, d.from), /*#__PURE__*/React.createElement("span", {
      "aria-hidden": "true",
      style: {
        margin: "0 3px",
        color: "var(--story-muted)",
        fontFamily: "var(--font-mono)",
        fontSize: 8
      }
    }, up ? "↑" : "↓"), /*#__PURE__*/React.createElement("strong", {
      ref: setRef("delta-" + i),
      style: {
        color: "var(--story-accent)",
        fontSize: 12,
        fontWeight: 700
      }
    }, d.from)));
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 10
    }
  }, /*#__PURE__*/React.createElement("span", {
    style: {
      position: "relative",
      display: "inline-block",
      color: "var(--story-accent)",
      fontFamily: "var(--font-sans)",
      fontSize: 11.5,
      fontWeight: 700,
      letterSpacing: "-0.02em",
      ...(at("rewrite-cta") ? drawn : revealStyle(false))
    }
  }, acceptLabel, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      position: "absolute",
      left: 0,
      right: 0,
      bottom: -3,
      height: 1.5,
      background: "var(--story-accent)",
      transformOrigin: "left",
      transform: at("rewrite-cta") ? "scaleX(1)" : "scaleX(0)",
      transition: "transform var(--dur-mid) " + EASE,
      transitionDelay: at("rewrite-cta") ? "var(--dur-base)" : "0ms"
    }
  })))), (selfLintLine || selfLintVerdict) && /*#__PURE__*/React.createElement("div", {
    style: {
      marginTop: 12,
      paddingTop: 11,
      borderTop: "1px solid var(--story-line)"
    }
  }, selfLintLine && /*#__PURE__*/React.createElement("div", {
    style: {
      display: "flex",
      alignItems: "baseline",
      gap: 7
    }
  }, /*#__PURE__*/React.createElement("span", {
    "aria-hidden": "true",
    style: {
      flex: "0 0 auto",
      width: 4,
      height: 4,
      borderRadius: "50%",
      background: activeLine === "self" ? "var(--story-accent)" : "var(--story-line)"
    }
  }), /*#__PURE__*/React.createElement("span", {
    ref: setRef("selflint-line"),
    style: {
      display: "inline-block",
      fontFamily: "var(--font-sans)",
      fontSize: 11.5,
      lineHeight: 1.4,
      letterSpacing: "-0.015em",
      ...(at("selflint-line") ? drawn : revealStyle(false)),
      ...(activeLine === "self" ? shimmerStyle : at("selflint-line") ? {
        color: "var(--story-muted)"
      } : null)
    }
  }, selfLintLine)), selfLintVerdict && /*#__PURE__*/React.createElement("p", {
    style: {
      margin: "6px 0 0",
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: 12.5,
      fontWeight: 650,
      lineHeight: 1.35,
      letterSpacing: "-0.02em",
      ...(at("selflint-verdict") ? drawn : revealStyle(false))
    }
  }, selfLintVerdict))))));
}

/** The ring alone, factored out so the compact (phone) render and the full sequence can share it.
 *  In compact mode it is always `filled` and carries no refs — nothing about it plays. */
function Ring({
  size,
  stroke,
  r,
  score,
  outOf,
  filled,
  setScoreRef,
  setRingRef
}) {
  const c = 2 * Math.PI * r;
  const frac = Math.max(0, Math.min(1, score / outOf));
  return /*#__PURE__*/React.createElement("div", {
    style: {
      position: "relative",
      flex: "0 0 auto",
      width: size,
      height: size
    }
  }, /*#__PURE__*/React.createElement("svg", {
    width: size,
    height: size,
    style: {
      display: "block",
      transform: "rotate(-90deg)"
    },
    "aria-hidden": "true"
  }, /*#__PURE__*/React.createElement("circle", {
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-line)",
    strokeWidth: stroke
  }), /*#__PURE__*/React.createElement("circle", {
    ref: setRingRef,
    cx: size / 2,
    cy: size / 2,
    r: r,
    fill: "none",
    stroke: "var(--story-accent)",
    strokeWidth: stroke,
    strokeLinecap: "butt",
    strokeDasharray: c,
    style: {
      strokeDashoffset: filled ? c * (1 - frac) : c,
      transition: "stroke-dashoffset " + RING_MS + "ms " + EASE
    }
  })), /*#__PURE__*/React.createElement("div", {
    style: {
      position: "absolute",
      inset: 0,
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
      justifyContent: "center"
    }
  }, /*#__PURE__*/React.createElement("strong", {
    ref: setScoreRef,
    style: {
      color: "var(--story-ink)",
      fontFamily: "var(--font-sans)",
      fontSize: size >= 70 ? 22 : 18,
      fontWeight: 700,
      lineHeight: 0.9,
      letterSpacing: "-0.05em",
      fontVariantNumeric: "tabular-nums"
    }
  }, setScoreRef ? 0 : score), /*#__PURE__*/React.createElement("span", {
    style: {
      marginTop: 3,
      color: "var(--story-muted)",
      fontFamily: "var(--font-mono)",
      fontSize: 7.5,
      fontWeight: 700
    }
  }, "of ", outOf)));
}
Object.assign(__ds_scope, { VersoHero });
})(); } catch (e) { __ds_ns.__errors.push({ path: "components\\case\\VersoHero.jsx", error: String((e && e.message) || e) }); }

__ds_ns.ScrollCue = __ds_scope.ScrollCue;

__ds_ns.StageAction = __ds_scope.StageAction;

__ds_ns.ArtifactFigure = __ds_scope.ArtifactFigure;

__ds_ns.CardGrid = __ds_scope.CardGrid;

__ds_ns.DataTable = __ds_scope.DataTable;

__ds_ns.FactsPanel = __ds_scope.FactsPanel;

__ds_ns.GridCard = __ds_scope.GridCard;

__ds_ns.Ledger = __ds_scope.Ledger;

__ds_ns.Outcome = __ds_scope.Outcome;

__ds_ns.Pipeline = __ds_scope.Pipeline;

__ds_ns.PullQuote = __ds_scope.PullQuote;

__ds_ns.ResumeEntry = __ds_scope.ResumeEntry;

__ds_ns.SourceMap = __ds_scope.SourceMap;

__ds_ns.TenGrid = __ds_scope.TenGrid;

__ds_ns.Eyebrow = __ds_scope.Eyebrow;

__ds_ns.LineArrow = __ds_scope.LineArrow;

__ds_ns.ProvenanceNote = __ds_scope.ProvenanceNote;

__ds_ns.RegistrationMark = __ds_scope.RegistrationMark;

__ds_ns.Tag = __ds_scope.Tag;

__ds_ns.PaperFrame = __ds_scope.PaperFrame;

__ds_ns.Stage = __ds_scope.Stage;

__ds_ns.StageCopy = __ds_scope.StageCopy;

__ds_ns.StageCurtain = __ds_scope.StageCurtain;

__ds_ns.StorySection = __ds_scope.StorySection;

__ds_ns.ChapterNav = __ds_scope.ChapterNav;

__ds_ns.IndexRail = __ds_scope.IndexRail;

__ds_ns.ProjectSpineItem = __ds_scope.ProjectSpineItem;

__ds_ns.UtilityNav = __ds_scope.UtilityNav;

__ds_ns.RuleSplit = __ds_scope.RuleSplit;

__ds_ns.ResearchGrid = __ds_scope.ResearchGrid;

__ds_ns.FingerprintBars = __ds_scope.FingerprintBars;

__ds_ns.EvidenceTrace = __ds_scope.EvidenceTrace;

__ds_ns.IndexRows = __ds_scope.IndexRows;

__ds_ns.DisciplineVenn = __ds_scope.DisciplineVenn;

__ds_ns.VoiceScoreRing = __ds_scope.VoiceScoreRing;

__ds_ns.BeforeAfter = __ds_scope.BeforeAfter;

__ds_ns.VoiceProfile = __ds_scope.VoiceProfile;

__ds_ns.Panel = __ds_scope.Panel;

__ds_ns.GateRun = __ds_scope.GateRun;

__ds_ns.CaptureFigure = __ds_scope.CaptureFigure;

__ds_ns.PanelRow = __ds_scope.PanelRow;

__ds_ns.Report = __ds_scope.Report;

__ds_ns.ReportSection = __ds_scope.ReportSection;

__ds_ns.DocFigure = __ds_scope.DocFigure;

__ds_ns.NoteRef = __ds_scope.NoteRef;

__ds_ns.Endnotes = __ds_scope.Endnotes;

__ds_ns.References = __ds_scope.References;

__ds_ns.Toolchain = __ds_scope.Toolchain;

__ds_ns.Finding = __ds_scope.Finding;

__ds_ns.PaperTable = __ds_scope.PaperTable;

__ds_ns.CaseSection = __ds_scope.CaseSection;

__ds_ns.StoryBeat = __ds_scope.StoryBeat;

__ds_ns.CaseQuote = __ds_scope.CaseQuote;

__ds_ns.MetricStrip = __ds_scope.MetricStrip;

__ds_ns.CaseNav = __ds_scope.CaseNav;

__ds_ns.MobileIdentityBar = __ds_scope.MobileIdentityBar;

__ds_ns.MobileMenu = __ds_scope.MobileMenu;

__ds_ns.CaseMeta = __ds_scope.CaseMeta;

__ds_ns.VersoHero = __ds_scope.VersoHero;

})();
